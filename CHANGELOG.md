# سجل التغييرات (Changelog)

كل التغييرات الملموسة في منصة IBP، منظّمة حسب المراحل. الصيغة مستلهمة من [Keep a Changelog](https://keepachangelog.com).

## [إصلاح: ثبات أعداد تابز الحالة في صفحة خدمة العملاء] ✅
- **الخلل:** أعداد التابز (مفتوح/قيد المعالجة/مُرسَل للمؤمِّن/مغلق) كانت تُحسب من القائمة المُرشَّحة على الخادم بالحالة نفسها، فعند اختيار تاب تنهار أعداد بقيّة التابز إلى صفر وتتغيّر عند التنقّل.
- **الإصلاح:** جلبٌ واحد ضمن نطاق «طلباتي» فقط (بلا ترشيح حالة على الخادم) ⇒ الأعداد محسوبة من المجموعة الكاملة (ثابتة عبر كل الحالات)، وترشيح الحالة على العميل (بلا إعادة جلب/وميض). تاب «الكل» صار يعرض الإجمالي. تغيير واجهة فقط؛ الـbackend كما هو. مُتحقَّق بصريًّا في المتصفّح (9+4+3+3 = 19، ثابتة عند التنقّل بين التابز).

## [إصلاحات وتحسينات: الجلسة + حقول العميل + الاستيراد الجماعي] ✅
- **إصلاح الخروج التلقائي (السبب الجذري):** الكود كان 8 ساعات، لكن قيم `JWT_EXPIRES_IN=15m` **قديمة** في `infra/k8s/configmap.yaml` وتوثيق `coolify.md`/`14`/`04`/`07` كانت تُطبَّق عند النشر ⇒ خروج بعد ~15 دقيقة. صُحِّحت كلها إلى `8h`. (تنبيه تشغيلي: تأكّد أن متغيّر Coolify على staging ليس 15m.)
- **حقول العميل — وضوح وتحقّق:** مسمّيات أوضح (اسم العميل/البريد الإلكتروني/السجل التجاري (CR))؛ **تحقّق صحّة المدخل**: السجل التجاري والهوية **10 أرقام**، الرقم الضريبي **15 رقمًا** (`@Matches` + إدخال أرقام فقط بطول محدّد وتلميح)؛ **حقول تواصل جديدة**: **اسم شخص التواصل** + **الجوال** (`05XXXXXXXX`) + **الهاتف الثابت** (`01XXXXXXXX`) مُتحقَّقة. migration `client_contact_fields` (`landline`/`contactName`).
- **الاستيراد الجماعي من CSV** (`lib/block-csv.ts`): على كل كتلة متكررة (مركبات/أعضاء/أرواح…) زرّا **«تنزيل قالب»** + **«استيراد CSV»** — للأساطيل والمجموعات الكبيرة بدل الإدخال صفًّا صفًّا. مُحلِّل نقي بلا مكتبة (اقتباس/BOM/CRLF) + مطابقة الأعمدة بالتسمية العربية/الإنجليزية أو المفتاح + **تحقّق كل صفّ** (مطلوب/خيارات/أرقام/هوية) + تقرير أخطاء دقيق بالصف والعمود؛ القالب بترميز BOM ليفتح صحيحًا في Excel العربية.
- e2e +2 (أطوال CR/VAT/الهوية · أرقام التواصل جوال/ثابت) ⇒ **282/282**.

## [توسّع ما قبل العرض — المنتجات/المستندات + صلاحيات المنتج + نموذج التحصيل] ✅
إكمال البنود المؤجَّلة بعد قرار «الوقت يكفي» — كلها **متوافقة رجعيًا ومُختبَرة e2e 280/280**:
- **إدارة المنتجات + المستودع المركزي للمستندات (I)**: صفحة `/tenant/products` (كتالوج 10 فئات/47 فرعًا بالمعالجة الضريبية + جاهزية النموذج + إحصاءات إنتاج المستأجر لكل فرع + طلب سريع) · صفحة `/tenant/documents` (مستودع موحّد لكل مستندات الشركة رسمية/مرفقات بفلترة/بحث وروابط للكيان) · endpoints `/catalog/stats` + `/documents/all` · عنصرا تنقّل جديدان.
- **صلاحيات على مستوى المنتج (H)** — **متوافقة رجعيًا**: `User.allowedProductLines` (فارغ = كل الفروع، فلا يتأثّر مستخدم قائم) تُطبَّق على قوائم الطلبات/الوثائق + إنشاء الطلب · `ProductScopeService` · endpoint `/staff/:id/product-scope` + محرِّر checkboxes مُجمَّع بالفئة في صفحة الموظف.
- **نموذج التحصيل القابل للتهيئة (#32)** — بتصميم احترافي: **يُضبط على بطاقة العميل** (collect_full الافتراضي | direct) و**يُبصَم على الوثيقة عند الإصدار** (مصدر الحقيقة، سلامة تاريخية لا تتأثّر بتغيير الإعداد لاحقًا). المباشر ⇒ **ذمّة عمولة على المؤمِّن** (حساب 0104) + **لا أمانة ولا تسوية** + إشعار مدين بالرسوم فقط. حرّاس المصبّ (payables/summary/overview) تستثني الدفع المباشر. تفرّع القيد في **موضع واحد** — نطاق تأثير محدود.
- migrations: `allowedProductLines` (User) · `collectionModel` (Client+Policy) · حساب COA جديد (0104). بذرة الديمو: مستخدم/عميل نموذجيان. e2e +4 (المسارين للتحصيل + توازن القيد + تصفية صلاحيات المنتج) ⇒ **280/280**.

## [جولة تطوير ما قبل العرض — 7 ميزات: تجربة المستخدم + نظرة المالك + الامتثال] ✅
حزمة تطوير موجّهة لعرض شركة وساطة، تعالج فجوات UX وتُكمل الامتثال (كلها مُختبَرة e2e **276/276**):
- **الإشعارات — النقر ينقل لمكان العمل**: كان الإشعار يُعلَّم كمقروء فقط؛ الآن يفتح الوجهة ذات الصلة (مهمة CRM ⇒ `/crm`، مطالبة ⇒ `/claims`، طلب خدمة ⇒ `/service`…) للموظف والعميل (`lib/notif-routes.ts`).
- **تجربة إنشاء الطلب**: بحث سريع عن فرع التأمين (بدل قائمة منسدلة) + شارة المعالجة الضريبية · **نموذج عرض السعر**: احتساب الضريبة/الإجماليات/العمولة **حيًّا** بنسبة الفرع (حياة 0%) بلا زر احتساب ولا إدخال يدوي.
- **نظرة المالك المالية** (تبويب «نظرة عامة» يتصدّر المالية): قائمة دخل مبسّطة (عمولات + رسوم خدمة − عمولات الوسطاء الفرعيين = صافي دخل الوساطة، وضريبة القيمة المضافة مستبعَدة كتحصيل نيابة) + مؤشّرات صحة الأعمال (القسط المُدار، معدّل العمولة الفعلي، تحصيل العمولات، الذمم، الأمانات، نسبة الخسارة) + اتجاه صافي الدخل 6 أشهر · `/finance/overview`.
- **تطوير خدمة العملاء** (أبعد من تغيير الحالة): إسناد لموظف + أولوية (منخفضة/عادية/عالية/عاجلة) + ملاحظات وخطّ زمني + تفاصيل الطلب + فلاتر (الحالة/طلباتي) + مؤشّر عمر الطلب · إشعار `staff_service_assigned`.
- **إثراء CRM**: مُدوِّن نشاط يدوي (مكالمة/بريد/اجتماع/ملاحظة) + مهمة متابعة مرتبطة بالصفقة + خطّ زمني بأيقونات — يُكمل حلقة CRM اليومية.
- **الملاحق (Endorsements) الاحترافية**: اتجاه مالي (قسط إضافي/مُرتجَع/بلا فرق) + معاينة مالية حيّة (ضريبة بنسبة الفرع + المستند الناتج مدين/دائن) + إرشاد الإلغاء للاحتساب النسبي.
- **إكمال امتثال فاتورة ZATCA**: رمز نوع الفاتورة (388) + بنود مفصّلة بفئة/نسبة ضريبة لكل بند + الرقم الضريبي والعنوان الوطني للبائع (صفحة الشركة) والمشتري (عميل/مؤمِّن).
- migrations: عناوين وطنية (Tenant) + رقم ضريبي/عنوان (Insurer) + إسناد/أولوية/تحديث (ServiceRequest). بذرة الديمو مُثراة بالحقول الجديدة. e2e +2 والإجمالي **276/276** عبر 40 ملفاً.

## [إدارة شركات التأمين (المؤمِّنون) — سجلّ + اتفاقيات/نِسب + تسوية + بنك] ✅
سدّ أكبر فجوة مقابل نظام وساطة ناضج: كان المؤمِّن مجرّد **نصّ** على الوثيقة — الآن **كيان «شركة تأمين» كامل**:
- نموذج `Insurer` (اسم عربي/إنجليزي · ترخيص · **نسبة العمولة المتّفق عليها** · **دورة التسوية/مهلة السداد** · **الحساب البنكي (بنك/آيبان)** · تواصل · حالة).
- CRUD كامل (`/insurers`، صلاحية المالية) + **إحصاءات إنتاج فعلية لكل شركة** (عدد وثائق/إجمالي أقساط/عمولة) محسوبة من الوثائق عبر الاسم.
- صفحة `/tenant/insurers` (بطاقات: الإنتاج + الاتفاقية + البنك + الترخيص + نموذج إضافة/تعديل) + عنصر تنقّل تحت المالية · بذرة 4 شركات لحساب GIB بأرقام فعلية.
- e2e +5 (إنشاء بإحصاءات · نسبة>100 ⇒ 400 · تعديل/حذف · عزل · صلاحية المالية 403). الإجمالي **274/274**. بناء الإنتاج **109/109**.

## [سجل التدقيق كصفحة فعلية — بأسماء المنفّذين (مستأجر + سوبر أدمن)] ✅
كان سجل التدقيق الثابت مُغذّى بكل العمليات الحسّاسة لكن **بلا واجهة** و**بمعرّف المنفّذ لا اسمه**. اكتمل الآن:
- **تحويل `userId` ⇒ اسم المنفّذ** (`AuditViewService` — دفعة واحدة، مع تسميات خاصة للنظام/المنصّة).
- **صفحة لشركة الوساطة** `/tenant/settings/audit` (صلاحية **الالتزام**): «من فعل ماذا ومتى» — المنفّذ/الفعل/العنصر/IP/الجهاز/الوقت + فلترة + **ملاحظة الثبات** (immutable).
- **صفحة سوبر أدمن** `/admin/audit`: السجل **عابر الحسابات** بأسماء المنفّذين (لمراجعة الهيئة) + فلترة بالحساب/الفعل/العنصر.
- الأفعال والعناصر **مترجمة ثنائية اللغة** (إنشاء/اعتماد/تسوية مؤمِّن… بدل `create`/`insurer_settlement`). عنصر تنقّل في القائمتين.
- e2e +5 (**تحويل المعرّف لاسم فعليًّا** · فلترة · صلاحية الالتزام 403 · عزل بين المستأجرين · عرض المنصّة عابر الحسابات بالأسماء). الإجمالي **269/269**. بناء الإنتاج **107/107**.

## [إصلاح: نموذج التواصل (Lead) + صفحة طلبات التواصل في السوبر أدمن] ✅
- **إصلاح خلل الإرسال**: كانت `createLead` تُسيء استخدام مانع القوّة الغاشمة (تسجّل «فشلًا» عند كل إرسال ناجح) فيُقفَل بريد المُرسِل بعد بضعة طلبات («محاولات كثيرة»). الآن **يُسمح بطلبات متعدّدة** من نفس البريد (استفسارات/صفحات مختلفة)، ونمنع فقط **النقر المزدوج السريع** (طلب متطابق خلال 60 ثانية ⇒ يُعاد المعرّف نفسه idempotent).
- **صفحة السوبر أدمن للطلبات** (كانت **ناقصة فعليًا** رغم ذكرها سابقًا — تصحيح): `GET /platform/leads` + `POST /platform/leads/:id/status` (new/contacted/closed) + صفحة `/admin/leads` (بطاقات: الاسم/البريد/الشركة/الجوال/الباقة/الرسالة/التاريخ + أزرار متابعة/إغلاق/إعادة فتح) + عنصر تنقّل في لوحة المنصّة.
- e2e +1 (الإرسال المتكرّر لا يُقفَل ويُعيد المعرّف · السوبر أدمن يرى/يحدّث الحالة · مستأجر ممنوع 403 · حالة خاطئة 400). الإجمالي **264/264**. بناء الإنتاج **103/103**. مُتحقَّق حيًّا.
- **إثراء بذرة الديمو للميزات الجديدة** (تظهر مأهولة عند `seed:demo` على staging): **3 طلبات تواصل** (Leads بحالات مختلفة) · **هدفا أداء لحساب GIB** (بأرقام فعلية محسوبة من إنتاجه) · **هوية بصرية مميّزة لحساب GIB** (لون كحلي `#1e3a8a` + اسم عرض) لعرض White-label حيًّا.

## [الإعدادات: صفحة الشركة + ترتيب القائمة + تواريخ انتهاء الباقة/التجربة] ✅
- **إعادة ترتيب قائمة الإعدادات**: قسم الإعدادات والاشتراك يبقى **آخر مجموعة**؛ **الاشتراك أولًا**، ثم **الشركة يليها الهيكل الإداري مباشرةً** (تحتها)، ثم الموظفون/الهوية/التكاملات/البريد/الأمان.
- **صفحة الشركة** (`/tenant/settings/company`، كانت «قريبًا»): عرض/تعديل بيانات الوساطة (الاسم عربي/إنجليزي · السجل التجاري · **الرقم الموحّد 10** · **الضريبي 15** · **الجوال 05XXXXXXXX** مُتحقَّقة) + «عضو منذ» + بطاقة رابط للهيكل الإداري. endpoints `GET/PUT /config/company`.
- **صفحة الاشتراك**: تُظهر **تاريخ انتهاء الفترة التجريبية** (بداية الاشتراك + أيام التجربة) مع شريط تنبيه («متبقٍّ X يوم — بعدها يُستحقّ الدفع») و**تاريخ التجديد القادم** (`renewsAt`) — فيعرف العميل متى يُطلب منه الدفع.
- e2e +3 (بيانات الشركة: قراءة/حفظ مُتحقَّق · رقم موحّد خاطئ 400 · صلاحية 403). الإجمالي **263/263**. بناء الإنتاج **101/101**. مُتحقَّق حيًّا.

## [P0+: مولّد وثائق مطبوعة بهوية المستأجر (فاتورة ضريبية PDF)] ✅
إكمال ما كان ناقصًا في P0-B (الوثائق تحمل هوية المستأجر):
- **وثيقة فاتورة ضريبية مطبوعة/PDF بهوية المستأجر** (`/tenant/documents/invoice/[id]`): ترويسة بلون الشركة + شعارها، بيانات البائع (شركة الوساطة: الاسم/الرقم الضريبي/السجل) والطرف (المؤمِّن/العميل)، البنود (صافي + ضريبة 15% + الإجمالي)، وكتلة توافق **ZATCA** (UUID/Hash). زر **«طباعة / حفظ PDF»** (`window.print()` مع CSS طباعة يُخفي الأدوات).
- **لماذا HTML→PDF لا مكتبة PDF خادمية:** يعالج **العربية RTL بدقّة** (مكتبات PDF الخادمية ضعيفة بالتشكيل العربي) وبلا تبعية ثقيلة (Chromium).
- endpoint `GET /finance/invoices/:id/document` (يجمع البائع/الطرف/الوثيقة/ZATCA) + رابط **«وثيقة»** بجانب كل فاتورة في تبويب المالية.
- e2e +1 (الوثيقة: صافي+ضريبة=الإجمالي · بائع/طرف/ZATCA · مجهولة ⇒ 404). الإجمالي **260/260**. بناء الإنتاج **99/99**. مُتحقَّق حيًّا.

## [P2: صفحة التملّك (Ownership License) — بيع استشاري للمؤسسات] ✅
- صفحة مستقلّة ثنائية اللغة `/ownership` بهوية المنصّة وRTL سليم: العنوان «امتلك منصتك التشغيلية كأصل استراتيجي».
- **مستويان**: (1) **رخصة التملّك (Source-Available)** — استخدام دائم + مصدر كامل + تعديل داخلي، بقيد عدم إعادة البيع؛ (2) **التملّك الكامل** — نقل الملكية الفكرية التجارية + استقلال تقني تامّ. لكلٍّ زر **«تواصل معنا»**.
- **خيار نشر** (On-premise/سحابة خاصة) + **قسم دعم وتحديثات اختياري** + **4 ركائز** (أصل بالميزانية · بلا رسوم شهرية · سيطرة كاملة · تخصيص لا محدود) + **نموذج تواصل استشاري** (يُعيد استخدام مسار `Lead`).
- رابط «خيار التملّك (للمؤسسات)» أسفل الباقات في اللاندينق. بناء الإنتاج **99/99**.

## [P1-B: إدارة الأهداف والأداء (Targets)] ✅
- نموذج `Target` (نطاق: **وسيط فرعي** / **فرع تأمين** · مقياس: أقساط/عدد وثائق/عمولات · فترة: شهري/ربعي/سنوي · قيمة الهدف).
- **الفعلي محسوب من الإنتاج القائم** (الوثائق المُصدَرة ضمن الفترة) — **لا بيانات جديدة**؛ نسبة الإنجاز = الفعلي ÷ الهدف.
- صفحة `/tenant/targets` (تحت الرؤى): إنشاء هدف + **لوحة أهداف بأشرطة تقدّم** ملوّنة حسب الإنجاز + فلترة بالفترة + حذف. محكومة بموديول `reports` (الإنشاء إشرافي).
- e2e +5 (خيارات/إنشاء+احتساب الفعلي/فلترة الفترة/قيمة غير موجبة 400/عزل/صلاحية 403). الإجمالي **259/259**. مُتحقَّق حيًّا.

## [P1-A: SLA لكل باقة · P1-C: تأطير «ادفع بقدر نموّك» · قوالب بريد ثنائية اللغة] ✅
- **P1-A — زمن استجابة الدعم (SLA) لكل باقة:** حقل `Plan.slaResponseHours` (الأساسية 24س=يوم عمل · الاحترافية 4س · المؤسسات 2س) **قابل للتهيئة من السوبر أدمن** (`/admin/plans`) ويظهر **سطرًا في جدول مقارنة الباقات** (يُعرض بلغة صحيحة: «خلال يوم عمل»/«خلال 4 ساعات») + ملخّص الاشتراك.
- **P1-C — «ادفع بقدر نموّك» (عرض فقط):** تأطير تسويقي فوق محدّد المستخدمين في الـonboarding + **تفصيل سعر شفّاف** (سعر المستخدم × العدد، بلا رسوم خفيّة) — **دون أي تغيير في منطق/تدفّق التسعير**. ثنائي اللغة.
- **قوالب البريد ثنائية اللغة:** كل نوع إشعار (21 نوعًا) صار له نصّ **عربي + إنجليزي**؛ `sendTenantEmail` يختار اللغة (والاتجاه RTL/LTR) — الافتراضي عربي، وقابل للتحويل للإنجليزية عبر `locale`. التخصيصات على مستوى الشركة تبقى كما كُتبت.
- e2e ثابت **254/254**. بناء الإنتاج **95/95**.

## [P0-A: بريد متعدّد المستأجرين عبر Resend (BYO + fallback) · P0-B: الهوية البصرية (White-label)] ✅
طبقة تخصيص العلامة لكل شركة وساطة — بريد رسمي من نطاقها وهوية بصرية تنعكس على كل الواجهات.

**P0-A — نظام بريد متعدّد المستأجرين (BYO Resend + fallback مركزي):**
- نموذج `TenantEmailSettings` (نطاق/حالة تحقّق/سجلّات DNS/وضع الإرسال) + **مفتاح Resend مشفّر at-rest** (AES-256-GCM عبر `CryptoVaultService`) — لا يُعرض/يُسجّل أبدًا (masked فقط).
- **ربط آلي عبر Resend Domains API** (بلا SDK — fetch نقيّ قابل للاختبار): إنشاء النطاق + استخراج DNS (SPF/DKIM/DMARC) للعرض + «تحقّق الآن» + **مجدول دوري (cron كل 30د) يرقّي النطاق تلقائيًا** حال اكتمال DNS.
- **دالة إرسال موحّدة `sendTenantEmail`** تمرّ عبرها كل إشعارات البريد: وضع المستأجر المُتحقَّق ⇒ من بريده؛ وإلا **fallback مركزي** (`notifications@mail.nx.sa` + `Reply-To` بريد المستأجر) — **بلا انقطاع** وترقية تلقائية للمستأجر بعد التحقّق.
- **وضع الردود فقط (بلا مفتاح Resend):** تستطيع الشركة ضبط بريدها الرسمي **دون Resend** فترسل عبر المركزي مع **Reply-To = بريدها** (تصل الردود إليها لا للمنصّة). و**Reply-To مضمون دائمًا**: بريد الشركة ← وإلا بريد مالك الحساب.
- **قوالب بريد HTML بهوية المستأجر** (شعار/لون) ثنائية الاتجاه (RTL/LTR). صفحة `/tenant/settings/email` (نطاق/DNS/نسخ/حالة). وضع Sandbox يحاكي Resend بلا شبكة (تطوير/اختبار).

**P0-B — الهوية البصرية (White-label):**
- إعداد `TenantConfig.branding` (لون أساسي/اسم عرض/شعار نصّي/شعار مرفوع) — **رفع الشعار يُخدَم برابط عام ثابت** (`GET /branding/:tenantId/logo`) كي يظهر في البريد الواصل لعملاء خارجيين.
- **تلوين ديناميكي**: `BrandingProvider` يقرأ `/branding` ويطبّق متغيّرات CSS (`--primary` ومشتقّاته) على **واجهة كل الموظفين** + **بوّابة العميل** (`/portal/branding`) — القيم الافتراضية = هوية NX-IBP.
- صفحة `/tenant/settings/branding` (منتقي لون + رفع شعار + **معاينة حيّة**) — التغيير ينعكس فورًا على كامل الواجهة.
- عزل تام (هوية مستأجر لا تمسّ غيره) + حارس صلاحية (تعديل الهوية/البريد لأصحاب `settings` فقط).

- e2e +12 (`email-branding`: افتراضيات/حفظ/عزل/صلاحيات/رفع شعار عام · بريد unconfigured→pending→verified مع الترقية + إخفاء المفتاح). تحديث `staff-notifications` (البريد يمرّ الآن عبر `sendTenantEmail`). الإجمالي **253/253**. بناء الإنتاج **95/95**. مُتحقَّق حيًّا (تغيير اللون ⇒ الواجهة كلها · إعداد البريد ⇒ DNS + fallback + masked).

## [تحسينات واجهة: صفحة المالية بتبويبات + ترقيم صفحات · تسمية «الوسطاء الفرعيون» · تنظيف الهيكل الافتراضي] ✅
حزمة صقل واجهة بناءً على ملاحظات الاستخدام:
- **صفحة المالية بتبويبات**: البلوكات الأربعة (شجرة الحسابات · الفواتير الضريبية ZATCA · المستحقّ للمؤمِّنين · ميزان المراجعة) صارت **تبويبات أنيقة** للتنقّل داخل الصفحة بدل التكديس الرأسي — مع بطاقات المؤشّرات ثابتة أعلى الصفحة.
- **ترقيم صفحات (50/صفحة)**: مكوّن `Pagination` + خطّاف `usePaged` (قابل لإعادة الاستخدام) يُطبَّق على جداول المالية — يظهر شريط الترقيم تلقائيًا فقط عند تجاوز البيانات 50 صفًّا (عرض «من–إلى من الإجمالي» + السابق/التالي، RTL).
- **تسمية موحّدة «الوسطاء الفرعيون»**: استُبدل مصطلح «المنتِجون/المنتِج» بـ «الوسطاء الفرعيون/الوسيط الفرعي» عبر الواجهة (عربي/إنجليزي: Sub-brokers) والتوثيق — اسم الوسيط لا اسم المنتج (كيان `Producer` والمسار `/producers` يبقيان في الكود).
- **تنظيف الهيكل التنظيمي الافتراضي**: بذرة الديمو كانت تُبقي أقسامًا قديمة (CEO فارغ) فوق الشجرة القياسية ⇒ جذران. الآن تُزال البقايا غير القياسية فيبقى **جذر واحد نظيف** («الإدارة العليا» + 6 أقسام بأدوارها). مُتحقَّق حيًّا (الشجرة جذر واحد + التبويبات + الترقيم يعمل). e2e ثابت **241/241**.

## [الباقات — نماذج التحقق KYC/KYB و«الفوترة الإلكترونية (ZATCA)» أصبحت قابلة للتفعيل/التعطيل وتُفرَض فعليًا] ✅
كانت هاتان الميزتان تظهران في جدول المقارنة لكنهما **غير قابلتين للضبط من السوبر أدمن** (بلا شريحة تبديل) و**بلا إنفاذ** — فيُصلَح الأمران:
- **صفحة الباقات (السوبر أدمن)**: أُضيفت شرائح `feature.verification` (نماذج التحقق KYC/KYB) · `feature.zatca` (الفوترة الإلكترونية) · `feature.auditImmutable` (السجل الثابت) إلى قائمة الميزات القابلة للضبط — تُدوَّر (مشمول ⇒ إضافة ⇒ حسب الاستخدام ⇒ معطّل) **كبقية الميزات** وتنعكس فورًا في صفحة المقارنة واللاندينق.
- **إنفاذ حقيقي (حارس الباقة)**: مسارات `/verification/*` صارت تتطلّب `feature.verification`، ومسارات تهيئة/تشغيل `/zatca/*` تتطلّب `feature.zatca` — فتعطيل الميزة للباقة يمنع الوصول فعليًا (403) لا مجرد إخفاء في المقارنة.
- e2e +1 (السوبر أدمن يعطّل `feature.zatca` للباقة ⇒ مستأجرها يُرفَض 403 ثم يعود 200 بعد الاستعادة). الإجمالي **241/241**. مُتحقَّق حيًّا (شرائح المزايا الثلاث تظهر وتُدوَّر على صفحة `/admin/plans`).

## [تزويد الحساب الجديد بالأقسام والأدوار الافتراضية (مطابقة لأقسام شركة الوساطة السعودية)] ✅
كل حساب وساطة جديد (تسجيل ذاتي) يُزوَّد الآن **بهيكل تنظيمي وأدوار جاهزة** مطابقة لواقع شركات الوساطة (منهجية أويسس/هيئة التأمين) — لا يبدأ فارغًا:
- **الهيكل التنظيمي الافتراضي**: «الإدارة العليا» جذرًا + **6 أقسام تشغيلية** (المبيعات وتطوير الأعمال · الالتزام والمطابقة · الاكتتاب الفني · الإدارة المالية والمحاسبة · خدمة العملاء · إدارة المطالبات) — يظهر فورًا في صفحة الهيكل التنظيمي.
- **الأدوار المُعدّة مسبقًا (12 دورًا)** بمصفوفة صلاحيات دقيقة لكل قسم (المدير العام · مدير/ممثل مبيعات · مكتتب · مسؤول العمليات · مدير التزام · محاسب/محصّل · عناية العملاء · مسؤول مطالبات · مدير موارد بشرية · مساعد إداري) — جاهزة للإسناد.
- **كل قسم مرتبط بدوره الافتراضي**: إسناد موظف لقسم يُورِّثه صلاحياته تلقائيًا (مثال: الاكتتاب الفني ⇒ مكتتب · المالية ⇒ محاسب). المالك = المدير العام ضمن الإدارة العليا.
- تُنقل مصفوفة `PRESET_ROLES` + `parsePerm` إلى ثوابت الـAPI (المرآة المحلية) وتُطبَّق في تزويد التسجيل. e2e +1 (الحساب الجديد: 12 دورًا + 7 أقسام + صلاحيات القسم مضبوطة فعليًا). الإجمالي **240/240**. مُتحقَّق حيًّا (الهيكل ⇒ 6 أقسام لكلٍّ دوره).

## [مقارنة أغنى (أوضاع الميزات) + مسار «تواصل معنا» للمؤسسات + إصلاح بناء الديبلوي] ✅
استكمالًا لمرجع التصميم (تهيئة):
- **قيم وصفية أغنى في المقارنة**: بدل ✓/— الثنائية، تعرض الخلايا **«مشمول» · «إضافة» (مدفوع) · «حسب الاستخدام» (metered) · الحصص** حسب **وضع الميزة** (`Entitlement.mode`). السوبر أدمن **يدوّر الوضع بالنقر** (مشمول ⇒ إضافة ⇒ حسب الاستخدام ⇒ معطّل) من `/admin/plans`، وينعكس فورًا. (البذرة: الاحترافية DLP=إضافة · API=حسب الاستخدام كنموذج.)
- **مسار «تواصل معنا» للباقات الكبيرة**: باقة المؤسسات زرّها **«تواصل معنا»** (بدل التسجيل الذاتي) + رابط **«تواصل مع المبيعات»** — يفتحان نموذج Lead (اسم/شركة/بريد/جوال مُتحقَّق/رسالة) ⇒ `POST /signup/lead` (نموذج `Lead` + هجرة `sales_lead`). التسجيل الذاتي يبقى للأساسية/الاحترافية.
- **🐛 إصلاح فشل الديبلوي**: `next build` كان يفشل بـ«useSearchParams() should be wrapped in a suspense boundary» في صفحة التسجيل — لُفّت بـ`<Suspense>`. (يُفوَّت في `tsc`/dev ويظهر في بناء الإنتاج فقط.)
- e2e +1 (طلب Lead عام 201 + تحقّق البيانات). الإجمالي **239/239**. **بناء الإنتاج ينجح (91/91 صفحة)**. مُتحقَّق حيًّا (مشمول/إضافة/حسب الاستخدام · نموذج التواصل ⇒ «تم استلام طلبك»).

## [التسعير لكل مستخدم بلا سقف + مقارنة مدمجة في اللاندينق + تجاوب الموبايل الكامل] ✅
تحسينات على تجربة الباقات والتصفّح:
- **بلا سقف لعدد المستخدمين**: أُزيل حدّ المقاعد من الباقة نهائيًا — التسعير **لكل مستخدم**، فيُضاف أي عدد (السعر يتغيّر)، والفوترة تتبع العدد الفعلي. حُذف صفّ «عدد المستخدمين حتى X» من المقارنة، وأُزيل حقل حدّ المقاعد من محرّر السوبر أدمن، ومعالج التسجيل بلا سقف (`/staff/seats` يعيد `limit=null`).
- **جدول المقارنة مدمج في اللاندينق** (كمرجع التصميم): الباقات + مبدّل شهري/سنوي + مصفوفة المميزات كلها في الصفحة الرئيسية (+ صفحة `/compare`)، عبر مكوّن مشترك `PlanComparisonTable`.
- **رأس جدول المقارنة أُعيد تصميمه** باحترافية (اسم بارز + سعر كبير + شارة تجربة + عمود مميّز للأكثر اختيارًا).
- **تجاوب الموبايل الكامل**: اللاندينق والمقارنة والتسجيل متجاوبة (الجدول ينزلق أفقيًا). **المنصّة (لوحة الموظف) صار لها درج تنقّل للموبايل** (زر قائمة في الشريط العلوي + درج منزلق مع تعتيم) — كانت القائمة الجانبية مخفيّة بلا بديل.
- e2e مُحدَّثة (بلا سقف: الإضافة تنجح والعدّاد يتزامن؛ `limit=null`). الإجمالي **238/238**. مُتحقَّق حيًّا (موبايل: درج المنصّة + لاندينق متجاوب · المقارنة برأس محسّن).

## [الباقات — إعادة هيكلة وفق هيئة التأمين + صفحة مقارنة + تفعيل/تعطيل المميزات ديناميكيًا] ✅
جعل تفعيل الموديولز والمميزات لكل باقة **ذكيًا وديناميكيًا** عبر محرّك الـEntitlements:
- **إعادة هيكلة تنافسية وفق هيئة التأمين**: الباقة **الأساسية تشمل كل جوهر التشغيل والامتثال الذي تتطلّبه الهيئة** (العملاء/الاكتتاب/الإصدار/التجديدات/الخدمة/**المطالبات**/المالية/**الالتزام والتدقيق**/التقارير التنظيمية/KYC/ZATCA) — لا تُحجب الأساسيات. التمايز على **المميزات المتقدمة** (CRM · المنتِجون · قوالب النماذج · **التحليلات المتقدمة** · سلاسل الاعتماد · الأقسام · إلزام MFA) في الاحترافية، و**الحوكمة** (HR · DLP · API · White-label · دعم أولوية) في المؤسسات.
- **تفعيل حقيقي (enforcement)**: CRM/المنتِجون/القوالب صارت محكومة بـ`feature.crm/producers/formTemplates`، والتحليلات بـ`feature.analytics` — يفرضها حارس الـEntitlements فعليًا (مستأجر خارج باقته ⇒ 403).
- **صفحة مقارنة الباقات** (`/compare`، عامة) — جدول مميزات لكل باقة (✓/—/إضافة/حصص) مجمّع بالفئات، مع **رابط «مقارنة الباقات»** تحت الأسعار في اللاندينق. تُبنى من `/signup/compare`.
- **السوبر أدمن يفعّل/يعطّل أي ميزة أو موديول لأي باقة** من `/admin/plans` (شرائح تبديل) — **وتظهر فورًا في صفحة المقارنة** وتُفرَض على الحسابات (عبر `POST /platform/plans/:code/entitlements` القائم).
- e2e مُحدَّثة (بوّابة الباقة: الأساسية تصل للمطالبات/الالتزام 200، وتُمنع من التحليلات/المنتِجين/القوالب 403). الإجمالي **239/239**. مُتحقَّق حيًّا (المقارنة كاملة التمايز + تبديل السوبر أدمن ينعكس فورًا).

## [الباقات والتسجيل — تسعير لكل مستخدم + شهري/سنوي بتوفير + تجربة مجانية + معالج onboarding] ✅
تجربة اشتراك احترافية متكاملة من اللاندينق حتى التزويد:
- **تسعير لكل مستخدم (per-user)**: `Plan.priceMonthly/priceYearly` صارت سعرًا **لكل مستخدم**؛ الإجمالي = السعر × عدد المستخدمين. لا «مستخدمون إضافيون».
- **السوبر أدمن يتحكّم بالكامل**: تعديل **السعر الشهري والسنوي** + **مدة التجربة المجانية** (`Plan.trialDays`) من `/admin/plans` (كان حدّ المقاعد فقط).
- **اللاندينق ديناميكي**: يجلب الباقات من `/signup/plans` (عام) بدل الأسعار الثابتة — **مبدّل شهري/سنوي** + **نسبة التوفير** المحسوبة + السعر لكل مستخدم + شارة التجربة + حدّ المقاعد. أي تغيير من السوبر أدمن يظهر فورًا.
- **معالج تسجيل من 3 خطوات**: (1) الحساب، (2) الباقة + عدد المستخدمين + **onboarding**: الرقم الموحد للمنشأة + الرقم الضريبي + رقم التواصل، (3) **ملخّص السعر** — إن شملت الباقة تجربةً يظهر **الإجمالي مشطوبًا + «مجانًا» + شارة التجربة** بتصميم تسويقي («ثم X ريال بعد التجربة»، بلا بطاقة).
- **تحقّق دقيق بعدد الخانات** (المعايير السعودية): الرقم الموحد 10 أرقام · الضريبي 15 · الجوال `05XXXXXXXX` — على الواجهة (تنقية فورية) والـAPI (regex). حقول `Tenant.unifiedNumber/vatNumber/phone` + `Subscription.cycle/seatsUsed` من المعالج. فوترة الاشتراك صارت لكل مستخدم × المقاعد. هجرة `plan_trial_onboarding`.
- e2e +6 (كتالوج عام/تحقّق الرقم الموحد والجوال/قصّ المقاعد للحدّ/تعديل السعر والتجربة من السوبر أدمن ⇒ يظهر في الكتالوج). الإجمالي **239/239**. مُتحقَّق حيًّا (اللاندينق شهري/سنوي+توفير 17% · المعالج ⇒ 11,920 مشطوب ⇒ مجانًا 14 يومًا).

## [مكتبة قوالب النماذج — تعبئة مسبقة قابلة لإعادة الاستخدام تُسرّع الطلبات المتكرّرة] ✅
البند البرمجي الاختياري الأخير — إغلاق قائمة النواقص البرمجية:
- **نموذج `FormTemplate`** (اسم/خطّ منتج/تعبئة `base`+`blocks`/عدّاد استخدام/حالة) + هجرة `form_templates`. خدمة+`/form-templates` (سرد حسب الخطّ · إنشاء · تعديل · حذف · **تطبيق** يزيد العدّاد ويعيد التعبئة) تحت وحدة المبيعات.
- **الواجهة**: في نموذج الطلب — **شرائح «تحميل من قالب»** (تُعبّئ النموذج فورًا) + زر **«حفظ كقالب»** (يحفظ التعبئة الحالية). `DynamicForm` صار يقبل قيمًا أولية.
- البذرة: قوالب جاهزة (طبي المنشآت/أسطول مركبات/ممتلكات). e2e +6 (إنشاء/سرد حسب الخطّ/تطبيق+عدّاد/تعديل+حذف/رفض خطّ مجهول/عزل). الإجمالي **233/233**. مُتحقَّق حيًّا (تطبيق قالب SME ⇒ تعبئة الشبكة والحدّ السنوي).

## [المرجعية التنظيمية — توحيد على هيئة التأمين (IA) — إكمال خارطة تجاوز أويسس 8/8] ✅
أويسس بُني لوسيط في مركز قطر للمال (QFC) 2022؛ سياق IBP سعودي 2026 تحت **هيئة التأمين (Insurance Authority)**. النظام مبنيّ أصلاً على IA؛ أُزيلت آخر إشارات «ساما» المتبقّية:
- توحيد النصوص: تلميح فصل المهام (EN) + تعليقات مدّة الاحتفاظ ⇒ «هيئة التأمين IA» بدل SAMA. صِفر إشارة SAMA/QFC في الكود.
- تأكيد الإطار التنظيمي السعودي الكامل (معروض في صفحة التكاملات عبر `/regulatory/status`): **ZATCA** (فوترة، مُفعّل) · **نفاذ/يقين/واثق/العنوان الوطني** (هوية/سجل/عنوان) · **نجم** (مركبات) · **مجلس الضمان الصحي/نِفيس** (طبي) · **هيئة التأمين IA** (رفع التقارير). إقامة البيانات داخل المملكة للإنتاج (PDPL/NCA).
- **خارطة تجاوز أويسس اكتملت (8/8)**: التحصيل · الإلغاء/CNP · اتجاه الفاتورة/CNC · تسوية المؤمِّن/ميزان المراجعة · تجربة المبيعات/التحويل · **سجلّ المنتِجين** · فصل الاكتتاب/الإصدار · **المرجعية التنظيمية IA**.

## [سجلّ المنتِجين (الوسطاء الفرعيون) + دفتر عمولاتهم وتسويتها (المرحلة 6 نحو تجاوز أويسس)] ✅
تحويل حقل «اسم المنتِج» الحرّ إلى **سجلّ منتِجين** أول-درجة (أويسس: Producers/Sub-brokers):
- **نموذج `Producer`**: رمز (PRD-)، نوع (فرد/شركة)، **رقم ترخيص هيئة التأمين**، سجل تجاري/هوية، تواصل، آيبان، **نسبة عمولة افتراضية** من عمولة الوسيط، حالة (نشط/موقوف). هجرة `producers_registry` + ربط `Policy.producerId`.
- **احتساب آلي لحصّة المنتِج**: عند ربط وثيقة بمنتِج، تُحتسب `producerCommission = عمولة الوسيط × نسبة المنتِج` (ما لم تُمرَّر يدويًا).
- **دفتر المنتِج**: العمولة المستحقّة = Σ حصص وثائقه المُصدَرة، المُسوّى، المتبقّي — مع تفصيل وثائقه وسندات صرفه.
- **تسوية (صرف) العمولة**: سند صرف (PYV) بقيد مصروف «عمولات المنتِجين» (COA `05010`) ⇒ نقد، يمنع تجاوز المستحقّ (409). صلاحية: السجلّ والتسوية تحت وحدة **المالية**.
- الواجهة: صفحة «المنتِجون» (بطاقات + سجلّ) + بطاقة تفصيل (دفتر + وثائق + صرف) + إنشاء منتِج. البذرة: 3 منتِجين + ربط جزء من الوثائق بحصصهم.
- e2e +5 (منع غير المالية · إنشاء PRD- · احتساب الحصّة 20% · تسوية/تجاوز 409/عزل). الإجمالي **227/227**. مُتحقَّق حيًّا (منتِجان بدفتر عمولات: مكتب الرواد + خالد الدوسري).

## [المالية — تصويب اتجاه الفاتورة الضريبية + رسوم الخدمة كإيراد + إشعار دائن للمؤمِّن (المرحلة 3 نحو تجاوز أويسس)] ✅
لمطابقة اتجاه الفوترة في وساطة التأمين (أويسس: "Tax Invoice to the insurance company against commission") وسدّ فجوة رسوم الخدمة:
- **رسوم الخدمة/الإصدار (`policyFees`) صارت إيرادًا مُحاسَبًا ومُفوترًا** بعد أن كانت تُخزَّن دون أثر مالي: عند الاعتماد المالي تُقيَّد في حساب إيراد مستقلّ **«رسوم خدمات وإصدار الوثائق» (04020)** + ضريبة مخرجات 15% (فئة S دائمًا)، وتُضاف لمطالبة العميل في إشعار المدين (مطالبة واحدة)، وتُصدَر لها **فاتورة ضريبية على العميل** (`kind=FEES`) — منفصلة عن أمانة القسط.
- **توجيه الفواتير صريح**: فاتورة **العمولة على المؤمِّن** (`kind=COMMISSION`)، وفاتورة **رسوم الخدمة على العميل** (`kind=FEES`). الحقلان `Invoice.kind`/`Invoice.clientId` + عرض «الطرف» في الواجهة.
- **الإلغاء يُنشئ إشعارًا دائنًا للمؤمِّن (CNC)** يعكس العمولة (وضريبتها) المستردّة نسبةً وتناسبًا — بجانب الإشعار الدائن للعميل (CNP). الحقلان `CreditNote.kind` (CNP/CNC)/`CreditNote.insurerName`.
- **العزل الصحيح للطرف**: بوّابة العميل تعرض فواتير رسومه فقط (لا فواتير العمولة على المؤمِّن)؛ حسابات العميل ومستحقّاته لا تتأثّر بإشعارات المؤمِّن الدائنة (CNC).
- الملخّص المالي: `serviceFees` + ضريبة المخرجات = عمولات + رسوم. البذرة أثرت الفواتير (عمولة على المؤمِّن + رسوم على العميل) وميزان المراجعة يبقى متوازنًا. هجرة `invoice_direction` + حساب COA جديد `[4,2]`.
- e2e: فحص الشلال يتحقّق من فاتورة الرسوم على العميل + قيد 04020 + توجيه العمولة؛ فحص الإلغاء يتحقّق من CNC؛ بوّابة العميل لا تكشف فواتير العمولة. الإجمالي **222/222**. مُتحقَّق حيًّا (فواتير: «عمولة — على المؤمِّن» × 12 · «رسوم خدمة — على العميل» × 4 · ميزان متوازن).

## [الصلاحيات — فصل الاكتتاب عن الإصدار + تسميات أدوار مهنية] ✅
لجعل صلاحيات كل قسم واضحة كما في شركات الوساطة السعودية (وأويسس: Underwriting Management ≠ Production):
- **وحدة صلاحية «الاكتتاب» (`underwriting`)** جديدة لطلبات الأسعار (Slip/RFQ) + عروض المؤمِّنين + المقارنة + أمر الإسناد — **منفصلة عن «الإنتاج/الإصدار» (`production`)**. مسارات `/slips` تفحص `underwriting` (مع بقاء `entitlement: module.production` فلا تتغيّر الباقات).
- **مصفوفة الأدوار**: المكتتب/التسعير = اكتتاب (إنشاء) + إصدار؛ **العمليات = إصدار فقط** (يقرأ الاكتتاب ولا يُنشئه)؛ المبيعات بلا اكتتاب. تسميات مهنية: «مكتتب (مسؤول التسعير)» · «مسؤول العمليات والإصدار».
- الواجهة: مصفوفة الصلاحيات تعرض «الاكتتاب (عروض الأسعار)» كوحدة مستقلّة.
- e2e +1 (دور «إنتاج فقط» ممنوع من Slip ⇒ 403). الإجمالي **222/222**. مُتحقَّق حيًّا (المكتتب: إنشاء=✓ · العمليات: إنشاء=✗).

## [تجربة المبيعات — إثراء الفرصة البيعية + التحويل لطلب (المرحلة 5 نحو تجاوز أويسس)] ✅
ربط المبيعات بالاكتتاب وفق منهجية أويسس (Sales Lead):
- **إثراء الصفقة (Sales Lead)**: نوع التعيين (حصري/غير حصري — **BOR**) · القسط التقديري · المصدر (مباشر/منتِج + اسم المنتِج) · المؤمِّن الحالي · **نسبة الخسارة** · المؤمِّنون المفضّلون · الإغلاق المتوقّع · ملاحظات. (مخطط `Deal` + هجرة `sales_lead_enrichment`.)
- **تفاصيل الفرصة** (`GET /crm/deals/:id`): بطاقة قابلة للتعديل بكل الحقول + سجلّ النشاط.
- **التحويل إلى طلب تأمين** (`POST /crm/deals/:id/convert`): يُنشئ طلبًا (DRAFT) مبنيًا على بيانات الفرصة ويدخل دورة الاكتتاب، ويكسب الصفقة ويربطها — يشترط عميلًا معتمَدًا وفرعًا (تكرار ⇒ 409). **يسدّ الفجوة بين المبيعات والاكتتاب**.
- **مدير حساب لكل عميل** (`Client.accountManagerId`) — معيار الوساطة.
- الواجهة: بطاقات الصفقة قابلة للنقر (تفتح التفاصيل) + مؤشّر «محوّلة» + فرع المنتج + القسط التقديري.
- e2e +1 (إثراء + تحويل ⇒ SL- + won + ربط + تكرار 409). الإجمالي **221/221**. مُتحقَّق حيًّا (تحويل ⇒ SL-PRP-2026-1007).

## [المالية — تسوية المؤمِّن + ميزان المراجعة (المرحلة 3 نحو تجاوز أويسس)] ✅
يُكمل **الجانب الخلفي المالي** (سندات الصرف + التقارير المحاسبية):
- **المستحقّ للمؤمِّنين (أمانات)**: `GET /finance/payables` — صافي القسط المحتفَظ به لكل مؤمِّن (الإجمالي − العمولة − ضريبتها) مع **أعمار الدَّين** والمُسوّى والمتبقّي.
- **سند صرف (PYV)**: `POST /finance/insurers/settle` — تسوية مستحقّ مؤمِّن (نقد دائن / أمانات مدين)، يمنع تجاوز المستحقّ (409).
- **ميزان المراجعة**: `GET /finance/trial-balance` — تجميع أطراف كل القيود حسب الحساب مع مؤشّر توازن (مدين = دائن).
- البذرة: **قيد يومية (JRV) لكل وثيقة مُصدَرة** ⇒ ميزان مراجعة مكتمل ومتوازن ومتّسق مع الملخّص.
- الواجهة: قسمَا «المستحقّ للمؤمِّنين» (بزر تسوية) و«ميزان المراجعة» (بشارة **متوازن**) على صفحة المالية.
- e2e +1 (المستحقّ + تسوية PYV + تجاوز 409 + صلاحية 403 + ميزان متوازن). الإجمالي **220/220**. مُتحقَّق حيًّا (تسوية ميدغلف 50K ⇒ PYV + ميزان متوازن 4.7M).

## [المالية — الإلغاء والإشعارات الدائنة (المرحلة 2 نحو تجاوز أويسس)] ✅
- **إلغاء وثيقة مُصدَرة**: `POST /finance/policies/:id/cancel` (finance:update) — يحسب **القسط المُرتجَع نسبةً وتناسبًا** من الأيام غير المنقضية، وينشئ ملحق إلغاء + **إشعارًا دائنًا (CNP)** للعميل + قيدًا عكسيًا (JRV) متوازنًا، ويحوّل الوثيقة إلى `CANCELLED` (إعادة الإلغاء ⇒ 409). مطابق لأويسس (Credit Note للعميل عند الإلغاء) وZATCA (إشعار دائن لأي تخفيض).
- **الإشعارات الدائنة في الحسابات**: `finance/summary` و`receivables` يخصمان الإشعارات الدائنة من الذمم؛ **كشف حساب العميل** يعرض الإشعار الدائن (credit) ويُنقص الرصيد؛ نظرة الوثيقة 360° تُظهرها في تبويب الفواتير.
- **إصلاح جوهري**: الإصدار صار يضبط **فترة التغطية** (`startDate`/`endDate` من نموذج الطلب، افتراضيًا سنة) — كانت الوثائق المُصدَرة عبر الـAPI بلا تواريخ، فيتعذّر حساب المُرتجَع والتجديد.
- الواجهة: زر «إلغاء الوثيقة» على صفحة التفاصيل (لأصحاب صلاحية المالية فقط، عبر فحص `/auth/me`) + نموذج تاريخ السريان/السبب.
- e2e +1 (إلغاء ⇒ CANCELLED + إشعار دائن + كشف حساب يعكسه · صلاحية 403 · تكرار 409). الإجمالي **219/219**. مُتحقَّق حيًّا (إلغاء GIB ⇒ CN + قسط مُرتجَع 2,322).

## [المالية — دورة التحصيل (المرحلة 1 نحو تجاوز أويسس)] ✅
سدّ **الفجوة الأكبر** مقابل منهجية أويسس: كان النظام يعترف بالذمم عند الإصدار بلا دورة تحصيل — فالذمم لا تنخفض. الآن دورة تحصيل كاملة:
- **سند قبض من العميل (RCV)** مقابل إشعار المدين: `POST /finance/debit-notes/:id/receipt` — قيد متوازن (نقد مدين / ذمم العميل دائن)، يزيد `DebitNote.settledAmount` ويمنع تجاوز المستحقّ (409).
- **استلام عمولة من المؤمِّن (RCV)**: `POST /finance/commissions/:id/receipt` — يضبط الحالة (مستلمة/فرق تحصيل).
- **ذمم حيّة**: `finance/summary` و`finance/receivables` تعرضان المتبقّي بعد التحصيل + **«المُحصَّل»**؛ أعمار الذمم على غير المُحصَّل فقط؛ كل إشعار يُظهر المُحصَّل/المتبقّي وحالته (مستحقّ/جزئي/مسدَّد).
- **كشف حساب العميل**: `GET /finance/statement/:clientId` — قيود (إشعارات) + مدفوعات (سندات قبض) برصيد جارٍ، ونافذة كشف حساب على صفحة الأقساط.
- الواجهة: «تسجيل دفعة» على الأقساط + «تسجيل استلام» على العمولات.
- مخطط: `DebitNote.settledAmount`/`settledAt` (migration `debit_note_settlement`). البذرة: تسوية واقعية لجزء من الإشعارات (تتحرّك الذمم في الديمو).
- e2e +2 (سند قبض/جزئي/409/مسدَّد + كشف حساب متوازن · صلاحية 403 وعزل 404). الإجمالي **218/218**. مُتحقَّق حيًّا.

## [اتّساق البيانات + إثراء صفحة الموظف] ✅
- **اتّساق «العمولات المعلّقة»**: كانت لوحة التحكّم تعرض **إجمالي** العمولات تحت مسمّى «معلّقة (لم تُستلم بعد)» بينما صفحة العمولات تعرض المعلّقة (المستحقّة) فقط ⇒ رقمان مختلفان لنفس المفهوم. أصبح مؤشّر اللوحة = العمولات المستحقّة (`status=accrued`) ليطابق البطاقة في صفحة العمولات تمامًا.
- **تدقيق اتّساق شامل** (GIB + gulf-demo): العمولات (صفحة العمولات = المالية = الهيئة) · GWP (المالية = الإنتاج = الهيئة) · الذمم المدينة (المالية = الأقساط) · الضريبة — **كلها متطابقة عبر الصفحات**، محسوبة من قاعدة البيانات مباشرةً. e2e +1 (اتّساق اللوحة/العمولات).
- **صفحة الموظف 360°**: إثراء تبويبي «صفقاته» و«مهامه» — الصفقة تعرض العميل/المنتج/تاريخ الإنشاء/القيمة المتوقّعة ومرحلة مترجَمة؛ المهمة تعرض الكيان المرتبط/تاريخ الإنشاء/الأولوية مترجَمة/الاستحقاق مع مؤشّر «متأخّرة».
- الإجمالي **e2e 216/216**. مُتحقَّق حيًّا.

## [الملاحق — إضافة ملحق للوثيقة (Endorsement)] ✅
- **الفجوة**: نموذج `Endorsement` كان موجودًا لكن **بلا مسار إضافة** — لا يستطيع الموظف إنشاء ملحق.
- **backend**: `POST /policies/:id/endorsements` (`production:create`) — يُنشئ ملحقًا على وثيقة **مُصدَرة فقط** (409 لغيرها) برقم `POL-…/E{n}`، نوعه (إضافة/حذف/تعديل/إلغاء) + **تاريخ سريان** + **فرق قسط** (موجب = إضافي، سالب = مُرتجع) + سبب. تدقيق + عزل بالمستأجر (404 لوثيقة الغير).
- **الواجهة**: تبويب «الملاحق» في صفحة تفاصيل الوثيقة 360° فيه الآن زر **«إضافة ملحق»** + نموذج، ونوع الملحق **يُترجَم** (بدل القيمة الخام). يظهر الزر على الوثائق المُصدَرة فقط.
- اختبار `detail-360.e2e` (+2: إضافة ملحق ⇒ 201 ويظهر في النظرة · عزل مستأجر آخر 404). الإجمالي **e2e 215/215**. مُتحقَّق حيًّا (النموذج end-to-end).

## [المالية — تحسينات احترافية للعمولات والأقساط والفواتير] ✅
- **العمولات**: إصلاح **حالة غير مترجَمة** (كانت تظهر `commissions.status.accrued`) ⇒ «مستلمة/مستحقّة/فرق تحصيل». + قسم **«حسب شركة التأمين»** (إجمالي/مستلم/متبقٍّ ونسبة تحصيل لكل مؤمِّن — للمطابقة والتحصيل من كل شركة) + **شريط نسبة التحصيل** الكلي.
- **الأقساط**: «المستحقّ حسب العميل» أصبح **تقرير أعمار ذمم مدينة** (AR Aging) احترافيًّا — شرائح 0–30 / 31–60 / 61–90 / +90 يوم لكل عميل + صفّ إجماليات + مؤشّر «متأخّرة (+60)»، يخدم أولويات التحصيل.
- **الفواتير الضريبية** (صفحة المالية): عرض **الصافي + الضريبة + الإجمالي** (كما يليق بالفاتورة الضريبية) بدل الإجمالي وحده.
- مُتحقَّق حيًّا لكل الصفحات.

## [إصلاح وجهة الدخول] — الموظف يهبط على لوحة المعلومات لا على صفحة خارج صلاحيته ✅
- **العلّة**: بعد تسجيل الدخول كان التوجيه ثابتًا إلى `/tenant/settings/staff` (صفحة **مقصورة على المدير العام** — `settings` مُطفأ لبقية الأدوار). فأي موظف غير المدير (تسعير/التزام/مطالبات…) يهبط على صفحة خارج صلاحيته فتظهر رسالة تقييد حتى يُحدّث/ينتقل يدويًا.
- **الإصلاح**: التوجيه بعد الدخول صار إلى **لوحة المعلومات** (`/tenant/dashboard`) — الوحيدة المتاحة **لكل الأدوار** (`dashboard:read` مكفول للجميع في مصفوفة الأدوار)، عبر `router.replace` (فلا تبقى صفحة الدخول في السجلّ). إلزام MFA غير المُفعّل ⇒ صفحة التفعيل كما كان.
- **تأكيد فلترة القائمة الجانبية** (كانت تعمل، وأُعيد التحقّق منها حيًّا): كل موظف يرى **أقسامه فقط** — مسؤول التسعير: لوحة/علاقات/طلبات/وثائق/تجديدات/تقارير · مدير الالتزام: + الالتزام/المطالبات/المالية/الأقساط/العمولات، بلا العملاء/الخدمة/الموارد/الإعدادات. الـbackend يفرضها أيضًا (403).

## [التجديد وفق معايير الوساطة] — دورة تجديد فعلية + تقييد الزر بنافذة 60 يومًا ✅
- **بدء التجديد أصبح دورة فعلية**: `POST /renewals/:policyId/initiate` (يعيد **201** الآن) يُنشئ **طلب تأمين جديدًا** (`PolicyRequest`) مبنيًا على بيانات الطلب الأصلي للوثيقة (استنساخ `base`/`details` + صفوف الكتل) مع رابط سلسلة التجديد `renewedFromPolicyId`، فيدخل دورة RFQ⇐عرض⇐إصدار — لا مجرّد تذكرة `ServiceRequest`. **يمنع التكرار** (طلب قائم لنفس الوثيقة ⇒ 409).
- **إزالة التذكير التلقائي للعميل عند البدء**: التذكير المبكّر مهمّة المجدول الدوري (`renewal_reminder` ≤30 يومًا)، والتواصل الفعلي هو إرسال عرض التجديد لاحقًا — فلا إزعاج مزدوج؛ يبقى إشعار فريق التجديدات (`staff_renewal_due`). نصّ تأكيد اللوحة حُدِّث.
- **تقييد زر التجديد بنافذة 60 يوماً** (بوّابة العميل: صفحة الوثيقة + قائمة الوثائق): يظهر فقط ضمن النافذة، وخارجها حالة «الوثيقة سارية حتى … — يفتح التجديد قبل الانتهاء بـ60 يوماً».
- مخطط: `PolicyRequest.renewedFromPolicyId` + فهرس (migration `renewal_chain`). اختبار `operations.e2e` (+1: بدء دورة ⇒ 201 طلب DRAFT + منع التكرار 409). الإجمالي **e2e 213/213**. مُتحقَّق حيًّا (الزر داخل/خارج النافذة + نصّ التأكيد + الدورة).

## [الهيكل الإداري — تابز: مخطط هرمي + إدارة] ✅
- صفحة الهيكل الإداري (`/tenant/settings/org`) صار فيها تبويبان: **«المخطط الهرمي»** (افتراضي) — مخطط تنظيمي شجري من أعلى لأسفل، كل قسم بطاقة (الاسم + عدد الأعضاء + الدور الافتراضي) وأبناؤه تحته بموصلات CSS متوافقة مع RTL؛ و**«الهيكل الإداري»** — نموذج الإضافة + القائمة المسطّحة القابلة للإدارة/الحذف. الأنماط تُحقَن عبر `dangerouslySetInnerHTML` (تفادي عدم تطابق الترطيب). مُتحقَّق حيًّا.

## [بوّابة العميل — الخدمة الذاتية الكاملة] — مطالبة/طلب خدمة/تجديد + تفاصيل وثيقة + عدّادات ✅
- **الخدمة الذاتية (backend)**: العميل يقدّم من بوّابته مباشرةً — `POST /portal/claims` (مطالبة جديدة بحالة `RECEIVED`) · `POST /portal/service-requests` (طلب خدمة بحالة `OPEN`، ٦ أنواع: شهادة/نسخة/تعديل/إلغاء/تجديد/استفسار) · `POST /portal/policies/:id/renew` (اختصار طلب تجديد). كل تقديم يمرّ عبر **حارس ملكية الوثيقة** (`assertOwnsPolicy` ⇒ 403 على وثيقة الغير) ويُطلق **إشعار الفريق المختصّ** (`staff_claim_created`/`staff_request_created`) ويُسجَّل في التدقيق (`viaPortal`).
- **تفاصيل الوثيقة للعميل**: `GET /portal/policies/:id` يعيد الوثيقة + مطالباتها + مستنداتها (مقصورة على وثائق العميل ⇒ 404 على غيرها). صفحة **تفاصيل وثيقة ٣٦٠°** في البوّابة: مؤشّرات (مبلغ التأمين/الإجمالي/الأيام المتبقّية/عدد المطالبات) + بيانات الوثيقة + التفصيل المالي + جدول المطالبات + المستندات (تُفتح عبر رابط موقّت).
- **الواجهة**: نموذج **«تقديم مطالبة»** (صفحة المطالبات) · نموذج **«طلب خدمة»** (صفحة الطلبات) · زر **«طلب تجديد»** على قائمة الوثائق وصفحة التفاصيل.
- **لوحة العميل — عدّادات محسّنة**: **وثائق قرب الانتهاء (≤٦٠ يوماً)** + **إجمالي مبالغ التأمين** (بدل عدّادَين مكرّرَين) + قائمة الوثائق المنتهية قريبًا بدرجة إلحاح (عاجل ≤١٥ يوماً) وروابط للتفاصيل.
- **البذرة**: تعبئة `Policy.sumInsured` لكل الوثائق عبر عامل تقديري حسب فرع التأمين (مركبات/طبي/ممتلكات/مسؤوليات) — لعرض واقعي في اللوحة وصفحة التفاصيل.
- اختبارات `portal.e2e` (+6: فتح التفاصيل · تقديم مطالبة/طلب خدمة/تجديد · عزل التقديم والتفاصيل 403/404). الإجمالي **e2e 212/212** (34 ملفًا). مُتحقَّق حيًّا (اللوحة + التفاصيل + التجديد end-to-end). موثّق في [docs/25](docs/25-client-portal.md).

## [التجديدات — لوحة بمعايير الوساطة] — نافذة زمنية + مؤشّرات + درجة إلحاح ✅
- **لوحة تجديدات مُعاد بناؤها** (`/tenant/renewals`): مُنتقي نافذة (٣٠/٦٠/٩٠ يومًا) · ٤ مؤشّرات (منتهية/خلال أسبوع/قادمة/معرّضة للخطر) · شارات إلحاح · أعمدة العميل والقسط · رابط لصفحة تفاصيل الوثيقة.
- **الـ backend**: `renewals.due()` صار غنيًّا — يعيد `totalPremium`/`commissionAmount` وأسماء العملاء لكل وثيقة مستحقّة للتجديد ضمن النافذة.

## [تفاصيل الوثيقة 360°] — صفحة `/tenant/policies/[id]` مجمّعة ✅
- **صفحة تفاصيل وثيقة ٣٦٠°** (الشركة): ترويسة + ٤ مؤشّرات + تبويبات (المالية/الملحقات/المطالبات/الفواتير/المستندات/السجلّ الزمني).
- **الـ backend**: `GET /policies/:id/overview` يجمع الوثيقة + العميل + الملحقات + المطالبات + إشعارات المدين + الفواتير + المستندات + النشاط + ملخّصًا ماليًّا في استدعاء واحد.

## [إصلاح جلسة أمني] — انتهاء التوكن يُعيد للدخول بلا انتحال ظاهري ✅
- **العرَض** (رُصد على staging): بعد خمول الصفحة والتحديث، تظهر جلسة موظف باسم أدمن مضمّن والقائمة الجانبية فارغة. **ليست تجاوزًا للجلسات** بل ثلاثة عيوب عرض متراكبة.
- **الإصلاح**: (1) عند 401 مع وجود توكن ⇒ **مسح التوكن وإعادة التوجيه للدخول** حسب النطاق (`tenant`/`platform`/`portal`) في `api.ts`؛ (2) إزالة الاسم الافتراضي المضمّن من `Topbar` (كان يُظهر مستخدمًا بعينه)؛ (3) رفع `JWT_EXPIRES_IN` الافتراضي من 15د إلى **8س** (جلسة عمل واقعية).

## [الاكتتاب الفني — إثراء مالي + الضريبتان] — وفق أويسس ومعايير الوساطة ✅
- **إثراء العرض المالي** (`/tenant/slips/[id]`): حقول مبلغ التأمين/رسوم الوثيقة/معدّل العمولة/مبلغ العمولة، وجدول مقارنة عروض بأعمدة ديناميكية، وزرّ **احتساب** (القسط = مبلغ التأمين × المعدّل، الإجمالي = الصافي + الرسوم + الضريبة).
- **الضريبتان معًا وباحتساب تلقائي 15%**: **ضريبة القسط المباشر** (15% قياسي؛ تأمين الحياة معفى `VATEX-SA-29-7`) + **ضريبة عمولة الوساطة** (15% **دائمًا** بصرف النظر عن الفرع — ضريبة مخرجات الوسيط). حقول `Quotation`: `sumInsured`/`policyFees`/`commissionRate`/`commissionAmount`/`commissionVat` (هجرتان). موثّق في [docs/10](docs/10-underwriting-rfq.md).

## [نموذج الطلب — بحث العميل وإضافة سريعة] — تجربة إدخال احترافية ✅
- **حقل عميل قابل للبحث** (بالاسم/الهوية الوطنية/رقم السجل التجاري) في نموذج الطلب الجديد؛ عند عدم وجود عملاء تُعرض **حالة فارغة** تُلزم بإضافة عميل عبر **بوب-أب سريع** (`POST /clients`) قبل إكمال الطلب، ويعمل بشكل طبيعي عند وجود عملاء.

## [حدّ مستخدمي الباقة (المقاعد)] — يضبطه سوبر أدمن المنصّة ويُفرَض على الشركة ✅
- **تعديل من إعدادات الباقات**: `PUT /platform/plans/:code { seatLimit }` (سوبر أدمن المنصّة) — يعدّل حدّ المقاعد لكل باقة (+الاسم/الأسعار)، مع تدقيق. صفحة الباقات (`/admin/plans`) فيها الآن محرّر **«حد المستخدمين»** لكل باقة بجانب حدّ الرفع/التخزين.
- **الفرض على الشركة**: إنشاء موظف (`POST /staff`) **يُرفَض (403)** عند بلوغ عدد المستخدمين النشطين حدّ باقة الشركة (`Plan.seatLimit` عبر الاشتراك). `GET /staff/seats` يعيد `{ used, limit, planName }` + مزامنة `Subscription.seatsUsed`.
- **الواجهة (الشركة)**: صفحة الموظفين تعرض شارة **«المستخدَم / الحدّ · اسم الباقة»** + تنبيه عند بلوغ الحدّ.
- البذرة: `gulf-demo` نُقل إلى **enterprise** (سعة كافية لبيئة العرض/الاختبار). اختبار `plan-seats.e2e` (4). الإجمالي **e2e 206/206** (34 ملفًا). مُتحقَّق حيًّا (المحرّر + الشارة).

## [كلمة مرور سوبر أدمن المنصّة من البيئة] ✅
- **أمر مخصّص `admin:set`** (`pnpm --filter @ibp/db admin:set`) — يضبط/يُغيّر كلمة مرور سوبر أدمن المنصّة من `PLATFORM_ADMIN_PASSWORD`/`PLATFORM_ADMIN_EMAIL` بلا إعادة بذر كامل: يُنشئ إن لم يوجد، ويُحدّث إن وُجد (idempotent، لا يمسّ MFA). تحقّق: كلمة مرور فارغة/أقل من 12 حرفًا ⇒ رفض.
- **البذر المنشور صار يحترم البيئة**: `seed:demo`/`seed:prod` يستخدمان `PLATFORM_ADMIN_EMAIL`/`PLATFORM_ADMIN_PASSWORD` إن ضُبطت (بدل `Passw0rd!` التطويري) — فلا تبقى كلمة المرور الافتراضية على بيئة منشورة.
- **الغرض**: تحصين staging/production — تغيير كلمة مرور `/admin/login` من متغيّرات Coolify. موثّق في [`coolify.md §8`](infra/docker/coolify.md). مُتحقَّق (guards + happy path + تحديث متكرّر على قاعدة نظيفة).

## [نشر staging] — المنصّة حيّة على `ibp.nx.sa` + تحصين مطابقة الإنتاج ✅
- **staging منشورة** (2026-07-05) على Coolify: `https://ibp.nx.sa` (واجهة) + `https://api.ibp.nx.sa` (API)، Postgres+Redis داخل الحزمة، تكاملات Sandbox، بيانات ديمو GIB عبر `seed:demo`. تُحاكي الإنتاج. **البيئات الثلاث موثّقة** في [docs/32](docs/32-environments.md): dev (جهاز المطوّر) · staging (السيرفر الحالي) · production (لاحقًا، داخل المملكة).
- **إصلاح نشر Coolify:** `build.context` من `../..` إلى `.` (Coolify يبني من جذر المستودع؛ `../..` كان يفشل بـ`lstat /apps`).
- **تحصين مطابقة الإنتاج (Dev-only UI):** تعبئة بيانات الدخول المسبقة (صفحات `login`/`admin`/`portal`) + تلميح «حساب تجريبي» + شارة «Demo Session» — كلها الآن مشروطة بـ`NODE_ENV!=production`، فتُخفى تمامًا في staging/production (خانات دخول فارغة، بلا إشارات ديمو).
- **توثيق:** قسم استكشاف أخطاء واقعي في [`coolify.md`](infra/docker/coolify.md) (مسار الملف، السياق، `exit 255`/الـcache الدافئ، خطوة البذرة عبر `docker exec`) + تحديث [docs/00](docs/00-project-status.md) و[docs/33](docs/33-launch-runbook.md).

## [MFA — افتراضي مُطفأ + إعادة تعيين إدارية] ✅
- **توضيح/تأكيد: المصادقة الثنائية مُطفأة افتراضيًا** لكل الحسابات (البذرة لا تُفعّلها لأحد؛ `mfaEnabled=false`، إلزام الشركة `false`) — فالدخول بكلمة المرور فقط، مناسب لتصفّح الديمو وتجربته. تبقى قابلة للتفعيل/التعطيل ذاتيًّا لكل موظف، والإلزام على مستوى الشركة اختياري.
- **جديد: إعادة تعيين إدارية** — `POST /staff/:id/mfa/reset` (صلاحية الإعدادات) يُتيح **لأدمن الشركة تعطيل مصادقة موظف** عند فقدان الجهاز؛ يعيد الموظف التسجيل لاحقًا. صفحة الموظف 360° تعرض **حالة MFA** + زر «إعادة تعيين المصادقة» (يظهر عند التفعيل فقط). يُسجَّل في التدقيق (`user_mfa_reset`).
- اختبار `mfa-staff.e2e` (+1: إعادة التعيين الإدارية + عزل 403). الإجمالي **e2e 202/202**. مُتحقَّق حيًّا (شارة «غير مفعّلة» على صفحة الموظف).

## [تجهيز حزمة النشر] — إصلاح بناء الواجهة + تحقّق كامل من الحزمة ✅
- **إصلاح جوهري للنشر:** `NEXT_PUBLIC_API_URL` يُدمَج في حزمة العميل **وقت البناء** (لا التشغيل) — كان النشر سيُثبّت `localhost:4000` في المتصفح. صار **build ARG** في `apps/web/Dockerfile` ويُمرَّر عبر `docker-compose.coolify.yml` (`build.args`). **مُتحقَّق:** الحزمة المبنية تحوي الدومين الصحيح بلا أثر `localhost`.
- **تكملة الحزمة:** الـ compose يمرّر الآن `NOTIFY_GATEWAY`/`BILLING_GATEWAY` (sandbox افتراضيًا) + `APP_PUBLIC_URL`/`API_PUBLIC_URL` + `SEED_MODE`/`PLATFORM_ADMIN_*` (لخطوة البذرة داخل الحاوية).
- **تحقّق end-to-end محليًّا (Docker):** الصورتان (api/web) تُبنيان · حاوية الـ API تُقلع خلال ~4ث وتطبّق **26 هجرة** تلقائيًا · `/health` = DB+Redis أخضر · `seed:demo` داخل الحاوية يبذر GIB والدخول ينجح عبر الـ API الحاوية.
- **توثيق:** قسم «نشر ديمو سريع» في [`coolify.md`](infra/docker/coolify.md) (المتغيّرات الدنيا Sandbox + تحذير إعادة البناء عند تغيير دومين الـ API).

## [الاحتفاظ والإتلاف الآمن + DLP] — حق المحو (PDPL) وإخفاء البيانات الحسّاسة ✅
- **DLP (إخفاء أقلّ امتياز)**: **الهوية الوطنية/الإقامة والآيبان تُخفى** لكل من لا يملك صلاحية **الالتزام أو المالية** (يبقى آخر 4 خانات للمطابقة)، وتظهر كاملةً لمن يحتاجها وظيفيًّا. يُطبَّق آليًّا على قائمة العملاء + التفاصيل + النظرة 360° (`common/security/dlp.ts`).
- **حق المحو (PDPL)**: `POST /clients/:id/erase` (صلاحية حذف العملاء) — يُخفي **كل PII** (الاسم/الهوية/التواصل/الآيبان/العنوان/الرقم الضريبي) و**يُبقي الهيكل المالي** (وثائق/مطالبات/قيود/فواتير) لسلامة التدقيق وZATCA؛ يُسجَّل في **سجلّ الإتلاف الثابت** (تدقيق `erase`). لا يتكرّر (409). حقول `Client.erasedAt`/`erasedBy` (migration `client_erasure`).
- **سجلّ الإتلاف + تقرير الاستحقاق**: `GET /clients/erasures` (الممحوّون — كود/توقيت/مُنفِّذ بلا PII) · `GET /clients/retention/due` (عملاء تجاوز آخر نشاطهم مدّة الاحتفاظ — استعراضي، الإتلاف بقرار).
- **سياسة الاحتفاظ**: `GET/PUT /config/retention { retentionYears }` (صلاحية الإعدادات، 1–30 سنة، افتراضي **10** — سجلّات التأمين).
- **الواجهة**: زر **«محو البيانات (PDPL)»** بتأكيد خطر على صفحة العميل 360° (لأصحاب حذف العملاء) + شارة «بيانات ممحوّة» · قسم **«الاحتفاظ بالبيانات»** على صفحة `/tenant/settings/security`.
- اختبار `retention-dlp.e2e` (6: إخفاء/إظهار حسب الصلاحية · محو + عدم تكرار · سجلّ الإتلاف · عزل 403 · حدود المدّة · تقرير الاستحقاق). الإجمالي **e2e 201/201** (33 ملفًا). مُتحقَّق حيًّا (الإخفاء + المحو + الواجهتان).

## [تجهيز الإطلاق] — فصل بذرة الإقلاع الإنتاجية عن بذرة الديمو ✅
- **`SEED_MODE=production` (بذرة إقلاع الإنتاج)**: تبذر المرجعيات فقط — الباقات + كتالوج المنتجات + مزوّدي التحقّق + **سوبر أدمن من البيئة** (`PLATFORM_ADMIN_EMAIL`/`PLATFORM_ADMIN_PASSWORD`، ≥12 حرفًا، لا تُعاد كتابته إن وُجد). **لا مستأجرين وهميين ولا بيانات تجريبية** — حساب العميل الحقيقي (GIB) يُنشأ لاحقًا عبر `/signup`/التزويد.
- **حاجز أمان**: بذرة الديمو **مرفوضة تلقائيًا على `NODE_ENV=production`** ما لم يُضبط `SEED_MODE=demo` صراحةً — يمنع حقن شركات وهمية في الإنتاج الحقيقي عن طريق الخطأ.
- **سكربتات**: `seed:prod` (إنتاج، مرجعيات + أدمن) · `seed:demo` (ديمو منشور صريح) · `seed` (تطوير محلي، دون تغيير). لا كلمة مرور أدمن افتراضية في الإنتاج (كانت `Passw0rd!`).
- **مُتحقَّق**: على قاعدة نظيفة ⇒ 3 باقات · 10 فئات · 47 خطًا · 5 مزوّدين · **1 سوبر أدمن · 0 مستأجر · 0 عميل**. الحاجزان يرفضان كما هو متوقّع. مسار الديمو (التطوير/الاختبار) دون تغيير — **e2e 195/195**. توثيق: `.env.example` · `coolify.md` · [33](docs/33-launch-runbook.md).

## [المصادقة الثنائية للموظفين] — MFA (TOTP) لموظفي شركات الوساطة + إلزام على مستوى الشركة ✅
- **تسجيل ذاتي (TOTP)** لكل موظف: `POST /auth/mfa/setup` (يولّد سرّ Base32 + رابط otpauth للـQR) ثم `POST /auth/mfa/enable` بعد تأكيد رمز من تطبيق المصادقة · `POST /auth/mfa/disable` · `GET /auth/mfa/status`. يعيد استخدام محرّك TOTP اليدوي القائم (`common/security/totp.ts`، بلا تبعية خارجية) — نفس معيار سوبر أدمن المنصة.
- **تحدّي دخول من خطوتين**: عند تفعيلها، `POST /auth/login` بكلمة المرور وحدها ⇒ `401 MFA_REQUIRED` (تكشفها الواجهة فتُظهر حقل الرمز)؛ مع رمز صحيح ⇒ توكن. الرمز الخاطئ يُحتسب محاولة فاشلة في حماية القوّة الغاشمة.
- **إلزام على مستوى الشركة (سياسة أمان)**: `GET/PUT /config/security { mfaRequired }` (صلاحية الإعدادات) — عند التفعيل، من لم يُسجّل بعد يحصل على `mfaEnrollmentRequired` في الدخول و`/auth/me` فتدفعه الواجهة للتسجيل، **ولا يمكن لأي موظف إلغاء الـMFA تحت الإلزام**.
- **المخطّط**: `User.mfaSecret`/`User.mfaEnabled` + `TenantConfig.securityPolicy` (Json) — migration `staff_mfa`. كل تغييرات MFA تُسجَّل في التدقيق (`user_mfa`/`security_policy`).
- **الواجهة**: صفحة `/tenant/settings/security` (تفعيل/إلغاء + مبدّل سياسة الشركة لأصحاب صلاحية الإعدادات) + تحدّي الرمز في صفحة الدخول. بند قائمة جانبية `settings.security` (مفلتر بالصلاحيات).
- اختبار `mfa-staff.e2e` (6: إعداد/تفعيل برمز صحيح-خاطئ · تحدّي الدخول · إلزام الشركة + منع الإلغاء · رفع الإلزام والإلغاء · عزل الصلاحية). الإجمالي **e2e 195/195** (32 ملفًا). مُتحقَّق حيًّا (الحلقة الكاملة + الواجهتان).

## [مجدول التذكيرات الدورية] — تذكيرات آلية للمهام المستحقّة وتجديد الوثائق ✅
- **مجدول داخلي** عبر `@nestjs/schedule` (`RemindersModule` + `RemindersService`) — مهمة cron يومية (`EVERY_DAY_AT_8AM`) تمسح **كل المستأجرين** وتُطلق تذكيرين بلا تكرار:
  - **مهام CRM المستحقّة**: `dueDate ≤ اليوم` ومفتوحة وغير مُذكَّرة ⇒ إشعار المُسنَد إليه (نوع جديد `staff_task_due`).
  - **الوثائق المقتربة من الانتهاء**: مُصدَرة و`endDate` خلال **30 يومًا** وغير مُذكَّرة ⇒ إشعار فريق التجديدات (`staff_renewal_due`) + تذكير العميل (`renewal_reminder`، Email/SMS).
- **الحتمية (idempotency):** حقلان جديدان `CrmTask.reminderSentAt` و`Policy.renewalRemindedAt` يُوسَمان بعد الإشعار فلا يتكرّر التذكير أبدًا (migration `reminder_fields` + فهرس `Policy(tenantId,status,endDate)`).
- **العزل:** المسح خارج سياق الطلب (عابر للمستأجرين داخل `ctx.run({})`)، لكن كل تذكير يُوجَّه بمعرّف مستأجره الصريح إلى دوال الإشعار المعزولة. **تشغيل يدوي** `POST /reminders/run` **مقصور على مستأجر المُستدعي** بصلاحية إشرافية (تعديل المبيعات) لتفادي إطلاق جماعي دون تخويل.
- اختبار `crm.e2e` (مهمة مستحقّة ⇒ تذكير + **عدم تكرار عند المسح الثاني** + عزل صلاحية 403 على التشغيل). أنواع الإشعارات **21** (تحديث `notifications.e2e`). الإجمالي **e2e 189/189** (بلا تراجع).

## [توسعة كتالوج المنتجات] — تغطية فروع الوساطة السعودية كاملةً ✅
- **الكتالوج من 7 فئات/15 خطًا إلى 10 فئات/47 خطًا فرعيًا** (`packages/shared/src/product-catalog.ts`) — يغطّي السوق السعودي: **الطبي** (جماعي/فردي/SME/عمالة/VIP) · **المركبات** (شامل/ضد الغير/أسطول) · **الممتلكات** (PAR/حريق/انقطاع أعمال/منازل/سطو) · **الهندسي** (CAR/EAR/عطل آلات/إلكترونيات/آليات/تأخّر تشغيل) · **البحري** (بضائع/أجسام/أجرة) · **الحوادث والمسؤوليات** (حوادث شخصية جماعي/فردي · عامة/منتجات/مهنية/D&O/أصحاب عمل/أخطاء طبية/سيبراني/خيانة أمانة/سفر) · **الطيران** (أجسام/مسؤولية) · **الطاقة** (برية-بحرية/حقول) · **الضمانات والسندات** (مناقصات/حسن تنفيذ/دفعة مقدمة/صيانة/جمركي) · **الحياة والحماية** (لأجل/جماعي/ائتماني/عقاري/ادخار — كلها معفاة ض.ق.م E1).
- **قسمان جديدان في محرّك النماذج**: `limitSection` (حدود التغطية للفروع بلا كتلة متكررة) و`suretySection` (بيانات الضمانات) — وكل خط له نموذج ديناميكي (يُعاد استخدام الكتل: أعضاء/مركبات/مواقع/شحنات/أرواح/مسافرون).
- كل الخطوط تُبذَر وتُفعَّل تلقائيًا (`enabledProducts`) وتظهر في شجرة الكتالوج والنموذج الديناميكي. اختبار `requests.e2e` (الكتالوج الموسّع + الفئات العشر). الإجمالي **e2e 188/188** (بلا تراجع).

## [المرحلة E5 — 360°] — صفحة العميل وصفحة الموظف المجمّعتان ✅
- **صفحة العميل 360°** `/tenant/clients/[id]` — تابات: نظرة عامة/**الوثائق المُصدَرة**/**التجديدات** (وثائق قاربت على الانتهاء ≤90 يومًا بمؤشّر «متبقٍّ/منتهية»)/المطالبات/الطلبات/المستندات/كشف الحساب/**الخط الزمني** + مؤشرات + **إضافة ملاحظة**. عبر `GET /clients/:id/overview` (معزول بالمستأجر).
- **صفحة الموظف 360°** `/tenant/settings/staff/[id]` — بياناته/دوره/قسمه + مؤشرات إنتاجية (عمليات/إصدارات/اعتمادات) + تابات **ما تحت مسؤوليته**: «وثائق أصدرها» (من التدقيق) · «صفقاته» (المُسنَدة إليه) · «مهامه» · «النشاط». عبر `GET /staff/:id` المُثرى (deals/tasks/policies).
- أسماء العملاء والموظفين في القوائم صارت **روابط** لصفحات التفاصيل. عزل مُثبَت (مستأجر آخر لا يقرأ نظرة عميل ليس له ⇒ 404).
- اختبار `detail-360.e2e` (3: نظرة العميل + عزل 404 + تفاصيل الموظف). الإجمالي **e2e 187/187** (31 ملفًا). **بذلك تكتمل رؤية E5 (CRM + 360°).**

## [المرحلة E5] — إدارة العلاقات (CRM): خطّ أنابيب + مهام + نشاط ✅ (القسم الأساسي)
- **نماذج** `Deal` (خطّ أنابيب بمراحل new→contacted→quoting→proposal→negotiation + حالة won/lost + إسناد + قيمة متوقّعة) · `CrmTask` (مهمة/تذكير بأولوية وتاريخ استحقاق قابلة للإسناد) · `CrmActivity` (نشاط/ملاحظة **polymorphic** — يخدم الخط الزمني). migration `crm_core`.
- **`CrmService` + `/crm`** (تحت وحدة المبيعات): صفقات (قائمة مُثراة باسم العميل/المُسنَد · إنشاء · تحديث مرحلة/إسناد/كسب/فقد) · مهام (قائمة/`?mine=1`/إنشاء/إكمال) · نشاط (خط زمني/إضافة). **نقل المرحلة وتغيّر الحالة يُسجَّلان تلقائيًا كنشاط**.
- **رؤية حسب الدور (فصل المهام في CRM):** من له **حذف على المبيعات** (المدير العام/مدير المبيعات) = **مدير** يرى **كل** الصفقات/المهام ويعدّلها؛ **المندوب** (مبيعات بلا حذف) يرى ويعدّل **ما أُسنِد إليه أو أنشأه فقط** (محاولة تعديل صفقة غيره ⇒ 403). الواجهة تُخفي مبدّل «مهامّي/الكل» عن المندوب. وال CRM كقسم يظهر فقط لمن له وحدة المبيعات (مفلتر بالقائمة + الـbackend).
- **لوحة «يحتاج متابعة» (عابرة للوحدات، تحترم الصلاحيات):** `GET /crm/follow-up` يجمع ما يحتاج انتباهًا: **وثائق قاربت الانتهاء** (≤60 يومًا) · **طلبات مفتوحة** · **مطالبات نشطة** (فقط لمن له claims) · **عمولات غير محصّلة** (فقط لمن له finance) · **مهام متأخّرة** (المدير: الكل / المندوب: مهامّه). بطاقات أعلى صفحة CRM. (`crm.e2e` — اختبار احترام الصلاحيات). الإجمالي e2e 188/188.
- **إشعارات الإسناد**: `NotificationsService.notifyUser(tenantId, userId, key, vars)` يُشعِر **مستخدمًا بعينه** (in-app + بريد) + نوعا `staff_task_assigned`/`staff_deal_assigned` (الكتالوج صار 20 نوعًا).
- **الواجهة**: صفحة `/tenant/crm` — **لوحة خطّ أنابيب** (أعمدة بالمراحل، بطاقات صفقات بالعميل/القيمة/المُسنَد + نقل مرحلة + كسب/فقد) + **لوحة مهام** (أولوية/استحقاق/إكمال + تبديل مهامّي/الكل) + نماذج إنشاء. عنصر تنقّل «إدارة العلاقات» (مفلتر بوحدة المبيعات). بيانات ديمو (صفقات/مهام). مُتحقَّق حيًّا.
- اختبار `crm.e2e` (4: صفقة + نقل مرحلة يُسجَّل نشاطًا + إثراء · مهمة بإسناد ⇒ إشعار in-app + إكمال · ملاحظة/خط زمني · 403 بلا مبيعات). الإجمالي **e2e 183/183** (30 ملفًا). [ ] صفحة عميل 360° + صفحة الموظف (تُبنى على نفس الأساس).

## [المرحلة E4] — التراجع خطوة للوراء (Undo/Revert) ✅
- **صلاحية جديدة `Permission.canRevert`** (migration `permission_can_revert`) + فعل RBAC **"revert"** (`ACTION_FLAG.revert = canRevert`) + رمز مصفوفة **"R"** في `@ibp/shared` (`parsePerm`). تُمنح للمدير العام (preset) ومالك الحساب (signup) وأي دور مخصّص عبر محرّر الموظفين. صلاحية إشرافية «فوق الموظف» كما طُلبت.
- **`RevertService` + `POST /revert/:entityType/:id`**: يُرجِع كيان آلة الحالة (`policy`/`claim`/`service_request`/`request`) **خطوة واحدة** لسابقتها في السلسلة القانونية. الصلاحية **ديناميكية** (وحدة الكيان) عبر `PermissionService` — بلا `@Authorize` ثابت. القراءة/الكتابة معزولة بالمستأجر.
- **حواجز امتثالية (هيئة التأمين/NCA):** الحالات النهائية/المالية **غير قابلة للتراجع** — **الوثيقة المُصدَرة (ISSUED) تُرفَض بـ 409** وتتطلّب إجراءً تعويضيًا (إشعار دائن/قيد عكسي) حفظًا لثبات القيد والتزام ZATCA؛ الحالة الأولى ⇒ 409. الوثيقة عند التراجع (FINANCE_REVIEW⇒TECHNICAL_REVIEW) تُفرَّغ خطوات اعتمادها ويُرجَع طلبها المرتبط. **التراجع نفسه إجراء مُدقَّق جديد** (`action="revert"`) — لا يمحو التاريخ.
- **الواجهة**: `/auth/me` صار يُرجِع خريطة صلاحيات المستخدم (module ⇒ access/create/edit/delete/revert). زر **«تراجع خطوة»** على صفحة الوثائق يظهر فقط لمن له `canRevert` على الإنتاج + للوثائق في حالة قابلة. ترجمات ar/en. مُتحقَّق حيًّا.
- اختبار `revert.e2e` (4: تراجع مطالبة + منع الحالة الأولى · 403 بلا صلاحية · 409 للوثيقة المُصدَرة · 400 لنوع غير مدعوم). الإجمالي **e2e 179/179** (29 ملفًا).

## [نظام الإشعارات — مركز الإشعارات داخل المنصة (in-app)] ✅
- **نموذج `Notification`** (migration `notifications_inbox`): صفّ لكل مستقبِل — موظف (`userId`) أو عميل بوّابة (`clientId`) — بحقول `eventKey/audience/title/body/readAt`، معزول بالمستأجر (فهارس على `[tenantId, userId, readAt]` و`[tenantId, clientId, readAt]`).
- **حفظ تلقائي داخل المنصة** عند كل حدث مُفعَّل: `notify` يحفظ نسخة للعميل (بتمرير `clientId`)، و`notifyStaff` يحفظ نسخة لكل موظف مستقبِل — بالتوازي مع الإرسال الخارجي (Email/SMS)، فيبقى سجلّ داخل المنصة حتى بلا مزوّد خارجي.
- **API صندوق الموظف** (مصادقة فقط، كل موظف يرى إشعاراته): `GET /notifications/inbox` · `GET /notifications/inbox/unread-count` · `POST /notifications/inbox/:id/read` · `POST /notifications/inbox/read-all`. **API بوّابة العميل**: `GET /portal/notifications` · `…/unread-count` · `POST /portal/notifications/:id/read` (معزول بـ clientId).
- **الواجهة**: مكوّن `NotificationBell` مُعمَّم (يُستخدم في `Topbar` للموظف و`PortalShell` للعميل) — **شارة غير المقروء** + قائمة منسدلة (عنوان/نص + نقطة «غير مقروء») + تعليم كمقروء (فردي/الكل). **مُتحقَّق حيًّا**: حدث «مطالبة جديدة» ظهر للمالك بشارة «1» ثم اختفت بعد القراءة.
- اختبارات: `staff-notifications.e2e` (+حفظ داخل المنصة/صندوق/تعليم مقروء/عزل بين الموظفين) + `portal.e2e` (إشعار العميل يظهر في بوّابته + عزل بين العملاء + تعليم مقروء). الإجمالي **e2e 170/170** (27 ملفًا).

## [نظام الإشعارات — توسعة لكامل النظام (عملاء + موظفون)] ✅
- **بُعد الجمهور (`audience`)** على كل نوع إشعار: **client** (يُرسَل لعميل الشركة عبر Email/SMS) أو **staff** (يُرسَل لموظفي الشركة عبر البريد). الكتالوج توسّع من 7 إلى **18 نوعًا** (7 عملاء + 11 موظفين)، وكلٌّ قابل للتفعيل/التعطيل وتعديل نصّه على مستويين (افتراضي المنصة/تخصيص الشركة).
- **`NotificationsService.notifyStaff(tenantId, key, vars)`**: يوجّه إشعار الموظفين لأصحاب صلاحية **وحدة الحدث** (`Permission.canAccess`) **+ مالك الحساب** (أوّل مستخدم) دائمًا، مُزال التكرار بالبريد. يُنفَّذ داخل سياق فارغ ويفلتر بالمستأجر صراحةً (يعمل حتى خارج سياق الطلب — مثل webhook الفوترة).
- **تغطية كل العمليات المهمّة** (fire-and-forget، لا تُفشِل العملية الأصل): `staff_request_created` (طلب) · `staff_quotation_added` (عرض) · `staff_policy_technical_review` (إصدار⇐فني) · `staff_policy_finance_review` (فني⇐مالي) · `staff_policy_issued` (اكتمال الإصدار) · `staff_claim_created` (مطالبة) · `staff_renewal_due` (تجديد) · `staff_verification_result` (تحقّق) · `staff_wallet_low` (رصيد محفظة التحقق < 10) · `staff_member_added` (مستخدم جديد) · `staff_subscription_status` (اشتراك). كل موديول مُطلِق يستورد `NotificationsModule`.
- **الواجهة**: مكوّن `NotificationManager` يجمع الأنواع بقسمين (**إشعارات العملاء** / **إشعارات الموظفين**) مع تلميح لكلٍّ، وإشعارات الموظفين «بريد فقط» (لا هاتف للمستخدمين). ترجمات ar/en. عنوان صفحة الشركة صار «كل الإشعارات (عملاء وموظفين)». **مُتحقَّق حيًّا** (القسمان يظهران RTL).
- اختبار `staff-notifications.e2e` (توجيه: يصل للمالك + صاحب صلاحية الوحدة، لا لغيره) + تحديث `notifications.e2e` (18 نوعًا + جمهور). الإجمالي **e2e 167/167** (27 ملفًا).

## [المرحلة E2] — سلاسل اعتماد الوثيقة القابلة للتهيئة (المحرّك + API) ✅
- **خطوات اعتماد إضافية قابلة للتهيئة** بين الموافقة الفنية والاعتماد المالي — تُخزَّن في `TenantConfig.approvalChains.policy.extraSteps`، لكل خطوة `{key, name, module, action}` (الوحدة/الفعل المطلوب صلاحيتهما للموافقة). `ConfigService` + `GET/PUT /config/approval-chain` (تحت settings، مُدقَّق).
- **الإنفاذ**: عند الموافقة الفنية تُحجَز مفاتيح الخطوات على `Policy.pendingApprovals` (migration `approval_chains`)، و**الاعتماد المالي (`FinanceService.approvePolicy`) محجوب بـ 409** حتى تُفرَّغ. `POST /policies/:id/approve-step {stepKey}` يتحقّق **ديناميكيًا** من صلاحية المستخدم لوحدة/فعل الخطوة عبر `PermissionService` (لا `@Authorize` ثابت)، ثم يُفرِّغ الخطوة ويُدقّق، ويُشعر المالية عند اكتمالها.
- **متوافق رجعيًا**: السلسلة الافتراضية فارغة ⇒ فني ⇒ مالي كما كان (كل الاختبارات القائمة خضراء بلا تعديل).
- **ديناميكي مع حارس امتثالي** (بناءً على توجيه: «متوافق مع الأنظمة، ديناميكي فيما تسمح به»): ✅ **البوّابة الفنية قابلة للتفعيل/التعطيل** من الشركة (`technicalGate` — عند تعطيلها يذهب الإصدار مباشرة للاعتماد المالي). ✅ **فصل المهام** (`segregationOfDuties`، افتراضي مُفعَّل — توصية رقابية هيئة التأمين/NCA): **المعتمِد المالي ≠ مُصدِر الوثيقة** (يُشتقّ المُصدِر من سجلّ التدقيق الثابت)، قابل للتخفيف من الشركة لهيكلها المرخّص. الاعتماد المالي/الإصدار يبقى الخطوة الطرفية الإلزامية (توليد ZATCA/القيد).
- **الواجهة**: صفحة `/tenant/settings/approval-chain` (تحت الإعدادات) — تصوّر المسار (فني ⇐ خطوات ⇐ مالي) + **مفتاحا سياسة** (البوّابة الفنية · فصل المهام بوسم «توصية رقابية») + إضافة/حذف خطوة باسمها ووحدتها المخوّلة وفعلها، وحفظ. عنصر تنقّل «سلسلة الاعتماد» + ترجمات ar/en. مُتحقَّقة حيًّا.
- اختبارات إضافية في `approval-chain.e2e` (تعطيل البوّابة الفنية ⇒ FINANCE_REVIEW مباشرة · فصل المهام: المُصدِر يُمنع 403 وبتعطيله يُسمح). الإجمالي **e2e 175/175**.
- اختبار `approval-chain.e2e` (3: قراءة السلسلة · رفض وحدة غير معروفة 400 · الشلال بخطوة إضافية — المالية محجوبة، صلاحية الخطوة ديناميكية 403/200، ثم ISSUED). الإجمالي **e2e 173/173** (28 ملفًا).

## [المرحلة E1] — الضريبة حسب فرع التأمين (إعفاء تأمين الحياة) ✅
- **قسط تأمين الحياة (فئة `LIF`) معفى** من ضريبة القيمة المضافة — خدمة مالية وفق المادة 29 من اللائحة التنفيذية: نسبة **0%**، فئة ZATCA **"E"**، رمز الإعفاء **`VATEX-SA-29-7`** ونصّه. بقية الفروع (طبي/مركبات/ممتلكات/هندسي/بحري/حوادث عامة/سفر) قياسية **15%** (فئة **"S"**).
- **الإنفاذ في نقطتين** عبر `common/tax/vat.ts` (`vatTreatmentForClass`، مستقلّ عن `@ibp/shared` وقت التشغيل — قرار المرحلة 0): (1) **الإصدار** (`ProductionService.issuePolicy`) يفرض ضريبة القسط حسب فئة المنتج **حتى لو أدخل المسعّر ضريبةً خاطئة**؛ (2) **الاعتماد المالي** (`FinanceService.approvePolicy`) يعيد فرضها على إشعار المدين + مستند ZATCA للعميل.
- **وسم ZATCA**: امتداد `UblLineItem` بـ `taxCategory` + `exemptionReasonCode`/`exemptionReason`، ويُصدِر بند الضريبة `cac:TaxCategory` (ID + النسبة + سبب الإعفاء للمعفى). لا يكسر سلسلة التجزئة (لا تأكيدات تجزئة حرفية).
- **عمولة الوساطة تبقى خاضعة 15%** (فئة "S") بصرف النظر عن فرع الوثيقة — رسم خدمة خاضع للضريبة.
- مصدر القاعدة مُطابَق في كتالوج `@ibp/shared` (`CatalogClass.vatExempt` على `LIF` + `isVatExemptLine`/`classCodeOfLine`) لعرض الإعفاء في الواجهة.
- اختبار `vat-branch.e2e` (3: إعفاء الحياة عند الإصدار · الشلال الكامل ⇒ إشعار مدين بضريبة 0 وفاتورة عمولة 15% · ضبط الطبي 15% دون تراجع). الإجمالي **e2e 166/166** (26 ملفًا).

## [المرحلة E3] — ربط أحداث الإشعارات التلقائية بدورة العمل ✅
- **إشعارات مُطلَقة تلقائيًا عند انتقالات الحالة** عبر `NotificationsService.notify` — كلها **fire-and-forget** (`void … .catch()`) فلا تُفشِل العملية الأصل عند تعذّر الإرسال، وتجلب `email/phone` للعميل ثم ترسل حسب القنوات المفعّلة والنص المُهيّأ لكل مستأجر:
  - **`debit_note`** — عند الاعتماد المالي للوثيقة (`FinanceService.approvePolicy`، **بعد تثبيت المعاملة**) ⇒ إشعار العميل برقم إشعار المدين.
  - **`request_ack`** — عند إنشاء طلب خدمة (`ServiceService.create`) ⇒ استلام الطلب.
  - **`claim_ack`** — عند إنشاء مطالبة (`ClaimsService.create`) ⇒ استلام المطالبة.
  - **`renewal_reminder`** — عند بدء تجديد (`RenewalsService.initiate`) ⇒ تذكير باستحقاق تجديد الوثيقة.
  - (`policy_issued` كان موصولًا سابقًا عند إصدار الوثيقة.)
- كل موديول مُطلِق يستورد `NotificationsModule` ويحقن `NotificationsService`. `tax_invoice` للمؤمِّن مؤجّل (لا جهة اتصال للمؤمِّن في النموذج الحالي).
- الإجمالي **e2e 163/163** (25 ملفًا) دون تراجع — الطبيعة اللاحاجزة للإشعارات تضمن عدم تأثّر المسارات القائمة.

## [الإطلاق] — دليل الإطلاق + محوّل إشعارات فعلي (Taqnyat/Resend) ✅
- **[`docs/33` — دليل الإطلاق](./docs/33-launch-runbook.md)**: قائمة ما قبل الإطلاق (بنية/نشر · توصيل التكاملات Sandbox⇒Production · أمن/امتثال NCA · بيانات) + go-live + التراجع.
- **محوّل إشعارات فعلي `LiveNotificationGateway`** (بلا تبعية، `fetch`): **SMS عبر Taqnyat** (`POST /v1/messages`) + **Email عبر Resend** (`POST /emails`) — يُفعَّل بـ `NOTIFY_GATEWAY=live` + المفاتيح. بناء الطلب مفصول ومُختبَر دون شبكة (`notification-gateway.e2e` 3). متغيّرات `TAQNYAT_*`/`RESEND_*` في [`.env.example`](./.env.example). الإجمالي **e2e 163/163** (25 ملفًا).

## [المرحلة H] — نظام الإشعارات والرسائل (Email/SMS) ✅ (النواة)
- **`NotificationSetting`** بمستويين: **افتراضي المنصة** (يديره سوبر أدمن المنصة، يرثه كل الحسابات) و**تخصيص الشركة** (يديره مالك الحساب فوق الافتراضي). 7 أنواع (ترحيب/إصدار وثيقة/إشعار مدين/فاتورة/استلام طلب/استلام مطالبة/تذكير تجديد) × قناتين (Email/SMS). migration `notification_settings`.
- **`NotificationsService`**: `list` (دمج التخصيص←الافتراضي←النظام مع مصدر كل إعداد) · `update` (upsert لكل مستوى) · `notify(tenantId, key, to, vars)` يحترم تفعيل القناة ويعبّئ المتغيّرات ويرسل عبر **بوّابة قابلة للتبديل** (`SandboxNotificationGateway` الآن؛ مزوّد فعلي لاحقًا).
- **الواجهة**: صفحة `/admin/notifications` (المنصة) وصفحة `/tenant/settings/notifications` (الشركة) — مكوّن مشترك: تفعيل/تعطيل كل قناة + تعديل نص كل نوع + شارة المصدر (مخصّص/موروث/افتراضي). عنصرا تنقّل.
- **مُتحقَّق حيًّا**: تخصيص الشركة يحفظ ويتجاوز الافتراضي؛ افتراضي المنصة يُورَّث. اختبار `notifications.e2e` (6: القائمة/التخصيص/الوراثة/العزل/نوع مجهول/المصادقة). الإجمالي **e2e 160/160** (24 ملفًا).
- **إصلاح دقيق**: `ctx.run({})` يجب أن يُنهي استعلام Prisma الكسول **داخله** (await داخل السياق) وإلا يلتقط سياق المستأجر — أُصلح في `rows`.
- متبقٍّ: ربط مزوّد SMS/Email فعلي + ربط الأحداث التلقائية (E3).

## [المرحلة G/SLA] — سجل تدقيق ثابت (Immutable) + التقاط IP (NCA ECC) ✅
- **`AuditLog` غير قابل للتعديل أو الحذف**: يُرفض `update/delete/upsert` في **طبقة Prisma** (`$use`) — حتى من كود المنصّة نفسها (دفاع متعمّق).
- **كل حدث يلتقط الآن**: المستخدم + **عنوان IP** (يراعي `X-Forwarded-For` خلف العاكس) + **الجهاز (User-Agent)** + الإجراء + الكيان + الوقت. (أعمدة `ipAddress`/`userAgent`، تُملأ من سياق الطلب عبر الـ middleware.) migration `immutable_audit_ip`.
- **مراجعة/تصدير للمفتّشين**: `GET /platform/audit` (سوبر أدمن المنصة، عابر للمستأجرين، فلترة اختيارية بالمستأجر).
- **توثيق RBAC (لا صلاحيات مطلقة)** + مصفوفة الإسناد في [`docs/30` §5.5](./docs/30-security-and-compliance.md): الأدوار قابلة للتخصيص (module×CRUD) فيمكن حصر «مسؤول النظام» على الإعدادات دون البيانات الحسّاسة.
- اختبار `audit-immutable.e2e` (4: رفض التعديل/الحذف + التقاط IP + التفتيش/المنع). الإجمالي **e2e 154/154** (23 ملفًا).

## [المرحلة G/P0] — المصادقة الثنائية (MFA) لسوبر أدمن المنصة ✅
- **TOTP يدوي** (`common/security/totp.ts`) عبر `node:crypto` — **بلا تبعية** (RFC 6238، SHA-1/6/30، متوافق Google Authenticator/Authy، مُتحقَّق ضد متجه RFC القياسي).
- **`PlatformAdmin`**: `mfaSecret` + `mfaEnabled` (migration `platform_mfa`). مسارات: `POST /platform/mfa/{setup,enable,disable}` + `GET /platform/mfa/status`. **الدخول يفرض الرمز** عند التفعيل (بدونه `401 MFA_REQUIRED`، خاطئ ⇒ 401).
- **الواجهة**: صفحة `/admin/security` (تفعيل: عرض السرّ/otpauth ⇒ تأكيد برمز ⇒ تعطيل) + حقل رمز في صفحة دخول الأدمن يظهر عند الحاجة + عنصر تنقّل «الأمان».
- **مُتحقَّق حيًّا**: تدفّق كامل برمز مُولَّد في المتصفح (setup→enable→الدخول يُحظر بلا رمز→ينجح بالرمز→تعطيل). اختبار `mfa.e2e` (5). الإجمالي **e2e 150/150** (22 ملفًا). يُكمل بند G/P0 (مطلب SLA/NCA §5.5).

## [المرحلة E0] — إثراء حقول المؤمن له والوثيقة (معايير وساطة التأمين) ✅
- بعد تدقيق فجوة مقابل **مواصفة متطلبات الوساطة** (المرجع الوظيفي)، أُضيفت الحقول المعيارية الناقصة:
  - **العميل (`Client`)**: `relationStatus` (أسير/غير أسير)، `legalForm` (الشكل القانوني)، `source` (مباشر/منتِج) + `producerName`، `businessActivity`، `vatNumber`، `iban`، `contacts[]`.
  - **الوثيقة (`Policy`)**: `insurerPolicyNo` (رقم المؤمِّن الرسمي)، `issuanceType`، `issueDate`، `policyFees`، `sumInsured`، `paymentTerms`، `producerName`، `producerCommission`.
- migration `broker_field_enrichment` + DTOs/خدمات (clients/production) بتحقّق القيم المقيّدة + نموذج إنشاء العميل في الواجهة.
- **مُتحقَّق حيًّا**: إنشاء عميل بالحقول الجديدة + إصدار وثيقة بـ insurerPolicyNo/sumInsured. اختبار `broker-fields.e2e` + تعزيز `finance.e2e`. **e2e 145/145** (21 ملفًا).
- **متبقٍّ:** توسعة كتالوج الخطوط الفرعية (~50) + مكتبة تنزيل النماذج في بوابة العميل.
- (توثيق: نُقلت المواصفة المرجعية إلى `docs/broker-requirements-coverage.md` وحُيِّدت أي علامة تجارية خارجية.)

## [المرحلة D2] — ضغط الصور عند الرفع (WebP) ✅
- **ضغط تلقائي للصور** (المسار المحلي): `StorageService.maybeCompress` عبر **`sharp`** (تبعية جديدة، بموافقة المستخدم) — الصور القابلة للضغط (المرفقات، غير الرسمية، غير WebP) تُحوَّل إلى **WebP جودة 80، أقصى بُعد 1200px**، مع احترام اتجاه EXIF وعدم تكبير الحجم. مطبَّق في `receiveBlob` قبل التخزين.
- **تكامل مع الحصّة (D1)**: الحجم المخزَّن المضغوط يُطابَق في عدّاد الاستهلاك (`reconcile`) فتعكس الحصّة الحجم الفعلي بعد الضغط.
- **قيد صادق**: الرفع السحابي المباشر (S3/R2) لا تمرّ بايتاته بالـ API ⇒ الضغط الخادمي غير ممكن؛ مؤجَّل كـ **worker خلفي** (D2-later).
- اختبار `image-compression.e2e` (PNG 2000px ⇒ WebP أصغر + استرجاع بوسم `RIFF/WEBP` + الحصّة بالمضغوط · PDF دون مساس). الإجمالي **e2e 142/142** (20 ملفًا).

## [تحسين] — تحكّم سوبر أدمن المنصة في حصّة التخزين لكل باقة ✅
- **صفحة الباقات** (`/admin/plans`): أُضيف حقل **حصّة التخزين (جيجابايت)** لكل باقة بجانب حدّ رفع الملفات — يقرأ/يحدّث entitlement `storage.quotaMb` عبر `updateEntitlement` (يحوّل GB↔MB). الواجهة الخلفية كانت جاهزة؛ هذا يكشفها بصريًا.
- **مُتحقَّق حيًّا**: عرض 1/10/100GB للباقات الثلاث، وتغيير حصّة «المؤسسات» إلى 200GB ⇒ حُفِظ (`storage.quotaMb=204800`) وانعكس. أي مستأجر على الباقة يرث الحصّة الجديدة فورًا (D1 يقرؤها حيًّا).

## [المرحلة D1] — حصص التخزين لكل مستأجر (حجز ذرّي + تلميتري) ✅
- **نموذج `TenantStorage`** (عدّاد `usedBytes` BigInt + `fileCount`، معزول، migration `tenant_storage`) + entitlement **`storage.quotaMb`** لكل باقة (basic 1GB · premium 10GB · enterprise 100GB، قابل للتهيئة من سوبر أدمن المنصة).
- **حجز ذرّي** (`StorageUsageService.reserve`): زيادة `usedBytes` عبر **UPDATE واحد بحارس `WHERE usedBytes ≤ quota − size`** — يمنع تجاوز الحصّة حتى تحت التزامن (لا سباق check-then-write). تجاوز ⇒ `403`. مطابقة الحجم الفعلي بعد الرفع (`reconcile`)، وتحرير عند فشل الإنشاء. عند أوّل استخدام يُعبَّأ العدّاد من مجموع المستندات الحالية.
- **تلميتري** `GET /documents/usage` (المستخدَم/الحصّة/عدد الملفات/النسبة) + **شريط استهلاك** في صفحة `/tenant/settings/billing`.
- **مُتحقَّق حيًّا**: حجز 3 ملفات × 8MB ⇒ التلميتري 24MB/1024MB والشريط يعرضه؛ تجاوز الحصّة ⇒ 403.
- اختبار `storage-quota.e2e` (5 حالات: التلميتري/الحجز/التجاوز/العزل/ملف مفرد كبير). الإجمالي **e2e 140/140** (19 ملفًا).

## [تحسين] — رؤية سوبر أدمن المنصة لمالكي الشركات (سوبر أدمن الشركة) ✅
- **توضيح النموذج**: «مالك الحساب» = **سوبر أدمن الشركة** (صلاحيات مطلقة داخل مستأجره)؛ **سوبر أدمن المنصة** يدير المنصة ويطّلع على كل الشركات المسجّلة ومالكيها.
- **الخلفية** (`platform.service`): `tenants()` تُرجع الآن **مالك كل شركة** (أوّل مستخدم)، و`tenant(id)` يُرجع **كل حسابات الشركة بأدوارهم** + المالك.
- **الواجهة** (لوحة المنصة): عمود «مالك الحساب» في قائمة الشركات + اسم الشركة رابط لـ **صفحة تفاصيل جديدة** (`/admin/tenants/[id]`): المالك مبرَزًا (سوبر أدمن الشركة) + بيانات الحساب + الاشتراك + العدّادات + جدول حسابات الموظفين بأدوارهم.
- **مُتحقَّق حيًّا**: سوبر أدمن المنصة يرى الشركات الخمس (مبذورة + مسجّلة ذاتيًا) مع مالكيها، والتفاصيل تُبرز سوبر أدمن الشركة.
- اختبار `platform.e2e` (حالة جديدة: القائمة والتفاصيل تُظهران المالك بدور «مالك الحساب»). الإجمالي **e2e 135/135**.

## [المرحلة C1] — الهيكل الإداري والأقسام ✅
- **نموذج `Department`** هرمي (`parentId`) معزول بالمستأجر + حقل `User.departmentId` (migration `departments`). القسم قد يحمل **دورًا افتراضيًا** (`defaultRole`).
- **`/org/departments`**: شجرة (tree مع عدد الأعضاء واسم الدور) + create/update/delete + **assign** (إسناد موظف لقسم يُورِّثه دور القسم الافتراضي ما لم يُمرَّر دور صريح) + roles/members. **منع الدورات** في الهيكل، وعزل تام بين المستأجرين، والحذف يفصل الأعضاء/الأبناء (FK SET NULL). خلف `@Authorize(settings)`.
- **صفحة `/tenant/settings/org`**: مخطّط شجري (شارة الدور الافتراضي + عدد الأعضاء) + نموذج إضافة قسم (الأب/الدور) + عنصر تنقّل جديد. ثنائية اللغة.
- **مُتحقَّق حيًّا في المتصفح:** إنشاء «الإدارة العامة» ⇒ «قسم المبيعات» تحته بدور افتراضي «مالك الحساب» يظهر في الشجرة.
- اختبار `org.e2e` (6 حالات: هرمية/وراثة الدور/منع الدورات/العزل/الحذف/التحقّق). الإجمالي **e2e 134/134** (18 ملفًا).

## [المرحلة B2] — فوترة الاشتراكات (بوّابة الدفع) ✅
- **صفحة `/tenant/settings/billing` (الواجهة)**: تعرض الاشتراك الحالي (الحالة/الباقة/المقاعد/التجديد) + كتالوج الباقات (مبدّل شهري/سنوي) + زر اشتراك يحوّل لصفحة الدفع + جدول الفواتير. ثنائية اللغة، عنصر تنقّل جديد في الإعدادات. + صفحة العودة `/billing/return` تطابق الحالة بعد الدفع. (نقطة `GET /billing/plans`.)
- **مُتحقَّق حيًّا في المتصفح:** مالك TRIAL ⇒ اشتراك «الاحترافية» ⇒ دفع Sandbox ⇒ العودة «تم الدفع وتفعيل اشتراكك» ⇒ صفحة الفوترة تعرض **نشط** + الباقة الاحترافية + مقاعد 20 + فاتورة **PAID** (1,499 SAR).

### الواجهة الخلفية
- **تجريد `PaymentGateway`** (نقطة تبديل واحدة، نمط seam مثل ZATCA): محوّل **Tap.company** للإنتاج (Charges API: `POST /v2/charges/`، Bearer `sk_…`، استرجاع، تحقّق `hashstring` عبر HMAC-SHA256) + **`SandboxGateway`** للتطوير/الاختبار (بلا شبكة). يُختار من `BILLING_GATEWAY`.
- **نموذج `SubscriptionInvoice`** (معزول بالمستأجر، منفصل عن فواتير التأمين) + enum `BillingInvoiceStatus`. migration `subscription_billing`.
- **التدفّق**: `POST /billing/checkout` (يُنشئ فاتورة PENDING + شحنة ويعيد رابط الدفع) ⇒ `POST /billing/:id/confirm` (يطابق الحالة بعد العودة) أو **`POST /billing/webhook`** (عام، موقّع) ⇒ **تفعيل ذرّي** `TRIAL → ACTIVE` + ترقية الباقة + تمديد `renewsAt`. + `GET /billing/{invoices,subscription}`.
- **عزل وأمان**: التأكيد معزول بالمستأجر (فاتورة الغير ⇒ 404)؛ الـ webhook يجري بسياق فارغ مع `tenantId` صريح؛ التوقيع الخاطئ ⇒ 409؛ checkout/invoices خلف `@Authorize(settings)`.
- اختبار `billing.e2e` (7 حالات: PENDING/PAID/ترقية/عزل/webhook موقّع/توقيع خاطئ/401/422). الإجمالي **e2e 128/128** (17 ملفًا). متغيّرات `BILLING_*`/`TAP_*` في [`.env.example`](./.env.example).
- متبقٍّ: صفحة فوترة في الواجهة (checkout + الفواتير). **قرار المزوّد: Tap.company** (مُعتمد من المستخدم).

## [المرحلة B1] — التسجيل الذاتي وتزويد المستأجر ✅
- **صفحة `/signup` (الواجهة)**: نموذج تسجيل ثنائي اللغة (اسم الشركة/المدير/البريد/كلمة المرور/الباقة) يربط بـ `POST /signup`، يعرض أخطاء الخادم مباشرةً (البريد مستخدم… إلخ)، ويسجّل الدخول تلقائيًا للوحة المستأجر. روابط من اللاندينق بيج (CTA) ومن صفحة الدخول.
- **مُتحقَّق حيًّا في المتصفح:** تسجيل شركة جديدة ⇒ تزويد المستأجر ⇒ دخول تلقائي ⇒ لوحة المستأجر الجديد تعرض هويّة المالك وبيانات صفرية (مستأجر فارغ) دون أخطاء.

### الواجهة الخلفية
- **`POST /signup`** (عام): يُزوّد مستأجرًا جديدًا **ذرّيًا** (transaction) — Tenant (حالة `TRIAL`، تجربة 14 يومًا) + Subscription على الباقة (افتراضي `basic`) + TenantConfig (تفعيل كل خطوط المنتجات) + فرع رئيسي + **دور «مالك الحساب»** بصلاحيات كاملة على كل الموديولز + المدير + **شجرة حسابات قياسية** (مستوى 1/2 مقفل، حساب ضريبة 0203) + مركز تكلفة — ثم يُسجّل الدخول مباشرةً (JWT).
- **العزل مضمون:** التزويد يجري داخل **سياق طلب فارغ** (`RequestContextService.run({})`) فيتخطّى guard العزل ويمرّر `tenantId` صراحةً — المستأجر الجديد لا يرى بيانات غيره (مُتحقَّق).
- **التحقّق:** البريد فريد عالميًا (تكرار ⇒ `409`، لأن تسجيل الدخول يبحث بالبريد عبر المستأجرين)، سياسة كلمات المرور (`400`)، باقة مجهولة (`422`)، كبح إساءة التسجيل عبر `RateLimitService` (scope `signup`).
- اختبار `signup.e2e` (6 حالات: تزويد + وصول فوري + عزل + التكرار/الضعف/الباقة/الاسم). الإجمالي **e2e 121/121** (16 ملفًا).
- متبقٍّ: صفحة تسجيل في الواجهة (تربط بهذا الـ endpoint). توثيق: [`docs/29`](./docs/29-roadmap-next.md).

## [المرحلة A2] — تجهيز نشر الإنتاج (Coolify) ✅
- **`infra/docker/docker-compose.coolify.yml`**: حزمة إنتاج كاملة (API + Web + Postgres + Redis) بفحوص صحّة لكل خدمة، تبعيات `service_healthy`، وأسرار **إلزامية بصيغة `${VAR:?}`** تُفشل النشر فورًا إن نقصت (لا قيم افتراضية ضعيفة). تحقّقت صحّة الـ Compose + عمل حارس الأسرار.
- **`apps/api/docker-entrypoint.sh`**: يطبّق `prisma migrate deploy` تلقائيًا عند الإقلاع ثم يُقلع الـ API — قابل للتعطيل بـ `SKIP_MIGRATIONS=true` للنشر متعدّد النسخ. سكربت `@ibp/db` جديد `migrate:deploy:prod` (يقرأ `DATABASE_URL` من البيئة بلا dotenv) + `seed:prod`. `Dockerfile` يستخدم الآن `ENTRYPOINT`.
- **دليل `infra/docker/coolify.md`**: خطوات النشر + جدول متغيّرات البيئة + توليد الأسرار + ربط الدومينات/TLS + ملاحظة سيادة البيانات + الهجرات متعدّدة النسخ.
- فحص صحّي للحاويات عبر `node fetch` لـ `/health/live` (بلا تبعية curl). توثيق: [`infra/docker/README`](./infra/docker/README.md)، [`docs/29`](./docs/29-roadmap-next.md).

## [المرحلة A1] — سائق تخزين سحابي متوافق S3 (بلا تبعية SDK) ✅
- **سائق S3-متوافق** (`s3-driver.ts`) يعمل مع **AWS S3 / Cloudflare R2 / MinIO / Alibaba OSS** عبر واجهة واحدة — **توقيع AWS SigV4 يدويًا** (`node:crypto` HMAC) + `fetch` العام، **بلا إضافة أي تبعية** (التزامًا بثبات الحزمة التقنية).
- **روابط موقّتة مباشِرة للدلو** (`direct:true`): البايتات لا تمرّ بالـ API. رحلة الرفع: `upload-url` ⇒ رفع مباشر ⇒ **`POST /documents/:id/confirm`** (HEAD يثبّت الحجم/البصمة). محليًا يبقى الرفع عبر الـ API بلا تأكيد.
- `StorageService` صار واجهة (facade) تختار السائق من `STORAGE_DRIVER`؛ **السلوك المحلي مطابق تمامًا** (افتراضي). عمليات الخادم `put/get/head` تعمل للسحابي أيضًا (فواتير ZATCA).
- متغيّرات `STORAGE_*` (نفس الاصطلاح القائم) + `STORAGE_FORCE_PATH_STYLE`. توثيق: [`docs/21`](./docs/21-document-service.md) §2 + [`.env.example`](./.env.example).
- اختبار `storage-s3.e2e` (8 حالات): بنية SigV4 + حتمية التوقيع + اختيار السائق + خطأ الإقلاع — **كلّها دون شبكة**. الإجمالي **e2e 115/115** (15 ملفًا).

## [المرحلة G/P0] — تحصين أمني: صلابة الدخول ✅
- **قفل الحساب ضد القوّة الغاشمة** (`RateLimitService`، مدعوم بـ Redis، بلا تبعية جديدة): 8 محاولات فاشلة خلال 15 دقيقة ⇒ `429` مع `retryAfterSec`؛ **النجاح يُصفّر العدّاد** فلا يضرّ الاستخدام المشروع. مطبّق على بوّابات الدخول الثلاث: الموظف (`auth`)، المنصّة (`platform`)، العميل (`portal`).
- **سياسة كلمات المرور** عند إنشاء الموظفين: 8 أحرف على الأقل تشمل حرفاً كبيراً وصغيراً ورقماً (`create-staff.dto`).
- وحدة `SecurityModule` عامّة (`@Global`). اختبار `security.e2e` (4 حالات: القفل بعد 8 محاولات، عدم تأثّر الدخول الناجح، رفض الكلمة الضعيفة، قبول القوية).
- **متبقٍّ في P0:** MFA للأدمن. التفصيل في [`docs/30`](./docs/30-security-and-compliance.md) §5 و[`docs/29`](./docs/29-roadmap-next.md).
- **e2e 107/107** (14 ملفًا).

## [تصحيح محاسبي] — المعالجة الضريبية للعمولة في القيد (وفق المعيار السعودي) ✅
- **ضريبة مخرجات الوسيط على العمولة (15%) تُقيَّد الآن كالتزام** `0203 ضريبة القيمة المضافة المستحقة` في قيد JRV (كانت مفقودة من القيد رغم ظهورها على الفاتورة).
- القيد الجديد متوازن: مدين ذمم العملاء = أمانات (T−C−VATₒ) + عمولة C + ضريبة مستحقة VATₒ. أمانات المؤمِّن صارت صافية بعد العمولة وضريبتها.
- توضيح الفصل: **ضريبة القسط** مخرجات شركة التأمين (تُحوَّل أمانةً، على إشعار العميل) · **ضريبة العمولة** مخرجات الوسيط (تُورَّد لـ ZATCA، على فاتورة المؤمِّن).
- `summary` يكشف `outputVatPayable`. حساب COA جديد `0203`. اختبار `finance.e2e` يؤكّد قيد ضريبة العمولة. **e2e 103/103**. توثيق: [`docs/20`](./docs/20-issuance-and-finance-core.md) §4.

## [المرحلة 4ب+] — جاهزية ZATCA Fatoora المرحلة 2 (Integration) ✅
- **تهيئة معزولة لكل مستأجر** (`TenantZatcaConfig`): رقم ضريبي 15 رقماً، حالة تهيئة، مفاتيح/شهادات مشفّرة at-rest (AES-256-GCM)، عدّاد وتجزئة معزولان.
- **خطّ تهيئة 4 خطوات** (`/zatca/onboard/*`): توليد CSR (ECDSA secp256k1) ⇒ تبادل OTP/CCSID ⇒ فحوص الامتثال (3 مستندات UBL) ⇒ PCSID/ACTIVE — عبر `ZatcaGateway` (Sandbox، نقطة تبديل واحدة للإنتاج).
- **مستند فوترة موحّد** (`BillingDocument`): UUIDv4 + عدّاد/تسلسل/تجزئة SHA-256 معزولة بالمستأجر + QR (TLV وسوم 1–7) + UBL 2.1 (JSON) + كل حقول التعريب السعودي.
- **توجيه B2B/B2C**: مقاصة فورية (Clearance) للمنشآت · إبلاغ خلفي (Reporting، طابور Redis، نافذة 24س) للأفراد.
- **دمج ذرّي** في الاعتماد المالي (ISSUED + JRV + COA + مستندات ZATCA ثم التوجيه). تشفير at-rest عبر `CryptoVaultService` و`ZATCA_ENC_KEY`.
- اختبارات: e2e 103/103 (zatca: 8). توثيق: [`docs/28`](./docs/28-zatca-phase2-fatoora.md). الفرع `phase-4b-zatca-p2`.

## [الواجهة والتسويق] — لاندينق بيج + تحسينات تجربة ✅
- **لاندينق بيج** لدومين المنصة: تموضع كـ «نظام تشغيل وساطة التأمين / InsurTech SaaS»، مزايا، باقات، امتثال، CTA (ثنائي اللغة).
- نافذة تأكيد موحّدة قبل كل إجراء، تبويبات/بحث في العملاء، زر تسجيل الخروج، إعادة تنظيم القائمة الجانبية، زر «تحقّق الآن».

## [إثراء البيانات] — بيانات شبه واقعية عبر كل المنصّة ✅
- `seedRichData()`: توسيع البذرة لتملأ كل صفحة — **21 عميلاً · 25 وثيقة · 10 مطالبات** + طلبات بمراحل مختلفة، عروض أسعار (RFQ + Quotations)، خدمة عملاء، فحوص KYC/PEP، إشعارات مدينة وفواتير وعمولات، ملاحق، مستندات، ومستخدمي بوّابة.
- أسماء/شركات تأمين/مدن سعودية واقعية + أفراد ومنشآت عبر 14 فرع تأمين و16 شركة تأمين.
- **إضافة آمنة**: معرّفات جديدة لا تمسّ كيانات الاختبارات (`cl-fahd`/`cl2-nukhba`)؛ خُفّفت تأكيدات مجاميع العمولات في `reports.e2e` لتكون متينة أمام نموّ البيانات. **e2e 95/95** يبقى أخضر.
- دخول بوّابة إضافي: `portal@naseej.sa` · `portal@salamah-hospital.sa` · `portal@redsea-dev.sa` · `portal@rimal.sa`.

## [المرحلة 9] — التكاملات التنظيمية والإطلاق (Regulatory & Launch) ✅
- **ZATCA (Fatoora) المرحلة 1**: مولّد رمز QR بترميز TLV (الحقول الخمسة) + بصمة SHA-256 + UUID؛ يُرفق بكل فاتورة عبر `/finance/invoices`.
- **النواة المالية الحيّة**: `/finance/{summary,coa,invoices,receivables}` — الفصل الائتماني (أمانات خارج الميزانية)، شجرة الحسابات 17 رقماً، الذمم حسب العميل.
- **لوحة الالتزام**: موديول `compliance` (`/compliance/overview`) — حالة العملاء + توزيع مخاطر PEP/العقوبات (`module.compliance`).
- **طبقة التكاملات التنظيمية**: موديول `regulatory` (`/regulatory/status`) — 9 موصِّلات حكومية (Sandbox في التطوير) + صفحة الإعدادات ← التكاملات.
- **استكمال كل الصفحات**: بُنيت `finance`/`premiums`/`compliance`/`settings/integrations` وأُزيل `comingSoon` — لا صفحات ناقصة.
- **النشر داخل المملكة**: `infra/k8s` (نشر+HPA+TLS+تقوية) و`infra/terraform` (حياديّ سحابياً + حارس توطين داخل المملكة + تشفير).
- بذرة: عمليات تحقّق KYC/PEP (low/medium/high) + إضافة `module.compliance` لـ demo-tenant.
- اختبارات: e2e 95/95 (regulatory: 7). توثيق: [`docs/27`](./docs/27-regulatory-and-launch.md). الفرع `phase-9-regulatory`.

## [المرحلة 8ج] — التقارير والتحليلات (Reports & Analytics) ✅
- **ربط الواجهات ببيانات حيّة**: تحوّلت لوحة التحكّم وصفحة العمولات من `@/lib/mock` إلى مكوّنات عميل تجلب من `/reports/*`.
- وحدة `reports`: dashboard, commissions, production, claims, regulatory, catalog — تجميعات `aggregate`/`groupBy` مفلترة تلقائياً بالمستأجر.
- كيان `Commission` المبسّط ⇐ **قيد عمولة كامل** (وثيقة/شركة/عميل/نسبة/متوقّع/مستلم/حالة/شهر) + migration `commission_ledger`.
- **تفويض متدرّج**: اللوحة `dashboard:read` (للجميع)، العمولات `module.finance` (للجميع)، التحليلات وتقارير الهيئة `module.reports` (مدفوع) — basic بلا الإضافة يُرفض (403).
- صفحة `/tenant/reports` جديدة (إنتاج/مطالبات/هيئة التأمين + كتالوج الـ12) + تفعيل عنصر «التقارير» في التنقّل.
- بذرة عمولات + وثيقة مستحقّة للتجديد وطلب قيد المراجعة (إثراء مؤشّرات اللوحة).
- اختبارات: e2e 88/88 (reports: 9). توثيق: [`docs/26`](./docs/26-reports-and-analytics.md). الفرع `phase-8c-reports`.

## [المرحلة 8ب] — بوّابة العميل (Client Portal) ✅
- **النطاق الأمني الثالث `client`**: كيان `ClientUser` (tenantId + clientId) + JWT `scope:client` + `PortalGuard`.
- **عزل مزدوج**: فلترة Prisma بـ `tenantId` (الطبقة القائمة) + فلترة صريحة بـ `clientId` في `PortalService` ⇒ العميل يرى بياناته هو فقط.
- وحدة `portal`: login, me, policies, requests (تأمين+خدمة), claims, statement (إشعارات مدين+فواتير+رصيد), documents + رابط موقّت بفحص ملكية.
- واجهة `/[locale]/portal/*` بـ token مستقلّ (`cpapi`): dashboard/policies/requests/claims/statement/documents + `PortalShell` — RTL وثنائية اللغة (namespace `portal`).
- **بذرة تشغيلية كاملة**: وثائق/طلبات/خدمة/مطالبات/إشعارات مدينة/فواتير/مستندات + مستخدمو بوّابة (`portal@alfahd.sa`, `portal@nukhba.sa`).
- **قاعدة اختبار منفصلة `ibp_test`** (عبر `.env.test`) كي لا تلوّث الاختبارات بيانات العرض في `ibp_dev`.
- اختبارات: e2e 79/79 (portal: 11). توثيق: [`docs/25`](./docs/25-client-portal.md). الفرع `phase-8b-portal`.

## [المرحلة 8أ] — لوحة السوبر أدمن (Platform Super Admin) ✅
- **نطاق منصّة عابر للمستأجرين**: كيان `PlatformAdmin` (بلا `tenantId`) + JWT بنطاق `scope:platform` ⇒ استعلامات Prisma غير مفلترة + `PlatformGuard` مستقلّ.
- عزل ثنائي الاتجاه مُثبَت: مستخدم المستأجر يُرفض من `/platform` (403)، ومدير المنصّة يُرفض من مسارات المستأجر (403).
- خدمة `platform`: دخول، قائمة/تفاصيل المستأجرين، تعليق/تفعيل الاشتراك (مُدقَّق)، الاستخدام الكلّي، و**إدارة الباقات والـ entitlements**.
- **التحكّم في حدّ رفع الملفات (وغيره) من اللوحة** عبر upsert للـ entitlement — لا قيم ثابتة في الكود (تنفيذ قرار المستخدم).
- واجهة `/[locale]/admin/*` منفصلة بـ token مستقلّ (`papi`): دخول، استخدام، مستأجرون، باقات — RTL وثنائية اللغة (namespace `admin`).
- اختبارات: e2e 68/68. توثيق: [`docs/24`](./docs/24-platform-super-admin.md). الفرع `phase-8a-superadmin`.

## [المرحلة 7] — التحقّق الحكومي KYC/KYB ✅
- طبقة موفّري التحقّق (Sandbox): يقين (هوية)، واثق (سجل/شركاء/UBO)، العنوان الوطني (مجاني)، فحص PEP/العقوبات.
- **تعبئة ذكية** للنموذج من نتيجة السحب + **خصم العملية** من المحفظة (Reseller) وتسجيلها في `TransactionLedger` و`VerificationCheck` والتدقيق.
- تصنيف مخاطر آلي (low/medium/high) للالتزام. ربط التحقّق بالعميل/الطلب.
- واجهة: صفحة التحقّق (أرصدة العمليات + سحب تجريبي + سجل) + عنصر تنقّل «التحقّق».
- اختبارات: e2e 60/60. توثيق: [`docs/23`](./docs/23-government-verification.md). الفرع `phase-7-verification`.

## [المرحلة 6] — الموديولز التشغيلية (Operational) ✅
- **خدمة العملاء** (`ServiceRequest`): طلبات إضافة/حذف/تعديل/استفسار/تجديد بدورة حالات (RQ-).
- **المطالبات** (`Claim`): دورة كاملة استقبال ← رفع ← تسوية ← إغلاق (CL-) محكومة بـ entitlement.
- **التجديدات**: الوثائق المستحقّة خلال نافذة + بدء تجديد (ServiceRequest type=renewal).
- واجهات: صفحات service/claims/renewals + عنصر «خدمة العملاء» في التنقّل وتفعيل المطالبات.
- اختبارات: e2e 53/53. توثيق: [`docs/22`](./docs/22-operational-modules.md). الفرع `phase-6-operations`.

## [المرحلة 5] — وحدة المستندات (Document Service) ✅
- وحدة مستندات موحّدة (polymorphic): رفع/عرض عبر **روابط موقّتة فقط** (Presigned، 5 دقائق) — لا روابط عامة.
- خدمة تخزين حيادية المزوّد (`STORAGE_DRIVER`: local/s3/minio/alibaba_oss/gcs) + عزل بالمسار `tenant_{id}/`.
- حد الرفع كـ **entitlement** للباقة (`upload.maxFileMb`)، فحص MIME (رفض التنفيذي)، تمييز الرسمي/المرفق (WebP)، وتسجيل كل رابط في التدقيق.
- اختبارات: e2e 45/45. توثيق: [`docs/21`](./docs/21-document-service.md). الفرع `phase-5-documents`.

## [المرحلة 4ب] — الإصدار والنواة المالية (Finance Core) ✅
- كيان `Policy` محسّن (premium/vat/commission/status) + `PolicyStatus` + مولّد تسلسلي بالفرع `POL-…`.
- **شجرة الحسابات (COA) 17 رقماً** مزروعة لكل مستأجر: المستوى 1/2 مقفل + فصل داخل/خارج الميزانية + مراكز التكلفة.
- وحدة `production`: إصدار من طلب AWARDED + **موافقة فنية**. وحدة `finance`: **اعتماد مالي** يولّد آلياً **قيد JRV مزدوجاً متوازناً** + إشعار مدين + فاتورة ضريبية + يفتح حساب العميل التحليلي — ذرّياً.
- واجهة: صفحة الوثائق بشلال الاعتماد + زر «إصدار» للطلبات المُسنَدة.
- اختبارات: e2e 39/39. توثيق: [`docs/20`](./docs/20-issuance-and-finance-core.md). الفرع `phase-4b-finance`.

## [المرحلة 4أ] — الاكتتاب الفني وعروض الأسعار (RFQ) ✅
- كيانات `Slip` (RFQ)، `Quotation` (هجينة: حقول معيارية + نص حر)، `Endorsement`.
- وحدة `underwriting`: إنشاء Slip (بوّابة الالتزام ⇒ الطلب QUOTING)، إضافة عروض، **جدول مقارنة آلي** (يحدّد الأرخص)، **Firm Order** ⇒ الطلب AWARDED. حماية `module.production`.
- واجهة: منضدة `/tenant/slips/[id]` + زر RFQ في قائمة الطلبات.
- **تهيئة بنية المرحلة 4ب** (stubs): `ChartOfAccount` (17 رقماً، مستويات، On/Off‑Balance، قفل 1/2)، `CostCenter`، `Voucher` (JRV/PYV/RCV/DPV)، `Invoice` ضريبية، `DebitNote`/`CreditNote`.
- اختبارات: e2e 36/36. الفرع `phase-4a-underwriting`.

## [المرحلة 3] — العملاء + الكتالوج + النموذج الديناميكي ✅
- محرّك نموذج **مدفوع بمخطط** (DSL في `@ibp/shared`) + كتالوج متنوّع (7 فئات/15 فرعاً: طبي/مركبات/ممتلكات/هندسي/بحري/عام/حياة).
- استبدال الجداول الأربعة الثابتة بمخزن عام `RequestBlockRow`.
- `Client`: كود تجاري + تفرّد + `complianceStatus` + **بوّابة الالتزام**.
- وحدات `catalog`/`clients`/`requests` + محرّك تحقّق عام.

## [المرحلة 2] — الصلاحيات (RBAC + Entitlements) ✅
- حارس موحّد (`@Authorize` + `AuthorizationGuard`): فحص entitlement الباقة + صلاحية الدور.
- وحدة `staff` (إنشاء موظف بمصفوفة) + شاشة الموظفين.

## [المرحلة 1] — المصادقة وعزل المستأجرين ✅
- JWT + سياق المستأجر (AsyncLocalStorage) + **Prisma middleware يفرض tenantId** + سجل تدقيق.

## [المرحلة 0] — التهيئة ✅
- monorepo (web/api/db/shared/infra)، Prisma + seed، Docker Compose، `/health`.
- تصليب العزل: تحويل `tenantId` إلى مفاتيح أجنبية حقيقية على مستوى DB.

---
التفاصيل الكاملة في [`docs/`](./docs/README.md) و[`ROADMAP.md`](./ROADMAP.md).
