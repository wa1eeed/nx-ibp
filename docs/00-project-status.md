# 00 — حالة المشروع ونقطة الاستئناف (Project Status & Resume)

> **نقطة الدخول للاستئناف.** تلخّص أين وصلنا، وكيف نشغّل المنصّة، وما المتبقّي. حُدِّثت: 2026-07-05.

## 1. الحالة العامة
- **المنصّة مكتملة وظيفياً** (المراحل 0–9 + **ZATCA Fatoora المرحلة 2**)، ويجري تنفيذ **مسارات ما بعد الاكتمال** (تجاري/أمني/امتثال) بالتدريج حسب الأولوية.
- **منشورة على staging** 🚀 (2026-07-05): `https://ibp.nx.sa` (واجهة) + `https://api.ibp.nx.sa` (API) عبر Coolify — بيانات ديمو GIB، تكاملات Sandbox، تُحاكي الإنتاج. البيئات الثلاث: **dev** (جهازك) · **staging** (السيرفر الحالي) · **production** (لاحقًا، داخل المملكة) — التفصيل في [32](./32-environments.md).
- **الاختبارات:** **e2e 222/222** (34 ملفاً) على قاعدة اختبار منفصلة `ibp_test`.
- **المستودع:** `github.com/wa1eeed/nx-ibp` — فرع **`main`** مرفوع ومتزامن. (ملاحظة: الدفع يتطلّب `git config http.version HTTP/1.1` — علّة HTTP/2 curl، مضبوط محليًا.)
- **التموضع:** InsurTech SaaS · «نظام تشغيل وساطة التأمين» — مع لاندينق بيج تسويقي على جذر الدومين.
- **دستور المشروع:** [`GUIDELINES.md`](../GUIDELINES.md) (يُقرأ ويُلتزم في كل جلسة).

## 2. ما أُنجز — الأساس (المراحل 0–9 + ZATCA P2)
| المرحلة | المحتوى |
|---|---|
| 0–3 | الأساس + المصادقة/العزل (3 طبقات) + RBAC/Entitlements + العملاء/الكتالوج/النموذج الديناميكي |
| 4أ/4ب | الاكتتاب وعروض الأسعار (RFQ + مقارنة + Firm Order) · الإصدار والنواة المالية (قيد JRV مزدوج، COA 17 رقماً، فاتورة) |
| 5–7 | المستندات (روابط موقّتة) · خدمة/مطالبات/تجديدات · التحقّق الحكومي (يقين/واثق/نفاذ/PEP) |
| 8/9 | السوبر أدمن · بوّابة العميل · التقارير الحيّة · التكاملات التنظيمية (Sandbox) + بنية النشر داخل المملكة |
| 4ب+ | **ZATCA المرحلة 2** (تهيئة معزولة، CSR/CSID، عدّاد/تجزئة معزولة، توجيه B2B/B2C) + المعالجة الضريبية للعمولة |

## 3. ما أُنجز — ما بعد الاكتمال (هذه الجلسة)
| البند | المحتوى | التحقّق |
|---|---|---|
| **G/P0** | تحصين الدخول: قفل قوّة غاشمة (Redis) + سياسة كلمات + **MFA لسوبر أدمن المنصة** (TOTP) | ✅ `security`/`mfa` e2e |
| **A1/A2/A2+** | سائق تخزين S3-متوافق (SigV4 بلا SDK) · نشر Coolify (compose + هجرات تلقائية) · توثيق البيئات الثلاث | ✅ |
| **B1/B2** | تسجيل ذاتي + تزويد مستأجر آلي · فوترة الاشتراكات عبر **Tap.company** (سحابي seam) | ✅ حيًّا |
| **C1** | الهيكل الإداري والأقسام (هرمي + دور افتراضي) + صفحة الهيكل | ✅ حيًّا |
| **D1/D2** | حصص تخزين لكل حساب (حجز ذرّي + تلميتري) · ضغط الصور WebP (Sharp، محلي) | ✅ حيًّا |
| **E0** | إثراء حقول المؤمن له/الوثيقة وفق معايير الوساطة (VAT/الشكل القانوني/رقم وثيقة المؤمِّن/مبلغ التأمين…) | ✅ حيًّا |
| **G/SLA** | **سجل تدقيق ثابت (immutable)** + التقاط IP/الجهاز + مراجعة/تصدير للمفتّشين (NCA ECC) | ✅ `audit-immutable` e2e |
| **H** | **نظام الإشعارات لكامل النظام**: عملاء (Email/SMS) + موظفون (بريد، توجيه حسب الوحدة + المالك)، 21 نوعًا، مستويان، بوّابة Taqnyat/Resend + **مركز إشعارات داخل المنصة (جرس in-app)** للموظف والعميل | ✅ حيًّا |
| **E3** | ربط أحداث الإشعارات بدورة العمل: إصدار (`policy_issued`) · إشعار مدين (`debit_note`) · طلب خدمة (`request_ack`) · مطالبة (`claim_ack`) · تجديد (`renewal_reminder`) — كلها fire-and-forget | ✅ |
| **E1** | الضريبة حسب فرع التأمين: **تأمين الحياة معفى** (0%، فئة ZATCA "E"، `VATEX-SA-29-7`)؛ البقية 15% — يُفرَض عند الإصدار والاعتماد المالي | ✅ `vat-branch` e2e |
| **E2** | **سلاسل اعتماد ديناميكية مع حارس امتثالي**: بوّابة فنية تُفعَّل/تُعطَّل + خطوات إضافية (وحدة/فعل لكلٍّ) + **فصل مهام** (المعتمِد المالي ≠ المُصدِر، توصية رقابية) — صفحة `/tenant/settings/approval-chain` | ✅ `approval-chain` e2e + حيًّا |
| **E4** | **التراجع خطوة للوراء (Undo/Revert)**: صلاحية `canRevert` إشرافية + محرّك يُرجِع كيان آلة الحالة خطوة، مع **منع تراجع الوثيقة المُصدَرة** (يلزم إجراء تعويضي) — زر «تراجع خطوة» على الوثائق | ✅ `revert` e2e + حيًّا |
| **E5 (CRM + 360°)** | **إدارة العلاقات**: خطّ أنابيب صفقات (مراحل/إسناد/كسب-فقد + رؤية حسب الدور) · مهام/تذكيرات بإسناد يُطلق إشعارًا · نشاط. + **صفحة عميل 360°** (تابات/خط زمني) و**صفحة موظف 360°** (نشاطه من التدقيق) | ✅ `crm`/`detail-360` e2e + حيًّا |
| **RBAC nav** | القائمة الجانبية مُفلترة بصلاحيات المستخدم — كل موظف يرى أقسامه فقط | ✅ حيًّا |
| **مجدول التذكيرات** | **cron داخلي** (`@nestjs/schedule`، 8ص يوميًا) يمسح كل المستأجرين: مهام CRM المستحقّة ⇒ `staff_task_due` للمُسنَد إليه · وثائق تنتهي خلال 30 يومًا ⇒ `staff_renewal_due` + تذكير العميل — **بلا تكرار** (`reminderSentAt`/`renewalRemindedAt`) + تشغيل يدوي معزول `POST /reminders/run` | ✅ `crm` e2e + حيًّا |
| **MFA للموظفين** | **مصادقة ثنائية (TOTP)، مُطفأة افتراضيًا**: تسجيل ذاتي (`/auth/mfa/*`) + تحدّي دخول من خطوتين (`MFA_REQUIRED`) + **إلزام على مستوى الشركة** (`/config/security`) + **إعادة تعيين إدارية** (`/staff/:id/mfa/reset`، لفقدان الجهاز). صفحة `/tenant/settings/security` + شارة/زر على صفحة الموظف | ✅ `mfa-staff` e2e + حيًّا |
| **الاحتفاظ/الإتلاف + DLP** | **DLP**: الهوية/الآيبان تُخفى لغير الالتزام/المالية (أقلّ امتياز) · **حق المحو (PDPL)**: `POST /clients/:id/erase` يُخفي كل PII ويُبقي الهيكل المالي + سجلّ إتلاف ثابت · **سياسة احتفاظ** (`/config/retention`، افتراضي 10 سنوات) + تقرير استحقاق للإتلاف. زر محو على صفحة العميل + إعداد على صفحة الأمان | ✅ `retention-dlp` e2e + حيًّا |
| **حدّ مقاعد الباقة** | سوبر أدمن المنصّة يضبط **حدّ المستخدمين لكل باقة** (`PUT /platform/plans/:code`، محرّر في `/admin/plans`)؛ يُفرَض على الشركة — إنشاء موظف يُرفَض 403 عند الحدّ (`GET /staff/seats` + شارة على صفحة الموظفين) | ✅ `plan-seats` e2e + حيًّا |
| **نموذج الطلب** | حقل عميل **قابل للبحث** (اسم/هوية/سجل تجاري) + **إضافة عميل سريعة** (بوب-أب) وحالة فارغة تُلزم بها قبل الطلب | ✅ حيًّا |
| **الاكتتاب الفني** | إثراء مالي (مبلغ التأمين/الرسوم/العمولة + مقارنة ديناميكية + زر احتساب) و**الضريبتان معًا** باحتساب تلقائي 15%: ضريبة القسط (الحياة معفاة) + **ضريبة عمولة الوساطة** (دائمًا) | ✅ `underwriting` e2e + حيًّا |
| **إصلاح جلسة أمني** | انتهاء التوكن ⇒ مسح + إعادة توجيه للدخول حسب النطاق (لا مستخدم افتراضي ظاهر) + جلسة 8س | ✅ حيًّا (staging) |
| **تفاصيل الوثيقة 360°** | صفحة `/tenant/policies/[id]` (تبويبات: مالية/ملحقات/مطالبات/فواتير/مستندات/زمني) + `GET /policies/:id/overview` · لوحة تجديدات بنافذة/مؤشّرات/إلحاح | ✅ حيًّا |
| **بوّابة العميل — خدمة ذاتية** | العميل يقدّم **مطالبة/طلب خدمة/تجديد** (بحارس ملكية + إشعار الفريق) + **صفحة تفاصيل وثيقة** + عدّادات (قرب الانتهاء + إجمالي مبالغ التأمين) | ✅ `portal` e2e (+6) + حيًّا |
| **التجديد بمعايير الوساطة** | «بدء التجديد» يُنشئ **طلب تأمين** مبنيًا على الوثيقة (`renewedFromPolicyId`) يدخل دورة RFQ→إصدار (لا تذكرة)، بلا تذكير تلقائي للعميل، ومنع تكرار (409). زر التجديد في البوّابة **مقيّد بنافذة 60 يوماً** | ✅ `operations` e2e (+1) + حيًّا |
| **الهيكل الإداري — تابز** | تبويبان: **مخطط هرمي شجري** (بطاقات + موصلات RTL) + الإدارة (إضافة/حذف) | ✅ حيًّا |
| **المالية — دورة التحصيل** | **سند قبض من العميل (RCV)** مقابل إشعار المدين (`POST /finance/debit-notes/:id/receipt`، منع تجاوز 409) + **استلام عمولة** + **ذمم حيّة** (المتبقّي بعد التحصيل + «المُحصَّل») + **كشف حساب العميل** (`/finance/statement/:id`). سدّ أكبر فجوة مقابل أويسس | ✅ `finance` e2e (+2) + حيًّا |
| **المالية — الإلغاء والإشعارات الدائنة** | **إلغاء وثيقة** (`POST /finance/policies/:id/cancel`) بقسط مُرتجَع **نسبةً وتناسبًا** + **إشعار دائن (CNP)** + قيد عكسي + `CANCELLED`؛ الإصدار صار يضبط **فترة التغطية** | ✅ `finance` e2e (+1) + حيًّا |
| **المالية — تسوية المؤمِّن + ميزان المراجعة** | **المستحقّ للمؤمِّنين** (`/finance/payables`، أعمار دَين) + **سند صرف PYV** (`/finance/insurers/settle`، تجاوز 409) + **ميزان مراجعة** متوازن (`/finance/trial-balance`) + JRV لكل وثيقة في البذرة | ✅ `finance` e2e (+1) + حيًّا |

> تحسينات تجربة المستخدم: مصطلح **«الحسابات» (Accounts)** بدل «المستأجر» في لوحة المنصة · ترجمة نموذج التحقق (عبر المنصة/عبر حسابات الشركة) · تحييد أي علامة تجارية خارجية (نُقلت مواصفة المتطلبات إلى [`docs/broker-requirements-coverage.md`](./broker-requirements-coverage.md)).

## 4. الواجهات الأربع + حسابات العرض (كلمة المرور: `Passw0rd!`)
| الواجهة | المسار | حسابات |
|---|---|---|
| اللاندينق | `/` | عامّة (تسويقية) — CTA يقود إلى `/signup` |
| الموظف/الشركة | `/login` | **🏢 حساب العرض للعميل الأول — Gulf Insurance Brokers Co.:** `AAlanazi@gib-sa.com` (المالك عبدالحميد العنزي) + `pricing/finance/claims/compliance/care@gib-sa.com` — ببيانات محاكية للواقع (10 عملاء · 14 وثيقة · مطالبات/تحقّق/عمولات) · حسابات أخرى: `waleed@gulf-demo.sa` · omar@aman-demo.sa · **أو تسجيل ذاتي عبر `/signup`** |
| السوبر أدمن (المنصة) | `/admin/login` | `admin@ibp-platform.sa` (يدعم MFA من `/admin/security`) |
| بوّابة العميل | `/portal/login` | **GIB:** `portal@alnoor-medical.sa` · `portal@gulf-maaden.sa` · `portal@alsafwa-mall.sa` · أخرى: `portal@alfahd.sa` |

## 5. التشغيل السريع (Resume)
```bash
# 1) البنية التحتية (Docker Desktop) — Postgres 5434 · Redis 6381 محليًا
open -a Docker && docker start ibp-postgres ibp-redis

# 2) (مرّة) القاعدة + البذرة — dev و test
pnpm --filter @ibp/db migrate:deploy && pnpm --filter @ibp/db seed
pnpm --filter @ibp/db test:setup          # قاعدة الاختبار ibp_test

# 3) التشغيل
pnpm --filter @ibp/api dev                 # nest --watch على http://localhost:4000
pnpm --filter @ibp/web dev                 # http://localhost:3000

# 4) الاختبارات
pnpm --filter @ibp/api test:e2e            # 222/222
```
> **إن ظهر 404 لمسار API جديد:** عملية الـ watch قديمة — اقتل عمليات NX-IBP فقط (ليس مشاريع أخرى) وأعد `pnpm --filter @ibp/api dev`.

## 6. ما المتبقّي (مرتّب بالأولوية)
التفصيل الكامل والحالة في **[29 — خطّة العمل القادمة](./29-roadmap-next.md)**، ومصفوفة الامتثال في **[30 §5.5](./30-security-and-compliance.md)**. أبرزها:
- **قابل للبرمجة:** مكتبة نماذج البوابة (المتبقّي الأخير). *(E1/E2/E3/E4/E5-CRM+360°/الإشعارات/فلترة القائمة/**توسعة الكتالوج**/**مجدول التذكيرات**/**MFA للموظفين**/**الاحتفاظ+الإتلاف+DLP** — كلها منجزة.)*
- **بنية/عمليات (تلزم موارد خارجية):** الاستضافة داخل المملكة (CST فئة C) · RPO/RTO الفعلي + DR · اختبار اختراق (جهة NCA) · KMS/Vault · WAF · SCA/SAST · ZATCA إنتاجي + PDF/A-3 · مفاتيح إنتاجية (Taqnyat/Resend/Tap/يقين-نفاذ).
- **الإطلاق:** بذرة إقلاع الإنتاج جاهزة (`SEED_MODE=production` = مرجعيات + سوبر أدمن فقط، بلا مستأجرين وهميين — [33 §د](./33-launch-runbook.md)). حساب **Gulf Insurance Brokers** الإنتاجي الحقيقي يُنشأ عبر `/signup`/التزويد عند تجهيز السيرفر والمفاتيح (الديمو مبذور في dev / `seed:demo`).

## 7. خريطة التوثيق
- المعمار والأمان: [02](./02-architecture.md) · [04](./04-security-and-multitenancy.md) · **[30 — الأمن السيبراني والامتثال](./30-security-and-compliance.md)** (NCA ECC/CCC + SLA/الإسناد §5.5)
- البيانات: [03](./03-data-model.md) · API: [06](./06-api-reference.md) · الوحدات: [07](./07-backend-modules.md)
- دورة الصفقة: [08](./08-deal-lifecycle-workflows.md) · المالية: [20](./20-issuance-and-finance-core.md) · المستندات: [21](./21-document-service.md) · ZATCA P2: [28](./28-zatca-phase2-fatoora.md)
- التشغيل: [13](./13-local-setup-and-operations.md) · الاختبارات: [16](./16-testing.md) · **البيئات الثلاث: [32](./32-environments.md)** · **دليل الإطلاق: [33](./33-launch-runbook.md)** · متطلبات الوساطة: [broker-requirements-coverage](./broker-requirements-coverage.md)
- **طبقة ما بعد الاكتمال (الإشعارات/E1–E5/CRM/360°): [34](./34-post-completion-features.md)** · كتالوج المزايا: [31](./31-feature-catalog.md)
- الفهرس الكامل: [README](./README.md) · سجل التغييرات: [CHANGELOG](../CHANGELOG.md) · خطّة العمل: [29](./29-roadmap-next.md)

## 8. ملاحظات تشغيلية مهمّة
- **لا تشغّل `pnpm build` (إنتاج) وخادم التطوير على نفس `.next`** — يسبب `Cannot find module './vendor-chunks/...'`. الحل: `rm -rf apps/web/.next` وأعد التشغيل ([13 §7](./13-local-setup-and-operations.md)).
- Docker Desktop قد يتوقّف بين الجلسات — أعد تشغيله قبل أي عمل بالقاعدة.
- الاختبارات تستخدم `ibp_test` (عبر `.env.test`) ولا تلوّث `ibp_dev`.
- الدفع للريموت: عند ظهور `RPC failed HTTP 400` اضبط `git config http.version HTTP/1.1` (مضبوط محليًا).
