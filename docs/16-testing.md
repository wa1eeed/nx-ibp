# 16 — الاختبارات (Testing)

> اختبارات IBP تركّز على المنطق الحرج: **عزل المستأجرين، الصلاحيات، الحسابات، والحوكمة** (GUIDELINES.md §4/§7). الطبقة الحالية: اختبارات تكامل e2e للـ API بـ **Jest + Supertest** ضد قاعدة مزروعة.

## جدول المحتويات
- [1. الإعداد](#1-الإعداد)
- [2. ملفات الاختبار والتغطية](#2-ملفات-الاختبار-والتغطية)
- [3. التشغيل](#3-التشغيل)
- [4. الإستراتيجية](#4-الإستراتيجية)

## 1. الإعداد

[`apps/api/test`](../apps/api/test):
- [`jest-e2e.json`](../apps/api/test/jest-e2e.json): إعداد Jest (ts-jest، `testRegex: .e2e-spec.ts$`، `testTimeout: 30000`).
- [`setup-e2e.ts`](../apps/api/test/setup-e2e.ts): يحمّل `.env` الجذر قبل تحميل وحدات التطبيق (JwtModule يقرأ `JWT_SECRET` عند الاستيراد).
- كل ملف يُنشئ التطبيق في الذاكرة عبر `Test.createTestingModule({ imports: [AppModule] })` ويختبره بـ Supertest (HTTP حقيقي عبر خادم Nest).

## 2. ملفات الاختبار والتغطية

| الملف | عدد | يغطّي |
|---|---|---|
| [`isolation.e2e-spec.ts`](../apps/api/test/isolation.e2e-spec.ts) | 8 | المصادقة (401)، كلمة مرور خاطئة (401)، **عزل المستأجرين** (كل مستأجر يرى عملاءه فقط، الوصول العابر بالمعرّف 404)، `/auth/me` |
| [`rbac.e2e-spec.ts`](../apps/api/test/rbac.e2e-spec.ts) | 10 | **بوّابة الباقة** (موديول خارج الباقة 403)، **بوّابة الدور** (موظف بلا صلاحية 403)، إدارة الموظفين، إنشاء موظف بمصفوفة تُطبَّق فعلاً |
| [`requests.e2e-spec.ts`](../apps/api/test/requests.e2e-spec.ts) | 13 | الكتالوج المتنوّع، **التحقّق حسب المنتج** (طبي/مركبات/حياة → 201، حمولة ناقصة → 422)، **بوّابة الالتزام** (قبل الاعتماد 409)، تفاصيل الطلب بكتله |
| [`underwriting.e2e-spec.ts`](../apps/api/test/underwriting.e2e-spec.ts) | 6 | حوكمة الالتزام على الـ Slip (409)، **فصل الاكتتاب عن الإصدار** (المبيعات + دور «إنتاج فقط» ممنوعان من Slip ⇒ 403)، **جدول المقارنة الآلي**، **Firm Order** ⇒ AWARDED، العزل |
| [`finance.e2e-spec.ts`](../apps/api/test/finance.e2e-spec.ts) | 21 | إصدار/اعتماد مالي (**قيد JRV متوازن** + فاتورة)، **دورة التحصيل** (قبض/جزئي/409/كشف)، **الإلغاء** (مُرتجَع + إشعار دائن)، **تسوية المؤمِّن** (PYV + ميزان مراجعة)، **القيود اليدوية** (توازن/ورقة/RBAC)، **عمولات الموظفين** (متوقّعة/مستحقّة/صرف)، **التقسيط** (تقسيم=الإجمالي · شلال · حواجز)، **الميزانية العمومية** (أصول=خصوم+حقوق)، **دفتر الأستاذ** (رصيد جارٍ=الميزان · 404)، **إقرار ض.ق.م** (مخرجات−مدخلات)، **أعمار الذمم** (شرائح حسب العمر)، **بيان التدفّق النقدي** (تشغيلي/تمويلي · تسوية)، **مستند السند المطبوع** (RCV/PYV + توازن الأطراف · 404)، **صرف مرتجع العميل** (PYV مدين ذمم/دائن نقد · مُعلَّم مصروف · تكرار 409 · RBAC) |
| [`documents.e2e-spec.ts`](../apps/api/test/documents.e2e-spec.ts) | 6 | **روابط موقّتة فقط** (Presigned)، حدّ الرفع كـ entitlement، فحص MIME (رفض التنفيذي)، عزل المسار `tenant_{id}/` |
| [`operations.e2e-spec.ts`](../apps/api/test/operations.e2e-spec.ts) | 9 | خدمة العملاء (دورة `RQ-`)، **المطالبات** (`CL-` محكومة بـ entitlement)، التجديدات، **بدء دورة تجديد** (⇒ 201 طلب تأمين DRAFT + منع تكرار 409)، RBAC والعزل |
| [`verification.e2e-spec.ts`](../apps/api/test/verification.e2e-spec.ts) | 7 | يقين يعبّئ النموذج و**يخصم عملية**، واثق (UBO)، العنوان مجاني، فحص PEP (low/high)، منع RBAC والعزل |
| [`platform.e2e-spec.ts`](../apps/api/test/platform.e2e-spec.ts) | 10 | دخول السوبر أدمن، **رؤية كل المستأجرين عابرةً للعزل**، رفض المستأجر من `/platform` (403)، الاستخدام، ضبط entitlement، تعليق/تفعيل، تعطيل `feature.zatca` ⇒ 403 ثم استعادته، **وطلبات التواصل (Leads): رؤية/تحديث الحالة + الإرسال المتكرّر لا يُقفَل + عزل 403 + حالة خاطئة 400** |
| [`portal.e2e-spec.ts`](../apps/api/test/portal.e2e-spec.ts) | 23 | دخول العميل، الملف/الوثائق/المطالبات/كشف الحساب/المستندات، **العزل المزدوج**، **الخدمة الذاتية** (مطالبة/طلب خدمة/تجديد + محادثة + تحديث تواصل)، **توفير الدخول** (دعوة ⇒ تفعيل ذاتي ⇒ دخول تلقائي · توكن فاسد 401)، **الدفع الإلكتروني** (دفع ⇒ سند قبض تلقائي · webhook موقّع · idempotent) |
| [`reports.e2e-spec.ts`](../apps/api/test/reports.e2e-spec.ts) | 9 | لوحة/عمولات/إنتاج/مطالبات/هيئة التأمين بأرقام حقيقية، **التفويض المتدرّج** (basic ⇒ 403 من التحليلات، 200 من اللوحة)، العزل |
| [`regulatory.e2e-spec.ts`](../apps/api/test/regulatory.e2e-spec.ts) | 7 | **ZATCA** (فكّ TLV للحقول الخمسة)، الملخّص المالي/الأمانات، شجرة الحسابات، الذمم، لوحة الالتزام (`module.compliance` ⇒ 403)، حالة التكاملات (Sandbox) |
| [`zatca.e2e-spec.ts`](../apps/api/test/zatca.e2e-spec.ts) | 8 | **ZATCA المرحلة 2**: عزل التهيئة وسلسلة التجزئة، 422 لرقم ضريبي خاطئ، خطّ التهيئة (CSR⇒OTP⇒Compliance⇒ACTIVE)، توليد المستندات (UUID/عدّاد/تجزئة/QR)، التوجيه B2B/B2C |

**ملفات ما بعد الاكتمال** (هذه الجلسة): `security` · `mfa` · `signup` · `billing` · `org` · `storage-s3` · `storage-quota` · `image-compression` · `broker-fields` · `audit-immutable` · `notifications` — تغطّي التحصين الأمني (قفل/MFA)، التسجيل الذاتي، الفوترة (Tap seam)، الأقسام، التخزين السحابي/الحصص/الضغط، إثراء الحقول، **سجل التدقيق الثابت + IP**، ونظام الإشعارات.

**+ ملفات ما بعد الاكتمال:** `security` · `mfa` · `audit-immutable` · `signup` · `billing` · `org` · `storage-quota` · `storage-s3` · `image-compression` · `broker-fields` · `notifications` · `notification-gateway` · `staff-notifications` (توجيه + in-app) · `vat-branch` (E1) · `approval-chain` (E2 + فصل مهام) · `revert` (E4) · `crm` (8: أنابيب/مهام/نشاط/رؤية حسب الدور/لوحة متابعة/**تحويل الفرصة لطلب**/**مجدول التذكيرات: مهمة مستحقّة ⇒ تذكير بلا تكرار**/عزل التشغيل) · `detail-360` (نظرة العميل/الموظف + عزل) · `mfa-staff` (7: إعداد/تفعيل TOTP · تحدّي الدخول من خطوتين · إلزام الشركة + منع الإلغاء · **إعادة تعيين إدارية** · عزل صلاحية) · `retention-dlp` (7: إخفاء الهوية/الآيبان حسب الصلاحية · **تشفير الآيبان at-rest** (AES-256-GCM — إثبات التخزين المشفّر عبر SQL خام + قابلية الفكّ) · محو PDPL + عدم تكرار · سجلّ الإتلاف · عزل 403 · حدود مدّة الاحتفاظ · تقرير الاستحقاق) · `plan-seats` (5: سوبر أدمن يعدّل السعر لكل مستخدم ⇒ الكتالوج العام · **الرخصة = المقاعد المختارة عند التسجيل** · **إضافة ضمن الرخصة تنجح وتجاوزها ⇒ 402 (`SEAT_LIMIT_REACHED`)** · **شراء مقاعد يرفع الرخصة فيُسمح بالإضافة** · عزل نطاق المنصّة) · `roles` (7: محرّر RBAC — سرد/إنشاء/تعديل مصفوفة/حذف · اسم مكرّر 409 · حذف مُعدّ مسبقًا 409 · **القفل الذاتي عن الإعدادات 400** · إسناد + منع حذف مُسنَد 409 · مستخدم بلا settings ⇒ 403).

**+ تفضيلات إشعارات لكل دور (§9.1):** `notifications` (+5: مصفوفة **أدوار × أنواع إشعارات الموظفين** (21 نوعًا، `module` لكل نوع، بلا مكتوم ابتداءً) · كتم نوع لدور ثم إعادة تفعيله · **التوجيه يحترم الكتم** (دور مكتوم لا يستلم `staff_member_added`، وغير مكتوم يستلم — مُثبَت عبر مركز in-app) · نوع عميل/مجهول أو دور مجهول ⇒ 400 · عزل الكتم بين الشركات).

**+ فصل المهام على البوّابة الفنية (§9.2):** `approval-chain` (+2: عند تفعيل `technicalSegregation` ⇒ مُصدِر الوثيقة يُمنع من اعتمادها فنيًا (403) بينما معتمِد فنّي مختلف ينجح ⇒ للاعتماد المالي ⇒ ISSUED · **افتراضيًا مُعطَّل** ⇒ المُصدِر يعتمد وثيقته فنيًا (توافق رجعي)).

**+ الموازنة التقديرية مقابل الفعلي (§1.8):** `budget` (7: ضبط موازنة لحساب + عرضها · **الموازنة مقابل الفعلي** (فعلي صفر لسنة مستقبلية ⇒ انحراف = −الموازنة، نسبة −100%) · **upsert** بلا تكرار · تحقّق (فترة/حساب مجهول ⇒ 400) · حذف · عزل بين الشركات · صلاحية المالية 403).

**+ التقارير المجدولة (§7.3):** `report-schedules` (7: إنشاء جدول + عرضه (موعد قادم مستقبلي) · **«إرسال الآن»** يرسل لكل مستلم (Sandbox ⇒ ok) ويضبط `lastSentAt` ويقدّم الموعد · المسح (`/reminders/run`) يُرجع عدّاد `reports` والجدول المستقبلي لا يُوزَّع · تحقّق (تقرير/دورية/بريد ⇒ 400) · تعديل + حذف · عزل · صلاحية `reports` 403).

**+ تحقّق المعمار والأمن ([36](./36-architecture-and-security-compliance.md)):** `security-arch` (2: **Carrier Webhook** — توقيع HMAC صحيح ⇒ 200 · خاطئ/غائب ⇒ 401 · **إثراء سجل التدقيق** — الدور + معرّف الجلسة + لقطتا الحالة `PENDING⇒APPROVED` تُلتقط فعليًا). *(التشفير at-rest للجوال والآيبان في `retention-dlp`.)*

**+ جاهزية تكاملات التحقّق (§9.3):** `regulatory` (+1: لوحة `/regulatory/status` تعكس **وضع البوّابة** (`gatewayMode`) و**جاهزية كل موصِّل** (`configured` بولياني + `environment` live/sandbox) — بلا مفاتيح ⇒ `sandbox`/غير مُهيّأ، و`summary.live=0`). بوّابة التحقّق (`VerificationGateway`) Sandbox افتراضيًا، والاختبارات القائمة (`verification`) تمرّ بلا تغيير على بياناتها التجريبية.

**+ حق العدول (§6.4):** `free-look` (4: الافتراضي 0=مُعطَّل · ضبط المدّة + حدّ >90 ⇒ 400 · **مُفعَّل** ⇒ الوثيقة تحمل نافذة عدول مستقبلية، والإلغاء ضمنها ⇒ **استرداد كامل** (`freeLook:true`، القسط 60000 كاملاً) · **مُعطَّل** ⇒ لا نافذة والإلغاء نسبةً وتناسبًا). أُصلح ضمنًا **فتح الحساب التحليلي للعميل بالتماس الأب بالكود** (كان يفشل على شركات التسجيل الذاتي — FK على `parentId`).

**+ تعدّد العملات (§9.4):** `multi-currency` (3: وثيقة بالريال ⇒ إشعار مدين 69000 (بلا تحويل) · **وثيقة بالدولار (سعر 3.75) ⇒ الدفاتر بالريال: إشعار مدين 258750** والقائمة تُظهر عملة USD · عملة أجنبية بلا سعر صرف ⇒ الإصدار 409).

**+ الرواتب (§8.1):** `payroll` (5: إنشاء كشف ⇒ تعبئة آلية بالموظفين + تعديل بند ⇒ صافي صحيح (10000+2000−500=11500) · **الترحيل ⇒ سند مصروف بصافي الكشف، ويظهر مدينًا في حساب الرواتب 05030** وميزان المراجعة متوازن · حواجز: إعادة ترحيل/تعديل مُرحَّل/تكرار فترة 409 · فترة غير صالحة/صافي صفر 400 · عزل · صلاحية المالية 403).

**+ طلبات الإجازات (§8.2):** `leave` (5: تقديم ⇒ أيام تشمل الطرفين + طلباتي/القائمة · **فصل مهام** الموظف بلا `settings` لا يرى القائمة ولا يبتّ (403) ومديرٌ يوافق ثم إعادة البتّ 409 · **المالك لا يبتّ طلب إجازته** (403) · تحقّق نوع/تواريخ 400 · عزل بين الشركات).

**+ تدوير الجلسة (Track G):** `auth-refresh` (4: الدخول يُصدر رمز وصول + **رمز تحديث** · **التدوير** — تحديث ⇒ وصول+تحديث جديدان والقديم يُبطَل (إعادة استخدامه 401) والرمز الجديد يعمل على `/auth/me` · **الخروج** يُبطِل الرمز · مجهول ⇒ 401 وأقصر من الحدّ ⇒ 400).

**+ سجلّ الوسطاء الفرعيين:** `producers` (5: منع غير المالية من الإنشاء · إنشاء برمز PRD- · **ربط وثيقة بوسيط فرعي ⇒ احتساب حصّته آليًا بنسبته** · تسوية PYV تُنقص المتبقّي/تمنع التجاوز 409/المكتتب 403 · العزل بين المستأجرين).

**+ مكتبة قوالب النماذج:** `form-templates` (6: منع غير المبيعات · رفض خطّ منتج مجهول 400 · إنشاء + سرد حسب الخطّ · **تطبيق يعيد التعبئة ويزيد عدّاد الاستخدام** · تعديل+حذف · العزل بين المستأجرين).

**+ الباقات والتسجيل:** `signup` (كتالوج عام `/signup/plans` بسعر/مستخدم + توفير · onboarding: تحقّق الرقم الموحد 10/الجوال 05 · التسعير لكل مستخدم؛ المقاعد المختارة = الرخصة) · `plan-seats` (السوبر أدمن يعدّل السعر/التجربة ⇒ الكتالوج العام · **رخصة المقاعد مسبقة الدفع: تجاوز الرخصة ⇒ 402 · شراء مقاعد يرفعها**).

**+ البريد والهوية البصرية:** `email-branding` (19: الهوية الافتراضية/حفظ لون واسم/لون غير صالح 400/**رفع شعار ⇒ رابط عام يخدمه بلا مصادقة**/عزل الهوية/صلاحية 403 · البريد unconfigured⇒fallback/**وضع الردود فقط بلا مفتاح**/حفظ⇒pending مع **إخفاء المفتاح**/تحقّق⇒**ترقية آلية لوضع tenant** · **بوّابة الدفع (BYO Tap/Moyasar)**: افتراضي none/حفظ Tap ⇒ **مفتاح masked لا خام**/تفعيل بلا مفتاح 400/عزل + صلاحية 403).

**+ الأهداف والأداء:** `targets` (5: خيارات الإنشاء · **إنشاء هدف فرع تأمين ⇒ احتساب الفعلي و% الإنجاز من الوثائق المُصدَرة** · فلترة الفترة · قيمة غير موجبة 400 · عزل/صلاحية 403). و`finance` (+1: **وثيقة الفاتورة المطبوعة** — صافي+ضريبة=الإجمالي · بائع/طرف/ZATCA · مجهولة 404).

**+ سجل التدقيق:** `audit` (5: **تحويل معرّف المنفّذ إلى اسمه فعليًّا** · فلترة بالفعل · صلاحية الالتزام 403 · عزل بين المستأجرين · عرض المنصّة عابر الحسابات بالأسماء + منع المستأجر 403).

**+ شركات التأمين:** `insurers` (5: **إنشاء بإحصاءات إنتاج + نِسبة/تسوية/بنك** · نسبة عمولة>100 ⇒ 400 · تعديل/حذف · عزل بين المستأجرين · صلاحية المالية 403).

**+ سجلّ الشكاوى:** `complaints` (3: دورة كاملة تسجيل⇒SLA⇒ملاحظة⇒تصعيد⇒معالجة + التقرير التنظيمي + فئة غير معروفة 400 · عزل بين المستأجرين + مجهولة 404 · RBAC بلا `compliance` ⇒ 403).

**+ كشف المؤمِّن الدوري:** `reports` (+4: الصافي للمؤمِّن = الإجمالي − العمولة لكل صفّ + المجاميع = مجموع الصفوف · التصفية بمؤمِّن تُرجع صفوفه فقط · فترة مستقبلية ⇒ صفر · عزل بين المستأجرين). **+ متانة تجديد** (`operations` صار حتميًّا عبر إعادات التشغيل — يتخطّى الوثائق ذات طلب تجديد قائم؛ والبذرة تمسح تجديدات الاختبار في قاعدة الاختبار فقط).

**+ تصدير التقارير CSV:** `reports` (+2: تصدير كشف المؤمِّن ⇒ `text/csv` + BOM + ترويسة أعمدة + صفّ إجمالي + `Content-Disposition` · تصدير العمولات · مفتاح غير معروف ⇒ 400).

**+ بيان التدفّق النقدي:** `finance` (+1: تصنيف تشغيلي/تمويلي حسب الطرف المقابل — تحصيل − رواتب = تشغيلي · ضخّ رأس مال = تمويلي · صافي التغيّر = مجموع الأنشطة · الختامي = افتتاحي + التغيّر · تسوية `reconciles` · فترة ماضية أصفار · RBAC).

**+ مكافحة غسل الأموال (AML):** `aml` (6: تقييم مخاطر بعوامل موزونة — PEP+دولة=50 متوسّط · عقوبات ⇒ مرتفع حتمًا · بصمة على العميل · الفرز نظيف/تطابق مؤكّد (OFAC) + تصرّف · STR مؤشّر مجهول 400 + مسودّة⇒رفع⇒إغلاق · النظرة/التقرير · RBAC 403 · العزل).

**+ عروض التأمين للعميل:** `proposals` (4: عرض ⇒ رؤية العميل **بلا بيانات عمولة** ⇒ قبول ⇒ أمر إسناد AWARDED + SELECTED · منع قرار مكرّر 409 · الرفض يوثّق ولا يُسنِد · عزل مستأجر آخر 404 · حواجز بلا عروض 400/مجهول 404/RBAC 403).

**+ مذكرة التغطية المؤقتة:** `cover-note` (3: إصدار لطلب مُسنَد بشروط العرض + منع تكرار + رؤية العميل والمستند · **الاستبدال التلقائي عند إصدار الوثيقة** (superseded + مرتبطة) · حواجز: طلب غير مُسنَد 409 · عزل 404 · RBAC 403).

**+ التسوية البنكية:** `bank` (2: إنشاء حساب ⇒ استيراد كشف ⇒ مطابقة إيداع بسند قبض ⇒ تجاهل رسوم بنك ⇒ **مُسوّى** (المطابَق/غير المطابَق/رصيد الكشف) · حواجز: سند غير نقدي 400 · تكرار مطابقة 409 · استيراد فارغ 400 · حساب مجهول 404 · عزل + RBAC 403).

**الإجمالي: 387 اختباراً (54 ملفاً).**

### قاعدة اختبار منفصلة (`ibp_test`)

منذ المرحلة 8ب، تعمل الاختبارات على قاعدة **`ibp_test`** مستقلّة كي لا تلوّث بيانات العرض في `ibp_dev`. الآلية: [`setup-e2e.ts`](../apps/api/test/setup-e2e.ts) يحمّل `.env` ثم **يتجاوز `DATABASE_URL`** من [`.env.test`](../.env.test) إن وُجد. التهيئة لمرّة واحدة:

```bash
pnpm --filter @ibp/db test:setup   # reset (تصفير) + migrate + seed على ibp_test — التصفير يمنع تراكم المستخدمين عبر التشغيلات
pnpm --filter @ibp/api test:e2e    # الاختبارات تعمل على ibp_test تلقائياً
```

## 3. التشغيل

```bash
# تتطلّب قاعدة مزروعة:
pnpm db:seed
pnpm --filter @ibp/api test:e2e
```
المتوقّع: `Test Suites: 36 passed`, `Tests: 238 passed`.

## 4. الإستراتيجية

- **اختبارات تكامل حقيقية:** تستخدم القاعدة المزروعة (مستأجران، أدوار، باقات) — تختبر الـ stack كاملاً (middleware → guards → service → Prisma → DB).
- **عزل صريح:** كل ميزة حسّاسة (عزل، RBAC، entitlement، حوكمة) لها اختبار منع صريح (مستأجر/دور لا يرى ما لا يخصّه).
- **idempotency:** الاختبارات التي تُنشئ بيانات تستخدم معرّفات/أرقاماً فريدة (مثل `Date.now()`) لتُعاد بأمان؛ واختبار العزل لا يثبّت أعداداً ثابتة.
- **التوسّع:** اختبارات الوحدة لمحرّك التحقّق والحسابات المالية تُضاف مع المرحلة 4ب وما بعدها (GUIDELINES.md §7 #4).

## انظر أيضاً
- [04 — الأمان وعزل المستأجرين](./04-security-and-multitenancy.md) · [05 — الصلاحيات](./05-rbac-and-entitlements.md)
- [08 — دورة حياة الصفقة](./08-deal-lifecycle-workflows.md) — الحوكمة المُختبَرة
- [18 — سير العمل و خارطة الطريق](./18-git-workflow-and-roadmap.md) — «تعريف تمّ»
