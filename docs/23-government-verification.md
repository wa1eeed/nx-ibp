# 23 — التحقّق الحكومي KYC/KYB (Government Verification)

> المرحلة 7: طبقة موفّري التحقّق الموحّدة — تعمل عبر **Sandbox تجريبي أولاً** (التكامل الحقيقي في المرحلة 9). تغذّي تعبئة النموذج ذكياً، وتخصم كل عملية مدفوعة من رصيد المستأجر وتسجّلها، وتفحص PEP/العقوبات. **الأرقام تقديرية وقابلة للتهيئة — لا تُكتب نهائياً في الكود.**

## جدول المحتويات
- [1. الموفّرون وأنواع التحقّق](#1-الموفّرون-وأنواع-التحقّق)
- [2. التعبئة الذكية](#2-التعبئة-الذكية)
- [3. الأرصدة وعدّاد العمليات](#3-الأرصدة-وعدّاد-العمليات)
- [4. فحص PEP/العقوبات](#4-فحص-pepالعقوبات)
- [5. الـ endpoints](#5-الـ-endpoints)
- [6. الاختبارات](#6-الاختبارات)

## 1. الموفّرون وأنواع التحقّق

| الموفّر | النوع | الاستخدام | التكلفة (تقديرية) |
|---|---|---|---|
| **يقين (Yaqeen)** | identity | KYC أفراد: الهوية/الإقامة + صلاحيتها | مدفوع (يُخصم) |
| **واثق (Wathiq)** | cr | KYB منشآت: السجل التجاري، الشركاء، **المستفيد الحقيقي (UBO)**، المفوّضون | مدفوع |
| **العنوان الوطني (SPL)** | address | العنوان الوطني | **مجاني** |
| **نفاذ (Nafath)** | auth | دخول وتوثيق العميل | مدفوع |
| **فحص (Screening)** | pep_sanctions | PEP/العقوبات وتصنيف المخاطر | الالتزام |

## 2. التعبئة الذكية

عند سحب البيانات، يُعيد الـ endpoint نتيجة الموفّر (مثل اسم/تاريخ ميلاد/صلاحية الهوية من يقين، أو اسم الشركة/الشركاء/UBO من واثق) لتعبئة النموذج الديناميكي تلقائياً — يقلّل الإدخال اليدوي ويرفع الدقّة.

## 3. الأرصدة وعدّاد العمليات

`Wallet` (رصيد عمليات لكل مستأجر/خدمة) + `TransactionLedger` (حركة الشحن/الخصم). الفوترة بنموذجين على مستوى المستأجر (`Tenant.billingModel`):

| النموذج | الآلية |
|---|---|
| **Pass-through** | المستأجر يربط حسابه مع المزوّد ويدفع عملياته؛ لا خصم من محفظتنا |
| **Reseller** | نخصم عملية واحدة من رصيد المستأجر لكل hit مدفوع، ونسجّلها في `TransactionLedger`؛ رصيد صفر ⇒ `402` (اشحن المحفظة) |

كل عملية ناجحة تُسجَّل في `VerificationCheck` (النوع، الحالة، التكلفة، الطابع الزمني، ربط بالعميل/الطلب) وفي `audit_log`.

## 4. فحص PEP/العقوبات

`POST /verification/screening` (صلاحية الالتزام) يعيد `riskLevel` (`low`/`medium`/`high`) ومطابقة PEP/العقوبات — أساس التصنيف الآلي للمخاطر وقرار القبول/الرفض في بوّابة الالتزام (انظر [08](./08-deal-lifecycle-workflows.md)).

## 4-أ. السجل التجاري عبر «البيانات المفتوحة» (وزارة التجارة)

مصدر **مجاني** بديل/مكمّل لواثق للتحقّق من السجل التجاري — مبنيّ على [داتاست السجلات القائمة](https://open.data.gov.sa/ar/datasets/view/aef772cd-354c-48bb-9819-c60706dc8b56) (لقطة فصلية). لأن الداتاست يُنشَر **ملفًّا مجمَّعًا** (لا واجهة استعلام لحظي)، نستورده إلى جدول مرجعي محليّ ([`CrRegistryRecord`](../packages/db/prisma/schema.prisma)) فيصبح البحث **فوريًّا برقم السجل بلا نداء خارجي**.

- **البحث** [`CrRegistryService.lookup`](../apps/api/src/modules/verification/cr-registry.service.ts): يُطبّع الرقم (يقبل الأرقام العربية) ويعيد السجل نظيفًا (اسم · نشاط · كيان قانوني · رقم موحّد · منطقة · مدينة · رأس مال · نوع السجل · تاريخ الإنشاء) أو `found:false`. الوجود ⇒ يُسجَّل كعملية تحقّق **مجانية** (`checkType=cr_opendata`, provider `opendata`).
- **الاستيراد — مجلد إسقاط (drop folder)**: تجلب المنصّة البيانات من مجلد مخصّص [`data/cr-registry/`](../data/cr-registry/README.md) (أو `$CR_REGISTRY_DIR`). أسقِط ملفّات اللقطة (`.xlsx`) ثم:
  - `node packages/db/prisma/import-cr-dir.cjs` — يستورد **الجديد فقط** (يتخطّى المتطابق عبر جدول `CrRegistryImport` بمطابقة الاسم+الحجم+زمن التعديل)، فإضافة شركات جديدة = إسقاط ملفّ جديد.
  - `node packages/db/prisma/import-cr-dir.cjs --fresh` — لقطة فصلية نظيفة (يمسح ثم يعيد الاستيراد الكامل).
  - يُوصى بربطه بـ**cron** ليجلب الملفّات الجديدة آليًّا. المستورِد يبثّ ورقة الإكسل عبر `unzip -p` فيتحمّل الملايين بذاكرة منخفضة (`createMany` بالدُّفعات). ملفّات CSV تُستورَد بـ[`import-cr-registry.ts`](../packages/db/prisma/import-cr-registry.ts). البيانات لا تُرفَع على git.
  - عيّنة (١٢ سجلًّا) تُبذَر في `seed.ts` لتعمل الميزة فورًا قبل أول استيراد.
- **الواجهة**: بطاقة «التحقّق من السجل التجاري» في صفحة التحقّق — إدخال رقم ⇒ عرض فوريّ لكل بيانات السجل مع وسم المصدر واللقطة، أو «غير موجود في اللقطة».

## 5. الـ endpoints

| الطريقة والمسار | الصلاحية |
|---|---|
| `POST /verification/yaqeen` · `/wathiq` · `/address` | `clients:update` |
| `POST /verification/cr-registry` (سجل تجاري — بيانات مفتوحة، مجاني) · `GET /verification/cr-registry/meta` | `clients:update` / `clients:read` |
| `POST /verification/screening` | `compliance:update` |
| `GET /verification/wallets` · `/checks?clientId=` | `clients:read` |

## 6. الاختبارات

[`test/verification.e2e-spec.ts`](../apps/api/test/verification.e2e-spec.ts): سحب يقين يعبّئ النموذج و**يخصم عملية واحدة**، واثق (الشركاء/UBO)، العنوان الوطني مجاني (لا خصم)، فحص PEP (low/high)، منع RBAC (المحاسب من التحقّق، المبيعات من الفحص)، والعزل؛ و**السجل التجاري من البيانات المفتوحة**: بحث موجود (بيانات + عملية مجانية) · تطبيع الأرقام العربية · غير موجود · meta · منع RBAC. **12/12**.

## انظر أيضاً
- [03 — نموذج البيانات](./03-data-model.md) — `VerificationProvider`/`VerificationCheck`/`Wallet`/`TransactionLedger`
- [01 — نظرة عامة](./01-overview.md) §طبقة التحقّق · [17 — الامتثال](./17-compliance-and-regulatory.md)
- [`BLUEPRINT.md`](../BLUEPRINT.md) §7‑أ — طبقة موفّري التحقّق ونموذج الفوترة
