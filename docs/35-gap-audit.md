# 35 — التدقيق الشامل للنواقص (منظور رئيس شركة وساطة تأمين سعودية)

> فحصٌ من الألف إلى الياء لكل أقسام المنصة وصفحاتها وبوّاباتها وصلاحياتها، مقابل **متطلبات هيئة التأمين السعودية + ZATCA + التشغيل الفعلي لشركات الوساطة**. الهدف: تحديد ما ينقص ليصبح النظام أفضل منصّة لإدارة نشاط الوساطة بجميع أقسامها. **القسم المالي هو الأخطر** — بدون اكتماله لا تؤدّي المنصّة دورها.
>
> **المنهج:** كل بند مُتحقَّق من الكود (✅ موجود · ⚠️ جزئي · ❌ مفقود). الأولويات: **P0** (لا تشتغل/تمتثل الشركة بدونه) · **P1** (احترافية وتميّز) · **P2** (تحسينات). تاريخ التدقيق: 2026-07-13.

## ما هو مكتمل فعلًا (الأساس القويّ)
دورة الوساطة الأساسية مبنية باحترافية: العملاء + بوّابة الالتزام · الكتالوج + النموذج الديناميكي · الطلبات → السلِبس/عروض الأسعار → المقارنة → الإسناد · الإصدار + الموافقة الفنية/المالية + الملاحق · التجديدات · المطالبات (+محادثة) · خدمة العملاء (+محادثة) · بوّابة العميل · المحاسبة المزدوجة (Voucher/COA) + فاتورة ZATCA المرحلة 2 + دورة تحصيل/تسوية/ميزان مراجعة · العمولات (وسطاء فرعيون) · الإشعارات + التدقيق + MFA + DLP + الاحتفاظ. **e2e 287/287.** النواقص أدناه تُكمِّل هذا الأساس لا تستبدله. **e2e 373/373** بعد المالية + البوّابة + الدفع + عنقود الامتثال الكامل (الشكاوى + كشف المؤمِّن + AML) + التدفّق النقدي (القوائم المالية الثلاث مكتملة) + إكمال تير P1 + بدء P2 (تفضيلات إشعارات لكل دور §9.1).

---

## 1) القسم المالي (الأخطر) — المحاسبة العامة غير مكتملة

المبني: قيود تُولَّد **آليًا** من العمليات (إصدار/قبض/تسوية/إلغاء) + شجرة حسابات + فواتير ZATCA + ذمم + إشعار مدين/دائن + ميزان مراجعة + قائمة دخل مبسّطة. لكن **المحاسبة كنظام مالي متكامل ناقصة**:

| # | النقص | الحالة | لماذا حرِج | البناء |
|---|---|---|---|---|
| 1.1 | **قيد يومية يدوي + مصروفات تشغيلية** (إيجار/رواتب/كهرباء/تسويات) | ✅ **تمّ** | ~~لا يمكن مسك دفاتر الشركة~~ — بُني: تبويب «القيود والمصروفات» في المالية (سند JRV متوازن على حسابات الترحيل + قالب مصروف + سجلّ) · `POST /finance/journal` بتحقّق صارم (≥طرفين · XOR · توازن · حساب ورقة) · **شجرة حسابات مُثراة** (نقد/بنوك · ضريبة مدخلات · حقوق ملكية · 7 حسابات مصروف). يتدفّق لميزان المراجعة. e2e ✓ |
| 1.2 | **قائمة المركز المالي (الميزانية العمومية)** | ✅ **تمّ** | ~~قائمة دخل فقط~~ — بُنيت: تبويب «الميزانية العمومية» + `GET /finance/balance-sheet` تُشتقّ من ميزان المراجعة مصنّفًا (أصول/خصوم/حقوق ملكية) · **أصول = خصوم + حقوق ملكية + صافي الدخل** (يتوازن حتميًا بالقيد المزدوج) · **الأمانات مُعلَّمة خارج الميزانية** (fiduciary) + مذكرة إفصاح · تنبيه لأي حساب بلا تصنيف. e2e ✓ · **بيان التدفّق النقدي** (`GET /finance/cash-flow`، مباشر: تشغيلي/استثماري/تمويلي حسب الطرف المقابل لحركة النقد 0101 · الختامي = افتتاحي + صافي التغيّر · تسوية حتمية) ✓ — **القوائم المالية الثلاث مكتملة** |
| 1.3 | **أعمار الذمم المدينة (AR aging 30/60/90)** | ✅ **تمّ** | ~~إدارة التحصيل عمياء زمنيًا~~ — بُني: `receivables()` يوزّع كل ذمّة قائمة على شرائح **جارٍ (0–30) / 31–60 / 61–90 / +90** إجمالاً و**حسب العميل** (`agingByClient`) + **عمر كل إشعار** (`ageDays`) + مذكرة المتأخّر. UI: بطاقات شرائح ملوّنة (جارٍ أخضر ← +90 أحمر) + عمود العمر في جدول الذمم بتدرّج تحذيري. e2e ✓ (تأريخ حتمي للإشعارات ⇒ توزيع صحيح) |
| 1.4 | **دفتر الأستاذ العام (GL) بالتنقّل لكل حساب + رصيد جارٍ** | ✅ **تمّ** | ~~ميزان مراجعة (لقطة) فقط~~ — بُني: `GET /finance/ledger/:account` يجمع كل أطراف القيود على الحساب مرتّبةً زمنيًا بـ**رصيد جارٍ** · **تنقّل (drill-down)** بنقر أي حساب في ميزان المراجعة أو الميزانية العمومية ⇒ كشف حركته. e2e ✓ (الرصيد الجارٍ يطابق ميزان المراجعة) |
| 1.5 | **إقرار ض.ق.م (VAT return)** | ✅ **تمّ** | ~~لا تقرير إقرار~~ — بُني: تبويب «الإقرار الضريبي» + `GET /finance/vat-return?from&to` — **ضريبة المخرجات** (0203) − **ضريبة المدخلات** (0105) = **صافي المستحقّ للهيئة** (سالب ⇒ رصيد استرداد) + القاعدة الخاضعة القياسية · محدّد فترة (ربعي/شهري) · المبيعات صفرية النسبة (الحياة) مستثناة. e2e ✓ |
| 1.6 | **إدارة الحسابات البنكية + التسوية البنكية (Bank reconciliation)** | ✅ **تمّ** | ~~لا حسابات بنكية ولا مطابقة~~ — بُنيت وحدة `bank`: **حسابات بنكية متعددة** (`BankAccount`) + **استيراد كشف البنك** (`BankTransaction`، موجب إيداع/سالب سحب) + **مطابقة** كل حركة بسند نظام (RCV/PYV، منع تكرار/غير نقدي) + **تجاهل** رسوم البنك + **ملخّص تسوية** (رصيد الكشف/المطابَق/الفرق + سندات غير مرتبطة) + `reconciled`. صفحة `/tenant/finance/bank`. e2e ✓ |
| 1.7 | **سندات مطبوعة (قبض/صرف) + مرتجع فعلي للعميل** | ✅ **تمّ** | ~~لا سند مطبوع ولا صرف مرتجع~~ — بُني **مستند سند مطبوع** (`GET /finance/vouchers/:id/document` — RCV/PYV/JRV بهوية المستأجر + الطرف المقابل + أطراف القيد) وصفحة طباعة + روابط على القيود · **صرف مرتجع فعلي** (`POST /finance/credit-notes/:id/refund` ⇒ سند PYV: مدين ذمم 0103/دائن نقد 0101 + تعليم CNP «مصروف»، منع تكرار) + قسم «المرتجعات» في الذمم. e2e ✓ |
| 1.8 | **موازنة تقديرية (Budget vs Actual)** مالية | ✅ **تمّ** | ~~لا مقارنة فعلي بموازنة~~ — بُنيت وحدة `budget`: نموذج `BudgetLine` (سنة/فترة سنوية أو ربعية/حساب/مبلغ) + **الموازنة مقابل الفعلي** (`GET /finance/budget/vs-actual`) — الفعلي يُشتقّ آليًا من حركة السندات على الحساب ضمن نطاق تاريخ الفترة بطبيعة الحساب، و**الانحراف** = الفعلي − الموازنة (+نسبة). `POST/DELETE /finance/budget` (upsert/حذف، تحقّق فترة/حساب). صفحة `/tenant/finance/budget` (منتقي سنة/ربع + إضافة/تعديل inline + جدول انحراف ملوّن). e2e ✓ |

## 2) التحصيل والمدفوعات

| # | النقص | الحالة | لماذا حرِج | البناء |
|---|---|---|---|---|
| 2.1 | **خطة تقسيط الأقساط** (تواريخ ومبالغ مخصّصة لكل قسط + متابعة) | ✅ **تمّ** | ~~تحصيل جزئي حرّ فقط بلا جدول~~ — بُني: نموذج `Installment` · `POST /finance/debit-notes/:id/installments` بنمطين: **توزيع سريع** (شهري 2–36) أو **جدول مخصّص** `schedule[]` (تاريخ ومبلغ لكل قسط، المجموع = الإجمالي وإلا 400) · **تعديل خطة قائمة** `PUT …/installments` (استبدال الجدول + إعادة توزيع المُحصَّل بالأقدم) · **الشلال**: التحصيل بالأقدم استحقاقًا · حالة كل قسط (مسدَّد/جزئي/متأخّر/قادم) · تبويب «الذمم والتقسيط» (محرّر جدول مخصّص + **لوحة متابعة**: متأخّرة/خلال 7 أيام/قادمة عبر `GET /finance/installments/tracking`) · **عرض الجدول في بوّابة العميل** · **تذكير قبل الاستحقاق بـ3 أيام** (`installment_upcoming`) + عنده (`installment_due`) حسب التاريخ المخصّص لكل قسط. e2e ✓ |
| 2.2 | **بوّابة دفع إلكتروني للأقساط** (Tap/Moyasar) | ✅ **تمّ** | **أ) إعدادات الدفع:** `TenantPaymentSettings` (BYO — Tap/Moyasar، **المفتاح السرّي مشفّر at-rest**) + صفحة الإعدادات + `GET/PUT /config/payment`. **ب) دفع العميل:** `POST /portal/pay` ⇒ شحنة عبر بوّابة المستأجر (`makeTenantGateway`، `TapGateway`/`MoyasarGateway`/`SandboxGateway`) ⇒ عودة/`POST /payments/webhook` (توقيع مُتحقَّق) ⇒ **سند قبض تلقائي** (`recordReceipt` + شلال الأقساط) بحتمية · زرّ «ادفع» في كشف حساب البوّابة + صفحة نتيجة. **ج) اشتراكات المنصّة:** عبر تجريد بوّابة `billing` نفسه (Tap). e2e ✓ (شلال + webhook موقّع + idempotent) |

## 3) العمولات

| # | النقص | الحالة | لماذا حرِج | البناء |
|---|---|---|---|---|
| 3.1 | **عمولات الموظفين** (مندوبي المبيعات) | ✅ **تمّ** | بُني: `User.commissionRate` (يُضبط من صفحة الموظف) · احتساب `salespersonCommission` عند الإصدار · دفتر **بالاستحقاق عند التحصيل** (متوقّعة/مستحقّة/مدفوعة/متبقّية) · صرف بسند PYV على حساب «عمولات وحوافز الموظفين» (05020). e2e ✓ |
| 3.2 | **توحيد العمولات في القسم المالي** (موظفون + وسطاء فرعيون) | ✅ **تمّ** | تبويب «العمولات» في `/tenant/finance` يجمع دفتر الموظفين + دفتر الوسطاء الفرعيين مع الصرف لكلٍّ |

## 4) الاكتتاب والإنتاج والعرض للعميل

| # | النقص | الحالة | لماذا حرِج | البناء |
|---|---|---|---|---|
| 4.1 | **عرض العروض للعميل + قبوله** (Proposal/Quote presentation) | ✅ **تمّ** | ~~داخلي فقط~~ — الوسيط يعرض عروضًا منتقاة (`/slips/:id/present`) ⇒ العميل يراها في بوّابته **بلا بيانات عمولة الوسيط**، **يقبل عرضًا ⇒ أمر إسناد (الطلب AWARDED)** أو يرفض. صفحة `/portal/proposals` (بطاقات خيارات + قبول/رفض) + شارة قرار العميل على صفحة الوسيط. إشعارات `proposal_ready`/`staff_proposal_accepted/declined`. e2e ✓ |
| 4.2 | **مذكرة تغطية مؤقتة (Cover Note / Binder)** | ✅ **تمّ** | ~~غير مُنمذجة~~ — بُني نموذج `CoverNote` يُصدَر لطلب مُسنَد بشروط العرض المختار + **صلاحية زمنية** (`COV-`) · `POST /cover-notes` (منع تكرار/غير مُسنَد 409) + مستند مطبوع بهوية المستأجر · **استبدال تلقائي** (superseded) عند إصدار الوثيقة · بوّابة العميل تعرضه وتطبعه · زرّ الإصدار على صفحة الإسناد. e2e ✓ |

## 5) بوّابة العميل

| # | النقص | الحالة | لماذا حرِج | البناء |
|---|---|---|---|---|
| 5.1 | **توفير حساب دخول للعميل** (Provisioning/Invite) | ✅ **تمّ** | ~~العميل الحقيقي لا يستطيع الدخول~~ — بُني: تبويب «دخول البوّابة» على صفحة العميل 360° (دعوة ببريد/اسم + رابط تفعيل قابل للنسخ + قائمة مستخدمين بحالة التفعيل + إلغاء دخول). الدعوة تُنشئ `ClientUser` بلا كلمة مرور + توكن موقّع (7 أيام) + بريد `portal_invite`. صفحة `/portal/activate` عامة: العميل يعيّن كلمة مروره ذاتيًا ⇒ **تسجيل دخول تلقائي**. e2e ✓ |
| 5.2 | **قبول العرض + الدفع من البوّابة** | ✅ **تمّ** | ~~ناقص~~ — الرحلة الذاتية مكتملة: يرى العرض (4.1) ⇒ يقبل ⇒ يُصدَر ويظهر الإشعار المدين في كشف حسابه ⇒ يدفعه أونلاين عبر بوّابة المستأجر (2.2-ب). تكامل القبول ← الإصدار ← الدفع |

## 6) الامتثال والتنظيم (متطلبات هيئة التأمين — سعودي)

| # | النقص | الحالة | لماذا حرِج | البناء |
|---|---|---|---|---|
| 6.1 | **سجلّ الشكاوى** (Complaints register) | ✅ **تمّ** | ~~لا سجلّ رسمي~~ — بُني: نموذج `Complaint` (فئة/مصدر/حالة/أولوية/**مهلة معالجة SLA**/إسناد/تصعيد للهيئة/حلّ) + رقم تسلسلي `CMP-` + ملاحظات (خط زمني) · صفحة `/tenant/complaints` (KPIs + فلاتر + تسجيل) وتفاصيل (معالجة/تصعيد/حالة) · **تقرير تنظيمي** (`/complaints/report`: تجميع بالفئة/الحالة + التزام SLA % + متوسّط زمن المعالجة + المُصعَّدة) · إشعار `staff_complaint_created`. صلاحية `compliance` + عزل. e2e ✓ |
| 6.2 | **AML/KYC** (مكافحة غسل الأموال) | ✅ **تمّ** | ~~لا تسجيل مخاطر/فرز/بلاغ~~ — بُنيت وحدة AML: **تقييم مخاطر** بعوامل موزونة (RBA) ⇒ درجة/مستوى + موعد مراجعة (عقوبات ⇒ مرتفع حتمًا) · **فرز عقوبات/PEP** (نظيف/محتمل/مؤكّد + تصرّف) · **سجلّ بلاغات الاشتباه (STR)** برقم `STR-` ودورة حياة (مسودّة⇒مرفوع⇒مغلق) · نظرة عامة + تقرير تنظيمي · صفحة `/tenant/aml` (3 تبويبات). صلاحية `compliance` + عزل. e2e ✓ · *(قوائم فرز حقيقية عبر مزوّد لاحقًا — 9.3)* |
| 6.3 | **حزمة تقارير هيئة التأمين** (Regulatory pack / Bordereaux) | ✅ **تمّ** | ~~لا كشوف مؤمِّنين~~ — بُني **كشف المؤمِّن الدوري** (`/reports/bordereau?insurer&from&to`): صفّ لكل وثيقة مُصدَرة (رقم/عميل/فرع/مؤمِّن/صافي/ض.ق.م/إجمالي/عمولة/**الصافي للمؤمِّن**) + مجاميع + منتقي مؤمِّنين · صفحة تقارير بمنتقي شركة + فترة + جدول تسوية. صلاحية `reports` + عزل. e2e ✓ · *(يتبقّى: صيغ تقديم رسمية للهيئة + تصدير — بند 7.1)* |
| 6.4 | **مدّة حق العدول (Free-look/Cooling-off)** | ✅ **تمّ** | ~~غير مُتتبَّعة~~ — بُني: `Policy.freeLookUntil` يُحتسب عند الإصدار من `freeLookDays` (سياسة تشغيل قابلة للتهيئة `TenantConfig.operationsPolicy`، افتراضي **0=مُعطَّل**، 0–90) · **الإلغاء ضمن النافذة ⇒ استرداد كامل** (بدل النسبة والتناسب) مع وسم القيد/الملحق بـ«ضمن حق العدول» · شارة «حق العدول» على قائمة الوثائق + ضبط المدّة في إعدادات سلسلة الاعتماد. `GET/PUT /config/operations`. e2e ✓ |

## 7) التقارير والتحليلات

| # | النقص | الحالة | لماذا حرِج | البناء |
|---|---|---|---|---|
| 7.1 | **تصدير التقارير (Excel/PDF)** | ✅ **تمّ** | ~~على الشاشة فقط~~ — بُني **تصدير CSV** (`GET /reports/export/:key` — Bordereau/العمولات) بترميز **UTF-8 + BOM** (يفتح في Excel بعربية سليمة) + زرّ «تصدير CSV» يحترم المرشّحات (عبر `downloadFile` بترويسة مصادقة). PDF عبر طباعة المتصفّح لصفحات التقارير/المستندات المُنسَّقة. e2e ✓ |
| 7.2 | **اكتمال كتالوج التقارير الـ12** | ✅ **تمّ** | ~~ليست كلها مبنية~~ — تقارير الكتالوج الـ12 كلها حيّة بمصادرها (إنتاج/عمولات/ذمم/مطالبات/تنظيمي/كشف المؤمِّن/تحويل/تجديدات/مخاطر…)، والجدولية منها قابلة للتصدير |
| 7.3 | **تقارير مجدولة/بالبريد** | ✅ **تمّ** | ~~لا إرسال دوري~~ — بُني نموذج `ReportSchedule` (تقرير/دورية أسبوعية أو شهرية/مستلمون) + خدمة توزيع تبني **ملخّصًا مُعنوَنًا** (لوحة/عمولات/كشف المؤمِّن) داخل سياق المستأجر وترسله عبر بريد الشركة (`sendTenantEmail`). **يُوزَّع عبر مجدول التذكيرات اليومي** عند حلول `nextRunAt`، أو فورًا بـ«إرسال الآن». `/reports/schedules` (CRUD + `:id/run-now`). قسم في صفحة التقارير. e2e ✓ |

## 8) الموارد البشرية (ثانوي — خارج نواة الوساطة جزئيًا)

| # | النقص | الحالة | لماذا | البناء |
|---|---|---|---|---|
| 8.1 | **الرواتب (Payroll)** | ✅ **تمّ** | ~~لا كشف رواتب~~ — بُنيت وحدة `payroll`: نموذجا `PayrollRun`(فترة/حالة) + `PayrollLine`(موظف: أساسي/بدلات/استقطاعات ⇒ صافي). إنشاء كشف شهري يُعبَّأ آليًا بالموظفين النشطين، تُعدَّل بنوده، ثم **الترحيل** يولّد **سند مصروف** (مدين الرواتب 05030 / دائن النقد 0101) بصافي الكشف ⇒ يظهر في ميزان المراجعة. صفحة `/tenant/finance/payroll`. عمولات الموظفين (§3.1) منفصلة (05020). e2e ✓ |
| 8.2 | **إجازات/حضور/مستندات الموظف/نطاقات** | ✅ **تمّ (السلِّة الأعلى قيمة)** | بُنيت **طلبات الإجازات** (وحدة `leave`، نموذج `LeaveRequest`): الموظف يقدّم طلبًا (نوع/فترة/أيام) ويرى طلباته؛ الإدارة (`settings`) تراها وتبتّها (معلّق ⇒ موافَق/مرفوض) بـ**فصل مهام** (لا يبتّ الموظف طلبه). صفحة `/tenant/settings/leave` (تقديم + طلباتي + إدارة). e2e ✓. **خارج النطاق عمدًا:** الحضور/البصمة وتعمّق الـHRIS (يُكامَل مع نظام موارد بشرية مختصّ)؛ مستندات الموظف عبر المستودع المركزي القائم. |

## 9) تقني/منصة

| # | النقص | الحالة | البناء |
|---|---|---|---|
| 9.1 | **تفضيلات إشعارات لكل دور** | ✅ **تمّ** | ~~مصفوفة دور×نوع~~ — بُني نموذج `NotificationPreference` (دلالة opt-out) + مصفوفة **دور × نوع إشعار موظفين** في `/tenant/settings/notifications` (تبديل فوري `PUT /notifications/preferences`). `notifyStaff` يستبعد الأدوار المكتومة (يشمل المالك)؛ إشعارات **الإسناد المباشر** (`notifyUser`) تتجاوزها. e2e ✓ |
| 9.2 | **فصل المهام على الموافقة الفنية** | ✅ **تمّ** | ~~حارس المعتمِد ≠ المُصدِر~~ — بُني علَم **اختياري** `technicalSegregation` (افتراضي مُعطَّل — لا يفرض على القائم) يُطبِّق maker-checker على **البوّابة الفنية + خطواتها الإضافية**: المعتمِد ≠ مُصدِر الوثيقة (يُشتقّ من سجل التدقيق الثابت). مستقلّ عن فصل المهام المالي (`segregationOfDuties`). تبديل في `/tenant/settings/approval-chain`. e2e ✓ |
| 9.3 | **تكاملات التحقّق الفعلية** (نفاذ/يقين/نجم/CCHI) | ✅ **جاهز للإنتاج** | ~~stub فقط~~ — بُنيت **بوّابة تحقّق قابلة للتبديل** (`VerificationGateway`): `Sandbox` افتراضيًا · `Live` تُنادي endpoints الموفّرين الفعليين **بمفاتيح كل موفّر من البيئة (BYO)** مع **تراجُع آمن** إلى Sandbox عند غياب المفتاح/فشل النداء — نفس نمط البريد (Resend)/الدفع (Tap). تُختار بـ`VERIFY_GATEWAY=live`. لوحة `/regulatory/status` تعكس **جاهزية كل موصِّل من البيئة** (`live`/`sandbox` + `configured`). **التفعيل الفعلي يحتاج عقود/مفاتيح الجهات** (خارج الكود). e2e ✓ |
| 9.4 | **تعدّد العملات** (بحري/طيران بالدولار) | ✅ **تمّ** | ~~لا دعم عملة~~ — بُني: `Policy.currency` + `fxRate` (سعر التحويل للريال) يُلتقطان عند الإصدار (SAR ⇒ 1؛ عملة أجنبية بلا سعر ⇒ 409). **الدفاتر بالعملة الوظيفية (الريال):** الاعتماد المالي والإلغاء يُرحّلان كل المبالغ **× سعر الصرف** (القيد/إشعار المدين/المرتجع بالريال)، والوثيقة تحتفظ بعملتها للعرض (شارة العملة على القائمة). SAR ⇒ fx=1 ⇒ صفر أثر رجعي. e2e ✓ (USD 3.75 ⇒ 69000×3.75=258750 ريال) |

---

## خارطة الأولويات (ترتيب البناء المقترح)

**P0 — بدونها المنصّة لا تُشغِّل شركة وساطة ولا تمتثل (تبدأ بالمالية):**
1. **إكمال المحاسبة العامة** (~~1.1 قيد يومية+مصروفات~~ ✅ · ~~1.2 ميزانية عمومية~~ ✅ · ~~1.4 دفتر أستاذ~~ ✅ · ~~1.5 إقرار ض.ق.م~~ ✅ · ~~1.2 التدفّق النقدي~~ ✅) — *أساس النظام المالي مكتمل (القوائم المالية الثلاث)*.
2. ~~**أعمار الذمم + إدارة التحصيل** (1.3)~~ ✅.
3. ~~**خطة التقسيط** (2.1)~~ ✅ + ~~**الدفع الإلكتروني** (2.2)~~ ✅.
4. **العمولات الموحّدة** (3.1 موظفون + 3.2 إبراز الوسطاء في المالية).
5. ~~**توفير دخول بوّابة العميل** (5.1)~~ ✅.
6. **الامتثال** ✅ (عنقود مكتمل): ~~سجلّ الشكاوى (6.1)~~ ✅ · ~~حزمة تقارير الهيئة/كشوف المؤمِّنين (6.3)~~ ✅ · ~~AML/KYC (6.2)~~ ✅ — *امتثال ترخيصي*.

**P1 — احترافية وتميّز تنافسي:**
7. ~~عرض العروض للعميل + قبوله + الدفع الذاتي (4.1 + 5.2)~~ ✅.
8. ~~مذكرة التغطية المؤقتة (4.2)~~ ✅ · ~~سندات مطبوعة + مرتجعات (1.7)~~ ✅ · ~~التسوية البنكية (1.6)~~ ✅ **— تير P1 #8 مكتمل**.
9. ~~تصدير التقارير (7.1) + إكمال الكتالوج (7.2)~~ ✅ **— تير P1 مكتمل بالكامل**.

**P2 — تحسينات:**
10. ~~إشعارات لكل دور (9.1)~~ ✅ · ~~SoD فني (9.2)~~ ✅ · ~~موازنة (1.8)~~ ✅ · ~~تقارير مجدولة (7.3)~~ ✅ · ~~تكاملات فعلية (9.3)~~ ✅ (جاهز للإنتاج — يحتاج مفاتيح الجهات) · ~~تعدّد عملات (9.4)~~ ✅ · ~~حق العدول (6.4)~~ ✅ · ~~رواتب (8.1)~~ ✅ · ~~إجازات/HR خفيف (8.2)~~ ✅.

> **✅ اكتملت كل النواقص الوظيفية في هذا التدقيق (P0 · P1 · P2).** المتبقّي **خارج الكود**: تفعيل التكاملات الحكومية بمفاتيح الجهات (§9.3) · إعادة كتابة التوثيق بالإنجليزية · إنشاء نسخة Production (الحالية = Staging) · تحصين أمني نحو الربط الحكومي ([30](./30-security-and-compliance.md)) — انظر [29](./29-roadmap-next.md).

---

## نطاق المرحلة المالية الأولى (مؤكّد) + قرارات تصميمية

تأكيدٌ أن **تقسيط العميل + عمولات الموظفين + سندات صرف العمولات + تفاصيل عمولات الموظفين والوسطاء الفرعيين** ضمن نطاق البناء المالي الأول (بنود 2.1/3.1/3.2 أعلاه). ملاحظات الخبير لضمان **دقّة البيانات**:

- **ما هو موجود فعلًا (نُبرزه لا نبنيه):** عمولات **الوسطاء الفرعيين** ودفترهم **وسند صرفهم** مبنية (`/producers/:id/settle` ⇒ سند PYV بقيد مصروف حساب `05010`). المطلوب: **توحيدها في القسم المالي** ضمن تبويب «العمولات».
- **ما هو جديد بالكامل:** عمولات **الموظفين** (دفتر + تفاصيل لكل وثيقة/فترة + سند صرف PYV بحساب مصروف جديد «عمولات/حوافز الموظفين» يُضاف للـCOA). تصنيفها: **تعويض داخلي (HR/مالي)، لا التزام تنظيمي**.
- **قرار تصميمي — استحقاق عمولة الموظف:** الموصى به **عند تحصيل الوسيط لعمولته من المؤمِّن** (لا عند الإصدار) — أأمن وأأدقّ ماليًا (لا ندفع على عمل قد يسقط)؛ تُعرَض بحالات «متوقّعة/مستحقّة/مدفوعة».
- **قرار تصميمي — تقسيط العميل مقابل تسوية المؤمِّن:** الموصى به أن يكون جدول تقسيط العميل **مستقلًّا** عن توقيت تسوية المؤمِّن (الوسيط يورّد حسب اتفاقه، والتقسيط شأن تحصيلي)؛ الذمّة تُثبَت **كاملة** عند الإصدار وكل قسط = سند قبض. (يُضاف لاحقًا خيار «تقسيط متزامن مع المؤمِّن» عند الحاجة.)

## مبدأ البناء (مُتّفَق عليه)
كل نقص يُبنى **بأقصى جودة ومعايير عالمية**: تجربة مستخدم راقية · **دقّة وشمول البيانات** (قيود متوازنة، أثر محاسبي صحيح، عزل بالمستأجر، حارس صلاحيات) · تغطية e2e · توثيق docs/+CHANGELOG ثم رفع GitHub — في كل مرة ([[ibp-docs-after-each-phase]]).

## انظر أيضاً
[03 — البيانات](./03-data-model.md) · [06 — API](./06-api-reference.md) · [20 — الإصدار والمالية](./20-issuance-and-finance-core.md) · [29 — خارطة الطريق](./29-roadmap-next.md) · [30 — الأمن والامتثال](./30-security-and-compliance.md) · [34 — طبقة ما بعد الاكتمال](./34-post-completion-features.md) · [CHANGELOG](../CHANGELOG.md)
