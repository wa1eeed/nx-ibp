# infra — البنية التحتية

| المجلد | الغرض | المرحلة |
|---|---|---|
| `docker/` | ملفات Docker مساعدة للتطوير (الصور الأساسية في `apps/*/Dockerfile`، والتنسيق في `docker-compose.yml` بالجذر) | 0 |
| `k8s/` | بيانات Kubernetes للنشر داخل المملكة | 9 |
| `terraform/` | بنية تحتية ككود (حيادي سحابياً: AWS/GCP/Alibaba) | 9 |

> توطين الإنتاج داخل المملكة يشمل قاعدة البيانات **والنسخ الاحتياطية والسجلات والمرفقات** (BLUEPRINT §7‑ب).
> بيئات التطوير ببيانات وهمية فقط، والتكامل الحكومي عبر Sandbox.
